{"id":"bd-11b","title":"ai chat: SmolLM2 loads and generates, but the CHAT path returns degenerate text","description":"Newly visible on 2026-07-27 once the tokenizer fix (AID-0054) let SmolLM2-360M actually load. Previously masked by the EchoAdapter fallback.\n\nTHE CONTRAST, same model file, same weights, same box:\n\n  raw generate() via netfetch_end_to_end:\n    prompt 'The capital of France is'  ->  ' Paris.'          CORRECT\n\n  kopitiam ai chat:\n    'What is the capital of France?'   ->  'The answer is the answer to the question:'   DEGENERATE\n\nSo the runtime, loader, tokenizer and forward pass are all fine — raw completion gives the right answer. What differs is that the chat path wraps the prompt in a chat template. Something in that wrapping is degrading the model.\n\nPRIME SUSPECT, and it already has a bead: bd-250.3 says the chat template is ChatML-only with no per-architecture dispatch. SmolLM2-Instruct DOES use ChatML (<|im_start|>role ... <|im_end|>), so the template family is right — which makes the likelier fault the SPECIAL TOKENS not being registered from the GGUF. If <|im_start|> is not a registered special token it gets BPE-split into literal characters, and the model sees garbage framing instead of a turn marker. 'kopitiam models inspect' on this file reports 17 CONTROL-type tokens, so the file HAS them; the question is whether the tokenizer build path registers them.\n\nNOT YET CONFIRMED — this is a hypothesis with strong circumstantial evidence, not a diagnosis. Reproduce first, then check whether tokenizer_from_gguf registers tokenizer.ggml.* special tokens (bos/eos/unknown/padding + the CONTROL entries), and whether encode() of a full templated prompt yields the special ids or a run of byte tokens. Do not fix before that check — the last two bugs in this area both had two opposite plausible causes.\n\nRepro:\n  printf 'What is the capital of France?\\n' | kopitiam ai chat","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-27T14:22:26.932000Z","updated_at":"2026-08-24T23:55:42.624000Z","closed_at":"2026-07-27T15:47:37.040000Z","close_reason":"done","external_ref":"gh-53","labels":["kopitiam-ai","models"]}
{"id":"bd-1wd","title":"Review AID-0050: kvim completion is now smart-case (nucleo Atom)","description":"AID-0050 (docs/ai-decisions/AID-0050-kvim-completion-smart-case.md), status Pending review.\n\nFixing the bd-8sh crash forced a case-matching policy choice, which is user-visible editor behaviour the maintainer would normally pick. Decision made without you: completion now uses nucleo::pattern::Atom with CaseMatching::Smart, so completion is SMART-CASE — all-lowercase prefix matches case-insensitively, any uppercase char makes the whole match case-sensitive. Same rule the kvim picker already uses.\n\nPractical effect: typing 'Th' will no longer offer 'this'. If you want completion to stay case-blind, reversal is one argument (CaseMatching::Smart -> CaseMatching::Ignore in merge_and_rank) plus the merge_and_rank_is_smart_case test.\n\nAlternatives weighed and why rejected are in the AID (prefix.to_lowercase(), CaseMatching::Ignore, CaseMatching::Respect, patching nucleo).","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-25T03:04:30.713000Z","updated_at":"2026-08-24T23:55:42.561000Z","external_ref":"gh-46","labels":["ai-decision"]}
{"id":"bd-250","title":"kopitiam-ai: replace Qwen default with a curated local model ladder","description":"Maintainer directive (2026-07-25): pivot kopitiam-ai's local-first default away from Qwen to a curated model ladder. Qwen is NOT used by default anymore; it is only loaded if the USER supplies a GGUF.\n\nProposed ladder (all open-weight, coding/agentic focus):\n- Local / small (1B, on-device, weak CPU): MiniCPM5-1B (openbmb) — 1.08B LlamaForCausalLM, Apache-2.0, 128K ctx, GGUF available. https://huggingface.co/openbmb/MiniCPM5-1B\n  - Alt 1B candidate: Maincoder-1B (Maincode). https://huggingface.co/Maincode/Maincoder-1B\n- Higher range (~3B): stable-code:3b (Stability AI StableCode). https://ollama.com/library/stable-code:3b\n- Stronger CPUs: North-mini-code (CohereLabs) — 30B MoE / 3B active, Apache-2.0, agentic coding, 128K ctx, BF16+FP8 (no GGUF noted yet). https://huggingface.co/blog/CohereLabs/introducing-north-mini-code\n- Qwen: opt-in only, when the user supplies a GGUF.\n\nImpact / open questions:\n- Existing local stack (kopitiam-tensor/runtime/loader/tokenizer) targets Qwen2 + Llama dense arch. MiniCPM5-1B is LlamaForCausalLM (fits). North-mini-code is MoE (NOT supported by the dense loader — needs work) and has no GGUF yet.\n- Requires an AID (supersedes/updates AID-0029 adapter selection, and the Qwen assumptions in kopitiam-ai crate docs + docs/SESSION-STATE.md).\n- Decide selection policy: default seed model, capability/CPU-based tier selection, user-GGUF override path.\n\nCaptured per AGENTS.md (maintainer prompt -> bead, not acted on live). Not yet implemented.","notes":"License filter: drop stable-code:3b (Stability AI) from the ladder — its community/non-commercial license is incompatible with KOPITIAM's AGPLv3. The ~3B 'higher range' tier is now open (needs an Apache-2.0/MIT/compatible ~3B coder). All remaining ladder models must be OSI-compatible with AGPLv3; MiniCPM5-1B, Maincoder-1B, North-mini-code are Apache-2.0.\n\nPREFERRED on-device default: google/gemma-4-E2B. ~2.3B effective params (5.1B w/ embeddings), multimodal (text+image, audio), coding/reasoning, 128K ctx, GGUF available. LICENSE: Apache-2.0 per authoritative license page (ai.google.dev/gemma/docs/gemma_4_license shows standard Apache-2.0 text, OSI-approved) — AGPLv3-compatible. Gemma 4 moved OFF the old custom 'Gemma Terms of Use'. CAVEAT: card links a separate 'Prohibited use'/'Intended use' policy; core grant is Apache-2.0 but verify that policy is advisory, not incorporated into the license, before shipping. This fills the ~3B tier vacated by stable-code. ENGINEERING IMPACT: Gemma arch (hybrid local/global sliding-window attention + Per-Layer Embeddings) is NOT the current dense Qwen2/Llama loader — needs loader work like North-mini-code; and multimodal is scope beyond the text-only runtime (text-only path first). https://huggingface.co/google/gemma-4-E2B\n\nLICENSE SPLIT (authoritative, ai.google.dev/gemma/terms): the Gemma family is NOT uniformly GPLv3-compatible.\n- Gemma 4: Apache-2.0 (OSI) → AGPLv3-compatible → OK to make default + BUNDLE/redistribute with kopitiam.\n- Gemma 1/2/3/3n + others: 'Gemma Terms of Use' custom license, NOT OSI. Section 3.2 + Prohibited Use Policy: 'You must include the use restrictions ... as an enforceable provision in any agreement governing the use and/or distribution.' Passing restrictions downstream violates GPLv3/AGPLv3 §7 (no further restrictions) → INCOMPATIBLE. kopitiam must NOT bundle or default to these; they may only be USER-SUPPLIED GGUF (same opt-in path as Qwen), where kopitiam never distributes the weights.\nRECOMMENDATION: default = Gemma 4 family only (gemma-4-E2B on-device; larger Gemma-4 variants for stronger CPUs). Older Gemma + Qwen = user-supplied-GGUF only. Maincoder-1B/MiniCPM5-1B/North-mini-code (all Apache-2.0) remain compatible bundleable alternatives.\n\nDECISION CONFIRMED by maintainer (2026-07-25): default local family = Gemma 4 (Apache-2.0), bundled — gemma-4-E2B on-device, larger gemma-4-* for stronger CPUs. Pre-4 Gemma (1/2/3/3n) and Qwen are USER-SUPPLIED GGUF ONLY, never bundled/redistributed. North-mini-code / MiniCPM5-1B / Maincoder-1B remain Apache-2.0 bundleable alternatives. stable-code rejected (license). Scope for implementation is now fixed; ready for an AID.\n\ngemma3n:e4b considered: rejected as BUNDLED/DEFAULT on two grounds. (1) License: Gemma 3n is pre-4 → Gemma Terms of Use (not OSI, AGPLv3-incompatible) → user-supplied-GGUF only, per confirmed scope. (2) Not lighter: E4B is ~4B effective vs gemma-4-E2B ~2.3B effective, so the Apache-2.0 default is already lighter. Outcome: gemma-3n (e2b/e4b) available only via the user-GGUF path; default stays gemma-4-E2B.\n\nRuntime loader constraints that shape the Gemma work: (a) K-quant support (Q4_K/Q6_K) is MISSING in kopitiam-loader (only classic quants + F16/BF16). (b) tokenizer path only accepts gpt2 byte-BPE, not SentencePiece — Gemma uses SPM. (c) gemma-4-E2B-it has no GGUF (safetensors only). So 'make Gemma chat' = arch support + SPM tokenizer support + (K-quant or safetensors) loading — three real pieces, not just an arch branch. Track as sub-work under this bead.\n\nCORRECTION (2026-07-27), from a real end-to-end run: the note above saying 'K-quant support (Q4_K/Q6_K) is MISSING in kopitiam-loader (only classic quants + F16/BF16)' is NO LONGER TRUE.\n\nsmollm2-1.7b-instruct-q4_k_m.gguf — a **Q4_K_M** file — was downloaded, sha256-verified, loaded, and generated text today: 'The capital of France is' -> ' Paris, the city of light, the'. So K-quants load and dequantize fine.\n\nThat leaves TWO of the three pieces standing for 'make Gemma chat', not three:\n  (a) Gemma architecture (hybrid local/global sliding-window attention + Per-Layer Embeddings) — still real work.\n  (b) SentencePiece tokenizer — still real work; the tokenizer path is still gpt2 byte-BPE only.\n  (c) K-quant or safetensors loading — DONE for K-quants. Only matters now if the chosen Gemma artifact is safetensors-only, which was the separate complaint about gemma-4-E2B-it having no GGUF.\n\nAlso relevant to the ladder: the byte-level BPE vocab check was too strict and blocked SmolLM2-360M (our DEFAULT_MODEL_ID) outright — fixed today, see AID-0054 / bd-3uh. SmolLM2 360M and 1.7B both now pass the full fetch -> load -> tokenize -> weights -> generate chain on real weights.","status":"open","priority":1,"issue_type":"feature","created_at":"2026-07-24T22:21:41.854000Z","updated_at":"2026-08-24T23:55:42.438000Z","external_ref":"gh-32","labels":["kopitiam-ai","local-first","models"],"comments":[{"id":1,"issue_id":"bd-250","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"License filter: drop stable-code:3b (Stability AI) from the ladder — its community/non-commercial license is incompatible with KOPITIAM's AGPLv3. The ~3B 'higher range' tier is now open (needs an Apache-2.0/MIT/compatible ~3B coder). All remaining ladder models must be OSI-compatible with AGPLv3; MiniCPM5-1B, Maincoder-1B, North-mini-code are Apache-2.0.","created_at":"2026-07-24T22:22:35.201000Z"},{"id":2,"issue_id":"bd-250","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"PREFERRED on-device default: google/gemma-4-E2B. ~2.3B effective params (5.1B w/ embeddings), multimodal (text+image, audio), coding/reasoning, 128K ctx, GGUF available. LICENSE: Apache-2.0 per authoritative license page (ai.google.dev/gemma/docs/gemma_4_license shows standard Apache-2.0 text, OSI-approved) — AGPLv3-compatible. Gemma 4 moved OFF the old custom 'Gemma Terms of Use'. CAVEAT: card links a separate 'Prohibited use'/'Intended use' policy; core grant is Apache-2.0 but verify that policy is advisory, not incorporated into the license, before shipping. This fills the ~3B tier vacated by stable-code. ENGINEERING IMPACT: Gemma arch (hybrid local/global sliding-window attention + Per-Layer Embeddings) is NOT the current dense Qwen2/Llama loader — needs loader work like North-mini-code; and multimodal is scope beyond the text-only runtime (text-only path first). https://huggingface.co/google/gemma-4-E2B","created_at":"2026-07-24T22:25:48.468000Z"},{"id":3,"issue_id":"bd-250","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"LICENSE SPLIT (authoritative, ai.google.dev/gemma/terms): the Gemma family is NOT uniformly GPLv3-compatible.\n- Gemma 4: Apache-2.0 (OSI) → AGPLv3-compatible → OK to make default + BUNDLE/redistribute with kopitiam.\n- Gemma 1/2/3/3n + others: 'Gemma Terms of Use' custom license, NOT OSI. Section 3.2 + Prohibited Use Policy: 'You must include the use restrictions ... as an enforceable provision in any agreement governing the use and/or distribution.' Passing restrictions downstream violates GPLv3/AGPLv3 §7 (no further restrictions) → INCOMPATIBLE. kopitiam must NOT bundle or default to these; they may only be USER-SUPPLIED GGUF (same opt-in path as Qwen), where kopitiam never distributes the weights.\nRECOMMENDATION: default = Gemma 4 family only (gemma-4-E2B on-device; larger Gemma-4 variants for stronger CPUs). Older Gemma + Qwen = user-supplied-GGUF only. Maincoder-1B/MiniCPM5-1B/North-mini-code (all Apache-2.0) remain compatible bundleable alternatives.","created_at":"2026-07-24T22:27:03.921000Z"},{"id":4,"issue_id":"bd-250","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"DECISION CONFIRMED by maintainer (2026-07-25): default local family = Gemma 4 (Apache-2.0), bundled — gemma-4-E2B on-device, larger gemma-4-* for stronger CPUs. Pre-4 Gemma (1/2/3/3n) and Qwen are USER-SUPPLIED GGUF ONLY, never bundled/redistributed. North-mini-code / MiniCPM5-1B / Maincoder-1B remain Apache-2.0 bundleable alternatives. stable-code rejected (license). Scope for implementation is now fixed; ready for an AID.","created_at":"2026-07-24T22:29:32.641000Z"},{"id":5,"issue_id":"bd-250","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"gemma3n:e4b considered: rejected as BUNDLED/DEFAULT on two grounds. (1) License: Gemma 3n is pre-4 → Gemma Terms of Use (not OSI, AGPLv3-incompatible) → user-supplied-GGUF only, per confirmed scope. (2) Not lighter: E4B is ~4B effective vs gemma-4-E2B ~2.3B effective, so the Apache-2.0 default is already lighter. Outcome: gemma-3n (e2b/e4b) available only via the user-GGUF path; default stays gemma-4-E2B.","created_at":"2026-07-24T22:30:09.869000Z"},{"id":6,"issue_id":"bd-250","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"Runtime loader constraints that shape the Gemma work: (a) K-quant support (Q4_K/Q6_K) is MISSING in kopitiam-loader (only classic quants + F16/BF16). (b) tokenizer path only accepts gpt2 byte-BPE, not SentencePiece — Gemma uses SPM. (c) gemma-4-E2B-it has no GGUF (safetensors only). So 'make Gemma chat' = arch support + SPM tokenizer support + (K-quant or safetensors) loading — three real pieces, not just an arch branch. Track as sub-work under this bead.","created_at":"2026-07-24T22:56:47.298000Z"},{"id":7,"issue_id":"bd-250","author":"fifad@Teddy-Thinkpad","text":"CORRECTION (2026-07-27), from a real end-to-end run: the note above saying 'K-quant support (Q4_K/Q6_K) is MISSING in kopitiam-loader (only classic quants + F16/BF16)' is NO LONGER TRUE.\n\nsmollm2-1.7b-instruct-q4_k_m.gguf — a **Q4_K_M** file — was downloaded, sha256-verified, loaded, and generated text today: 'The capital of France is' -> ' Paris, the city of light, the'. So K-quants load and dequantize fine.\n\nThat leaves TWO of the three pieces standing for 'make Gemma chat', not three:\n  (a) Gemma architecture (hybrid local/global sliding-window attention + Per-Layer Embeddings) — still real work.\n  (b) SentencePiece tokenizer — still real work; the tokenizer path is still gpt2 byte-BPE only.\n  (c) K-quant or safetensors loading — DONE for K-quants. Only matters now if the chosen Gemma artifact is safetensors-only, which was the separate complaint about gemma-4-E2B-it having no GGUF.\n\nAlso relevant to the ladder: the byte-level BPE vocab check was too strict and blocked SmolLM2-360M (our DEFAULT_MODEL_ID) outright — fixed today, see AID-0054 / bd-3uh. SmolLM2 360M and 1.7B both now pass the full fetch -> load -> tokenize -> weights -> generate chain on real weights.","created_at":"2026-07-27T13:18:20.135000Z"}]}
{"id":"bd-250.1","title":"kopitiam-ai: gate LocalAdapter::load on general.architecture — fail loud, not garbage","description":"TODAY THERE IS NO ARCHITECTURE GATE. crates/kopitiam-ai/src/local/adapter.rs:126 reads `general.architecture` from the GGUF ONLY to pick a tokenizer (`.or_else(|| loaded.metadata().architecture.clone())`). Nothing anywhere refuses a GGUF whose architecture the runtime cannot actually execute.\n\nkopitiam-runtime implements exactly ONE forward pass — QwenConfig/QwenModel (pre-norm residual, RoPE, grouped-query attention, SwiGLU MLP). Point KOPITIAM_MODEL_GGUF at a Gemma / Phi / Mistral-shaped GGUF and the loader may well parse the tensors, build a QwenModel over them, and start generating — producing NONSENSE rather than an error. Silent wrongness is worse than a crash: the user sees the TUI stream confident garbage with no hint the weights were never runnable.\n\nWHAT TO DO: after kopitiam-loader hands back metadata, check `general.architecture` against a whitelist of what the runtime genuinely implements (today: qwen2, and llama insofar as it is the same dense shape — CONFIRM the llama path is actually exercised before whitelisting it, don't assume). Unknown/unsupported architecture => a clean Err from LocalAdapter::load. adapter.rs's select_adapter already turns a load Err into FallbackReason::LoadFailed with a Singlish note, so the whole degradation path exists — this just needs to reach it.\n\nWHY THIS IS THE FIRST GEMMA BEAD: it is small, independent of every other item under bd-250, and it converts the Gemma failure mode from 'quietly wrong' to 'clearly told'. Worth doing even if the Gemma forward pass never lands.\n\nWHAT WOULD MAKE THIS WRONG: if a whitelist is too tight it locks out GGUFs that DO run fine today (some llama-family quants are dense-Qwen-shaped in every way that matters). Gate on 'can the runtime build a model from this', not on brand names — and test with the two GGUFs already in ~/.cache/kopitiam/models (qwen2.5-0.5b, llama-3.2-1b) to be sure neither regresses to Echo.\n\nFound while answering 'how to run kopitiam tui with gemma' (2026-07-25).","notes":"CORRECTION + SHARPENING (verified against the tree on 2026-07-25, not taken from another bead's notes):\n\nMy original framing — 'a Gemma GGUF may well load clean and emit garbage' — was OVERSTATED at the time of writing, and is becoming TRUE as the accidental backstops come down. The accurate picture:\n\nTWO GATES were incidentally catching non-Qwen models. Neither was designed as an architecture check.\n- QUANT GATE: GONE. bd-uij's comment records gguf.rs mapping only F32/F16/Q4_0/Q4_1/Q5_0/Q5_1/Q8_0/BF16, so K-quant files hard-failed at parse. That is NO LONGER TRUE in the working tree — dtype_from_ggml_type now maps ids 10-15 to Q2_K..Q8_K (the bd-3t0 K-quant work). Modern K-quant GGUFs parse today.\n- TOKENIZER GATE: still standing. kopitiam-runtime/src/gguf_tokenizer.rs accepts `tokenizer.ggml.model == \"gpt2\"` only. Most Gemma GGUFs are SentencePiece, so they still fail here — loudly, which is the correct outcome, but by accident rather than by design.\n\nTHE TIMING POINT, AND WHY THIS BEAD IS URGENT: bd-v3x item 2 is 'add SentencePiece tokenizer support'. The moment that lands, the LAST accidental guard is gone — and a Gemma GGUF will parse, tokenize, load into QwenModel, and generate confident nonsense with nothing left to stop it. THIS GATE MUST LAND BEFORE OR TOGETHER WITH THE SPM TOKENIZER WORK, not after. Sequenced any later and there is a window where the product silently lies.\n\nOPEN QUESTION WORTH ANSWERING WHILE BUILDING THIS (a claim I am NOT making, only flagging): llama-3.2-1b-instruct-q4_0 is in the shipped catalog and sitting in ~/.cache/kopitiam/models, and it is gpt2-BPE + Q4_0, so it presumably loads and runs through QwenModel TODAY. But Qwen2 and Llama are not the same dense architecture — Qwen2 carries QKV biases, Llama does not. Either bridge.rs handles that difference correctly, or the second catalog model has been producing subtly wrong output all along. Someone should establish which, with a reference comparison, before the whitelist is written — because the answer decides whether 'llama' belongs on it.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-24T23:59:43.279000Z","updated_at":"2026-08-24T23:55:42.498000Z","closed_at":"2026-07-28T14:08:00.941000Z","close_reason":"done","external_ref":"gh-39","labels":["kopitiam-ai","models","safety"],"dependencies":[{"issue_id":"bd-250.1","depends_on_id":"bd-250","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}],"comments":[{"id":1,"issue_id":"bd-250.1","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"CORRECTION + SHARPENING (verified against the tree on 2026-07-25, not taken from another bead's notes):\n\nMy original framing — 'a Gemma GGUF may well load clean and emit garbage' — was OVERSTATED at the time of writing, and is becoming TRUE as the accidental backstops come down. The accurate picture:\n\nTWO GATES were incidentally catching non-Qwen models. Neither was designed as an architecture check.\n- QUANT GATE: GONE. bd-uij's comment records gguf.rs mapping only F32/F16/Q4_0/Q4_1/Q5_0/Q5_1/Q8_0/BF16, so K-quant files hard-failed at parse. That is NO LONGER TRUE in the working tree — dtype_from_ggml_type now maps ids 10-15 to Q2_K..Q8_K (the bd-3t0 K-quant work). Modern K-quant GGUFs parse today.\n- TOKENIZER GATE: still standing. kopitiam-runtime/src/gguf_tokenizer.rs accepts `tokenizer.ggml.model == \"gpt2\"` only. Most Gemma GGUFs are SentencePiece, so they still fail here — loudly, which is the correct outcome, but by accident rather than by design.\n\nTHE TIMING POINT, AND WHY THIS BEAD IS URGENT: bd-v3x item 2 is 'add SentencePiece tokenizer support'. The moment that lands, the LAST accidental guard is gone — and a Gemma GGUF will parse, tokenize, load into QwenModel, and generate confident nonsense with nothing left to stop it. THIS GATE MUST LAND BEFORE OR TOGETHER WITH THE SPM TOKENIZER WORK, not after. Sequenced any later and there is a window where the product silently lies.\n\nOPEN QUESTION WORTH ANSWERING WHILE BUILDING THIS (a claim I am NOT making, only flagging): llama-3.2-1b-instruct-q4_0 is in the shipped catalog and sitting in ~/.cache/kopitiam/models, and it is gpt2-BPE + Q4_0, so it presumably loads and runs through QwenModel TODAY. But Qwen2 and Llama are not the same dense architecture — Qwen2 carries QKV biases, Llama does not. Either bridge.rs handles that difference correctly, or the second catalog model has been producing subtly wrong output all along. Someone should establish which, with a reference comparison, before the whitelist is written — because the answer decides whether 'llama' belongs on it.","created_at":"2026-07-25T00:02:33.603000Z"}]}
{"id":"bd-250.2","title":"kopitiam-runtime: Gemma 4 forward pass (hybrid local/global sliding-window attention + Per-Layer Embeddings)","description":"THE BIG ONE under bd-250. Without this, nothing else makes Gemma runnable.\n\nCURRENT STATE: crates/kopitiam-runtime is a single-architecture engine. config.rs's own doc says it 'encodes one specific architecture family: pre-norm residual', and FORMAT = \"qwen-config\". The model type is QwenModel: embedding, RoPE, grouped-query attention, SwiGLU MLP, KV cache, greedy sampling. That is Qwen2 (and dense-Llama by coincidence of shape).\n\nGEMMA 4 IS NOT THAT SHAPE. Per bd-250's own engineering note: hybrid local/global sliding-window attention plus Per-Layer Embeddings. Beyond that, the Gemma line historically differs from Qwen in ways that silently corrupt output if you skip them — GeGLU rather than SwiGLU, RMSNorm weights applied as (1 + w), input embeddings scaled by sqrt(d_model), and on Gemma 2/3 logit softcapping and alternating sliding-window layers. VERIFY EACH OF THESE AGAINST GEMMA 4 SPECIFICALLY before implementing; do not port assumptions from Gemma 2/3 blog posts, and do not trust this bead's list as authoritative — it is a starting checklist, not a spec.\n\nARCHITECTURAL QUESTION TO SETTLE FIRST (this is the real design work, not the kernels): does the runtime grow a second concrete model type, or does it gain a trait that both Qwen and Gemma implement? Today QwenConfig/QwenModel are named concretely throughout, and kopitiam-ai's LocalAdapter is written against them. A second copy-pasted transformer is the tempting-and-wrong answer. Per CLAUDE.md, propose the architecture and record an ADR/AID BEFORE writing the forward pass.\n\nPROVENANCE IS MANDATORY HERE. Per CLAUDE.md's Provenance Standards and the attribution rule: state in rustdoc AT THE POINT OF USE where each layout/algorithm came from (paper, model card, or a specific reference implementation and its license), and distinguish clean-room study from close adaptation. A transformer variant is exactly the kind of hard-won format knowledge that must live next to the code.\n\nSCOPE: text-only path first. Gemma 4 E2B is multimodal (text+image+audio) and that is well beyond the text-only runtime — explicitly out of scope here.\n\nVALIDATION: needs a real gemma-4-E2B GGUF and a known-good reference output to compare against. Greedy decoding with a fixed prompt against llama.cpp's output for the same file is the honest check; 'it emits plausible English' proves nothing, since a wrong forward pass does that too.\n\nDEPENDS ON: nothing in this tree, but pairs with the architecture-gate bead (which stops a Gemma GGUF from silently mis-running before this exists).","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-25T00:00:03.491000Z","updated_at":"2026-08-24T23:55:42.507000Z","closed_at":"2026-07-25T00:02:01.777000Z","close_reason":"done","external_ref":"gh-40","labels":["gemma","kopitiam-runtime","models"],"dependencies":[{"issue_id":"bd-250.2","depends_on_id":"bd-250","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-250.3","title":"kopitiam-ai: chat template is ChatML-only — dispatch per architecture, add Gemma's <start_of_turn>","description":"crates/kopitiam-ai/src/local/chat_template.rs renders ONE format and only one: ChatML. IM_START = \"<|im_start|>\", IM_END = \"<|im_end|>\", and render_chatml() emits a block per message plus an unconditional trailing `<|im_start|>assistant\\n` generation prompt. That is Qwen's format, hard-coded — there is no dispatch, no per-model branch, nothing reads the architecture.\n\nGemma wants a different wire format entirely: `<start_of_turn>user\\n{content}<end_of_turn>\\n<start_of_turn>model\\n`. Note it is 'model', NOT 'assistant', and Gemma has historically had no separate system role — the system prompt gets folded into the first user turn. CONFIRM BOTH AGAINST THE GEMMA 4 MODEL CARD before coding; do not carry these over from Gemma 2 by assumption.\n\nWHY THIS MATTERS EVEN WITH A CORRECT FORWARD PASS: a right model fed the wrong turn markers does not error, it just answers badly — the tokens are in-vocabulary, so nothing crashes. This is the same class of silent wrongness as the missing architecture gate, and it is invisible in a demo where the reply merely looks a bit off.\n\nWHAT TO BUILD: a template selector driven by the GGUF's architecture (and ideally by `tokenizer.chat_template` in the GGUF metadata when present, which is the model's OWN answer and beats any table we hand-maintain). Keep render_chatml() as one implementation behind it, unchanged and still exactly tested — its existing tests assert byte-exact output, which is the right standard for every template added.\n\nKEEP THE EXISTING RIGOUR: chat_template.rs's tests pin exact strings including edge cases (empty history, system-only, no escaping of user content that happens to contain `<|im_end|>`). Any Gemma template must be tested to the same byte-exact standard, including the 'user content containing a literal turn marker is never reinterpreted' case.\n\nBLOCKED-ISH BY: the Gemma forward-pass bead — a template with no engine to run it is untestable end-to-end. But the template itself, and the dispatch mechanism, can be built and unit-tested independently right now.","status":"open","priority":2,"issue_type":"feature","created_at":"2026-07-25T00:00:22.212000Z","updated_at":"2026-08-24T23:55:42.516000Z","external_ref":"gh-41","labels":["gemma","kopitiam-ai","prompting"],"dependencies":[{"issue_id":"bd-250.3","depends_on_id":"bd-250","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-250.4","title":"kopitiam-models: add gemma-4-E2B catalog spec + kill the placeholder sha256s that break every pull","description":"TWO PROBLEMS, ONE FILE (crates/kopitiam-models/src/catalog.rs).\n\n(1) NO GEMMA IN THE CATALOG. Catalog::builtin returns exactly two specs: qwen2.5-0.5b-instruct-q4_0 (Apache-2.0) and llama-3.2-1b-instruct-q4_0 (Llama-3.2-Community). `Architecture::Gemma` exists as a bare enum variant at catalog.rs:34-35 with NO spec behind it, so `KOPITIAM_MODEL=gemma-...` does not error — apps/cli/src/adapter.rs's Catalog::find falls back to the default spec, and the user silently gets Qwen while believing they asked for Gemma. Needs a real gemma-4-E2B ModelSpec: id, display_name, Architecture::Gemma, license, artifact url + REAL sha256 + size.\n\n(2) EVERY PULL IS BROKEN BY DESIGN. Both existing entries carry `sha256: PLACEHOLDER_SHA256` (64 zeros) with a `// TODO: record real sha256 after first successful pull`. `kopitiam models pull` therefore downloads hundreds of MB and then DELIBERATELY fails the checksum gate. apps/cli/src/adapter.rs's module docs already call this out as the reason it never calls ensure_available. Both URLs also carry TODO(verify-url) notes saying the exact quant filename is unconfirmed. Net effect: the autofetch path has never actually worked end-to-end, and bring-your-own is the only real route to weights today.\n\nThe two GGUFs sitting in ~/.cache/kopitiam/models (qwen2.5-0.5b 409M, llama-3.2-1b 738M) arrived some other way — sha256 them and record the real values, and the existing catalog becomes usable at the same time.\n\nLICENCE GATE — READ bd-250's THIRD COMMENT BEFORE ADDING ANY GEMMA ENTRY. The Gemma family does NOT share one licence. Gemma 4 is Apache-2.0 (OSI) and is therefore compatible with KOPITIAM's GPL-3.0-only, so it may be defaulted to and its URL shipped in the catalog. Gemma 1/2/3/3n are under the custom 'Gemma Terms of Use', whose §3.2 requires passing use-restrictions downstream as an enforceable term — that collides head-on with GPLv3 §7's no-further-restrictions rule. Those must NEVER be added to the built-in catalog; they are user-supplied-GGUF only, via KOPITIAM_MODEL_GGUF, where KOPITIAM never distributes the weights. The `license` field on the spec is not decoration — it is the record of that decision, so fill it exactly.\n\nALSO VERIFY (bd-250 comment 2 flagged it unresolved): Gemma 4's card links a separate Prohibited Use / Intended Use policy. Confirm that policy is ADVISORY and not incorporated into the Apache-2.0 grant before shipping a bundled default. If it turns out to be incorporated, Gemma 4 drops to user-supplied-only with the older Gemmas and this bead's premise collapses — say so in an AID rather than quietly shipping it.\n\nNOTE the crate already has a mechanism for this: catalog.rs's own comment points at kopitiam-56q for building specs from HF repo/filename/sha with a pinned commit revision, and src/hf.rs exists. Prefer that over hand-writing a third entry.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-25T00:00:45.768000Z","updated_at":"2026-08-24T23:55:42.525000Z","closed_at":"2026-07-25T00:02:01.877000Z","close_reason":"done","external_ref":"gh-42","labels":["catalog","gemma","kopitiam-models"],"dependencies":[{"issue_id":"bd-250.4","depends_on_id":"bd-250","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-2c2","title":"gpu: offload the matmuls to wgpu (blocked on bd-b9x), keep Termux builds clean","description":"Maintainer request (2026-07-28): chat is laggy on CPU; offload some tensor ops to a wgpu kernel when resources allow. Must stay compiling cleanly on Termux, and the hybrid CPU/wgpu path must be compared properly against llama.cpp.\n\nSTATE OF PLAY, measured not assumed:\n* crates/kopitiam-gpu already exists (AID-0037): wgpu + pollster + bytemuck, a GpuContext, an Executor with a GPU->CPU fallback cascade, and exactly ONE op - vector_add. It is NOT wired into kopitiam-tensor or kopitiam-runtime at all, so nothing in inference touches the GPU today.\n* TERMUX COMPILES CLEAN ALREADY. 'cargo check --target aarch64-linux-android -p kopitiam-gpu' succeeds in ~30s; wgpu pulls ndk-sys/jni-sys/wgpu-core-deps-windows-linux-android and checks green. So a build-time gate is NOT required for Termux to compile. A gate would buy smaller/faster Termux builds (skipping the whole wgpu tree) rather than fixing a breakage - worth doing for that reason, but the premise that it currently fails to compile does not hold.\n\nWHAT TO OFFLOAD, in cost order: the matmuls dominate. attn q/k/v/o projections, the MLP gate/up/down, and the output projection - that is where a 360M spends nearly all its time. probs@V and q@K^T are next. Elementwise ops (silu, rms_norm, add) are not worth a round trip on their own; they are only worth it if the tensors are already resident on the GPU, which means the real win needs a resident-tensor design rather than op-by-op offload with a copy each way.\n\nHARD SEQUENCING CONSTRAINT - read bd-b9x first. Our CPU forward pass currently DISAGREES with llama.cpp, and the gap grows with prompt length (bd-b9x, P0, confirmed against a locally built llama.cpp on the same .gguf). Offloading now would (a) make a wrong answer arrive faster, and (b) destroy the ability to validate: a GPU-vs-llama.cpp comparison would inherit the existing error, so a genuine GPU kernel bug could not be told apart from bd-b9x. Fix bd-b9x, THEN the three-way GPU/CPU/llama.cpp comparison the maintainer asked for is actually meaningful.\n\nHOW TO VALIDATE when the time comes: docs/REFERENCE-ORACLE.md documents building and driving llama-completion/llama-tokenize from the vendored clone, and the variables that must be controlled (greedy both sides, -ctk/-ctv f32, the -f vs -e -p trailing-newline trap, KOPITIAM_FORCE_F32_WEIGHTS). Every GPU kernel additionally gets the treatment the CPU ops already have: compared element-wise against a hand-computed reference at REAL attention shapes, not just 'it ran'.\n\nAlso worth knowing before designing: on Termux there is usually no usable Vulkan driver, so the cascade will fall back to CPU there anyway. The lag the maintainer feels on Windows is the case that benefits.","notes":"PROGRESS, commit ce74f2b1be: the wgpu matmul kernel exists, is correctness-checked against its CPU twin at real transformer shapes, and - more importantly - is MEASURED. The measurements change the plan.\n\nMEASURED on Intel integrated GPU vs 14 CPU cores, SmolLM2-360M real shapes, timings INCLUDE upload+readback:\n  decode  attn q/o     (1 tok)   cpu   443us   gpu 1.88ms   0.24x   LOSS\n  decode  mlp gate/up  (1 tok)   cpu  1.28ms   gpu 3.20ms   0.40x   LOSS\n  decode  mlp down     (1 tok)   cpu  1.29ms   gpu 2.86ms   0.45x   LOSS\n  decode  output head  (1 tok)   cpu 25.67ms   n/a - exceeds binding limit\n  prefill attn q/o    (33 tok)   cpu 12.89ms   gpu 3.72ms   3.46x   WIN\n  prefill mlp gate/up (33 tok)   cpu 40.83ms   gpu 7.77ms   5.26x   WIN\n\nCONCLUSION: do NOT offload uniformly. A decode step is one row of activations and cannot amortise moving a weight matrix; a 33-token prefill does 33x the arithmetic against the same upload and wins outright. Offloading everything - the obvious reading of the original request - would make chat 2-4x SLOWER.\n\nREMAINING WORK, in value order:\n1. OUTPUT HEAD, QUANTIZED + RESIDENT. It is 49152x960 and costs 25.67ms per token on CPU - more than every other decode matmul combined, so it IS the chat lag. It cannot go to the GPU as f32 at all: 188MB against this adapter's 128MB max_storage_buffer_binding_size. Keeping it Q8_0 on the device is ~47MB, which fits. This needs a q8 matmul kernel (dequantise in-shader) plus residency. Highest value by a wide margin.\n2. PREFILL OFFLOAD. Wire matmul_nt into kopitiam-runtime's linear() for the seq>1 case only, guarded on Executor::has_gpu(). Straightforward given the kernel exists; worth 3.5-5.3x on prompt ingestion.\n3. RESIDENT WEIGHTS for decode. Upload each weight once at model load. Ownership/lifetime design question - a GpuLinear owning its device buffer - deliberately not smuggled in with the first kernel.\n4. TILED KERNEL. The current shader is one invocation per output element with a plain dot product. Shared-memory tiling would raise the prefill win further, but it is a multiplier on (2), not a prerequisite.\n\nBUG FIXED EN ROUTE, found by benchmarking rather than review: wgpu treats an over-limit storage binding as a validation error and PANICS, aborting the process instead of returning Err for the cascade. The output head triggers it exactly. matmul_nt_gpu now checks device limits up front and refuses with InvalidInput so the Executor falls to CPU; regression test constructs a weight one row past whatever the local adapter's limit is, so it holds on any device.\n\nTERMUX: confirmed again - cargo check --target aarch64-linux-android passes. No gating was ever needed for compilation. Note Termux typically has no usable Vulkan driver, so the cascade lands on CPU there regardless; the lag this work addresses is the desktop case.\n\nCOMPARISON DISCIPLINE (maintainer's instruction): the hybrid path must be compared to llama.cpp, and that is now possible - bd-b9x is fixed, so our CPU forward pass agrees with llama.cpp. docs/REFERENCE-ORACLE.md documents the oracle. Any offload landing in the forward pass must be checked three ways: GPU vs our CPU twin element-wise, and end-to-end output vs llama.cpp.","status":"open","priority":2,"issue_type":"feature","created_at":"2026-07-27T23:51:59.195000Z","updated_at":"2026-08-24T23:55:42.650000Z","external_ref":"gh-56","labels":["kopitiam-gpu","performance"],"dependencies":[{"issue_id":"bd-2c2","depends_on_id":"bd-b9x","type":"blocks","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}],"comments":[{"id":1,"issue_id":"bd-2c2","author":"fifad@Teddy-Thinkpad","text":"PROGRESS, commit ce74f2b1be: the wgpu matmul kernel exists, is correctness-checked against its CPU twin at real transformer shapes, and - more importantly - is MEASURED. The measurements change the plan.\n\nMEASURED on Intel integrated GPU vs 14 CPU cores, SmolLM2-360M real shapes, timings INCLUDE upload+readback:\n  decode  attn q/o     (1 tok)   cpu   443us   gpu 1.88ms   0.24x   LOSS\n  decode  mlp gate/up  (1 tok)   cpu  1.28ms   gpu 3.20ms   0.40x   LOSS\n  decode  mlp down     (1 tok)   cpu  1.29ms   gpu 2.86ms   0.45x   LOSS\n  decode  output head  (1 tok)   cpu 25.67ms   n/a - exceeds binding limit\n  prefill attn q/o    (33 tok)   cpu 12.89ms   gpu 3.72ms   3.46x   WIN\n  prefill mlp gate/up (33 tok)   cpu 40.83ms   gpu 7.77ms   5.26x   WIN\n\nCONCLUSION: do NOT offload uniformly. A decode step is one row of activations and cannot amortise moving a weight matrix; a 33-token prefill does 33x the arithmetic against the same upload and wins outright. Offloading everything - the obvious reading of the original request - would make chat 2-4x SLOWER.\n\nREMAINING WORK, in value order:\n1. OUTPUT HEAD, QUANTIZED + RESIDENT. It is 49152x960 and costs 25.67ms per token on CPU - more than every other decode matmul combined, so it IS the chat lag. It cannot go to the GPU as f32 at all: 188MB against this adapter's 128MB max_storage_buffer_binding_size. Keeping it Q8_0 on the device is ~47MB, which fits. This needs a q8 matmul kernel (dequantise in-shader) plus residency. Highest value by a wide margin.\n2. PREFILL OFFLOAD. Wire matmul_nt into kopitiam-runtime's linear() for the seq>1 case only, guarded on Executor::has_gpu(). Straightforward given the kernel exists; worth 3.5-5.3x on prompt ingestion.\n3. RESIDENT WEIGHTS for decode. Upload each weight once at model load. Ownership/lifetime design question - a GpuLinear owning its device buffer - deliberately not smuggled in with the first kernel.\n4. TILED KERNEL. The current shader is one invocation per output element with a plain dot product. Shared-memory tiling would raise the prefill win further, but it is a multiplier on (2), not a prerequisite.\n\nBUG FIXED EN ROUTE, found by benchmarking rather than review: wgpu treats an over-limit storage binding as a validation error and PANICS, aborting the process instead of returning Err for the cascade. The output head triggers it exactly. matmul_nt_gpu now checks device limits up front and refuses with InvalidInput so the Executor falls to CPU; regression test constructs a weight one row past whatever the local adapter's limit is, so it holds on any device.\n\nTERMUX: confirmed again - cargo check --target aarch64-linux-android passes. No gating was ever needed for compilation. Note Termux typically has no usable Vulkan driver, so the cascade lands on CPU there regardless; the lag this work addresses is the desktop case.\n\nCOMPARISON DISCIPLINE (maintainer's instruction): the hybrid path must be compared to llama.cpp, and that is now possible - bd-b9x is fixed, so our CPU forward pass agrees with llama.cpp. docs/REFERENCE-ORACLE.md documents the oracle. Any offload landing in the forward pass must be checked three ways: GPU vs our CPU twin element-wise, and end-to-end output vs llama.cpp.","created_at":"2026-07-28T11:52:24.739000Z"}]}
{"id":"bd-2wo","title":"kmux test osc_title_rename_is_applied_when_allow_set_title_is_on fails on maintainer's box","description":"crates/kmux/tests/issue8_interactive_exit.rs:165 fails locally:\n\n  left:  \"teddy0@arch-aftershock-rapid-glass-S267454:/home/.../crates/kmux\"\n  right: \"CLAUDETITLE\"\n\nThe test spawns a real shell in a PTY and expects the OSC title it wrote (CLAUDETITLE) to stick. On the maintainer's box the interactive shell's own prompt writes OSC 0/2 on every prompt redraw, so the shell's title lands AFTER the test's and wins. Environment-dependent, not a kmux regression — the assertion races the user's shell rc.\n\nFix direction: spawn the child with a rc-less non-interactive shell (or set a stub PS1 with no OSC escape), so nothing but the test writes the title. Do NOT just relax the assertion — the OSC path is the thing under test.\n\nFound while running the full workspace gate for bd-8sh (nucleo panic fix). Pre-existing: kmux has no dependency on kopitiam-neovim, so that fix cannot have caused it.","status":"open","priority":3,"issue_type":"bug","created_at":"2026-07-25T03:01:43.351000Z","updated_at":"2026-08-24T23:55:42.552000Z","external_ref":"gh-45"}
{"id":"bd-3t0","title":"kopitiam-loader/tensor: GGUF K-quant dequant (Q4_K/Q5_K/Q6_K/…), bit-exact","description":"Add K-quant dequantization so modern GGUFs (Q4_K_M etc.) load. Bit-exact to ggml. Reference: vendored crates/kopitiam-ai/vendor/llama.cpp (ggml-quants.c, ggml-common.h), MIT, attributed at point of use. Wires new DType variants (kopitiam-core) + dtype_from_ggml_type (loader) + dequant (tensor) + bridge (runtime). In progress via agent. Unblocks loading modern quants; prerequisite context for the Gemma demo (bd for gemma arch/tokenizer). See bd-uij loader-reality notes.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-24T23:05:29.460000Z","updated_at":"2026-08-24T23:55:42.464000Z","closed_at":"2026-07-25T00:59:38.164000Z","close_reason":"done","external_ref":"gh-35","labels":["kopitiam-ai","loader","quantization"]}
{"id":"bd-3uh","title":"Review AID-0054: byte-level BPE now tolerates holes in the 256-byte alphabet","description":"AID-0054 (docs/ai-decisions/AID-0054-tolerate-incomplete-byte-level-vocab.md), status Pending review.\n\nSmolLM2-360M-Instruct is DEFAULT_MODEL_ID and could not be used at all: downloaded clean, sha256-verified clean, then died before one token came out with 'byte-level vocab has no single-byte token for byte 0x04'.\n\nTwo OPPOSITE causes were open (loader blind to llama.cpp's <0xNN> BYTE-type spelling, vs our all-256 check too strict). Settled by evidence, not guessed: 'kopitiam models inspect' on the real 386MB file reports 21 bytes absent and ZERO <0xNN> spellings. So the check was too strict.\n\nDECISION MADE WITHOUT YOU: BpeTokenizer::byte_ids is now [Option<u32>; 256]. A vocab with holes builds; an unmapped byte is DROPPED at encode (byte-level BPE has no <UNK> to substitute with, and inventing an id the model never trained on is worse). New BpeTokenizer::missing_byte_tokens() exposes which bytes are absent.\n\nWHAT IT COSTS, precisely: encode() takes &str so input is always valid UTF-8, and 13 of the 21 missing bytes CANNOT occur in valid UTF-8 at all (0xc0/0xc1 overlong lead bytes, 0xf5..=0xff above the max legal lead byte 0xf4). The 8 reachable ones are six C0 controls (EOT ACK DC3 DC4 SYN GS) plus 0xf1/0xf2, lead bytes for Unicode planes 4-11 which are entirely unassigned. So decode(encode(s))==s still holds for everything except six rare control characters.\n\nREVERSAL: one field type plus the from_vocab error, and the 4 new tests in bpe.rs.\n\nWHAT WOULD MAKE THIS WRONG: a GGUF that IS the loader-gap case now gets quietly tolerated instead of erroring — silent quality loss instead of loud failure. 'models inspect' still tells the two apart. Stronger fix if it bites: teach the loader to decode BYTE-type tokens, then holes are only ever genuine.\n\nPROVEN END TO END on real weights: 'The capital of France is' -> ' Paris.' (360M) and ' Paris, the city of light, the' (1.7B Q4_K_M).","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-27T13:17:13.816000Z","updated_at":"2026-08-24T23:55:42.570000Z","external_ref":"gh-47","labels":["ai-decision"]}
{"id":"bd-4et","title":"kopi-beans: add 'bn daemon stop' and 'bn daemon restart' (machinery exists, just not reachable)","description":"Maintainer's ask, 2026-08-13, straight out of dogfooding: `bn sync` hung (bd-95p)\nand there was NO supported way to bounce the daemon. Had to `pkill -f \"bn daemon\nrun\"` and let the next command autostart one. Cannot expect a user to reach for\npkill to recover their own tracker lah.\n\nToday `bn daemon --help` offers exactly one thing:\n\n    Commands:\n      run   Run the daemon in the foreground (internal)\n\nThe good news: NOTHING new needs inventing. The whole mechanism is already\nwritten and already cross-platform in\ncrates/kopi-beans/src/surface/ipc/client.rs — it is just PRIVATE, so only the\nIPC retry path can reach it, never a user:\n\n  * `kill_daemon_forcefully(pid, socket)` (:1162 unix, :1221 windows) —\n    SIGTERM, wait 3s for the socket to go away, escalate to SIGKILL, wait 2s\n    more, then remove `daemon.sock` + `daemon.meta.json` as last resort.\n    Windows arm has no SIGTERM so it terminates directly, same cleanup.\n    Missing process (ESRCH) is treated as success, which is what we want.\n  * `try_restart_daemon_by_socket(socket)` (:1245) — reads the pid out of\n    `daemon.meta.json`, else removes the stale socket.\n  * `read_daemon_meta_at(socket)` (:132) -> `Option<DaemonInfo>` (version,\n    protocol_version, pid, started_at_ms).\n  * `socket_path()` (:119), and autostart already spawns `bn daemon run`\n    (see `with_autostart_program`, :253).\n\nSo the job is plumbing, not design: expose a public stop/start, add the two\nsubcommands, wire the dispatch (cli.rs:363 `Command::Daemon` and the\n`daemon_cmd_name` mapper at :621), and render like the existing\n`render_daemon_info` (\"daemon 0.1.4 (protocol 3, pid 4242)\").\n\nBehaviours that must be right:\n  * `stop` when nothing is running = SUCCESS, exit 0, say so plainly. It must be\n    idempotent — scripts and session-close hooks will call it blind.\n  * `restart` must WAIT for the new daemon to be serving before returning, not\n    just fire and hope. `wait_for_daemon_ready(expected_version)` (:329) already\n    exists for exactly this.\n  * `restart` with nothing running = just start one, not an error.\n  * `--json` on both, same as every other command.\n  * Report the OLD pid and the NEW pid on restart, so the user can see it really\n    bounced.\n  * Keep `bn daemon` hidden as it is now, but stop/restart are user-facing, so\n    their help text must be written for a human.\n\nRelated: bd-95p (the `bn sync` hang that prompted this) and bd-cni (Windows: a\nread autostarts a daemon that then blocks every write) — a working `stop` is the\nrecovery path for bd-cni too.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-08-13T00:28:35.394000Z","updated_at":"2026-08-13T00:57:09.853000Z","closed_at":"2026-08-13T00:57:09.853000Z","close_reason":"done","external_ref":"gh-24","labels":["cli","dogfooding","kopi-beans"]}
{"id":"bd-515","title":"kopitiam-pdf 0.2.6: blue/black glyph-fallback boxes still appear on a LaTeX dissertation PDF (follow-up to #31)","description":"Mirrors GitHub issue theodoreOnzGit/kopitiam#67. Unverified report (no repro file attached yet): a LaTeX-produced dissertation PDF still shows solid blue/black advance-box glyph fallback on kopitiam-pdf 0.2.6 (the release that closed #31).\n\nHypotheses from the issue, roughly by likelihood for a LaTeX dissertation:\n1. Composite/CID-keyed OpenType-CFF fonts (CIDFontType0C) -- common with XeLaTeX/LuaLaTeX + OpenType text/math fonts. This routing predates #31 and is untouched by it, so if broken, it's a separate pre-existing gap, not a regression.\n2. seac-composed accented glyphs in glyph_type1.rs's Type1 interpreter -- my new code from #31, plausible edge case.\n3. Predefined Expert/ExpertSubset CFF encoding -- documented, accepted ceiling from #31 (glyph_cff.rs:39-40). Unlikely for LaTeX but not ruled out.\n\nNeeds a reproduction (the dissertation PDF or a trimmed excerpt) plus which LaTeX engine/font setup produced it before a real diagnosis is possible.","notes":"Cross-engine graceful fallback landed on main (176c52b, mandatory, no feature gate): rasterize_page now re-renders with hayro whenever kopitiam's own DrawDevice hit the advance-box fallback for at least one glyph on a page, covering all three hypotheses above (CID-keyed CFF, seac, predefined-Expert encoding) without needing to diagnose which one applies -- hayro is a separate, independently-maintained PDF interpreter so it isn't subject to the same ceilings. Still needs verification against the actual repro (no dissertation PDF/excerpt attached yet) before this can be closed as fixed; if hayro also can't parse the page, kopitiam's box-fallback output is kept as-is (never worse than 0.2.6).","status":"open","priority":1,"issue_type":"bug","created_at":"2026-08-25T01:12:46.392000Z","updated_at":"2026-08-25T11:00:32.701000Z","external_ref":"gh-67","comments":[{"id":1,"issue_id":"bd-515","author":"root@vm","text":"Cross-engine graceful fallback landed on main (176c52b, mandatory, no feature gate): rasterize_page now re-renders with hayro whenever kopitiam's own DrawDevice hit the advance-box fallback for at least one glyph on a page, covering all three hypotheses above (CID-keyed CFF, seac, predefined-Expert encoding) without needing to diagnose which one applies -- hayro is a separate, independently-maintained PDF interpreter so it isn't subject to the same ceilings. Still needs verification against the actual repro (no dissertation PDF/excerpt attached yet) before this can be closed as fixed; if hayro also can't parse the page, kopitiam's box-fallback output is kept as-is (never worse than 0.2.6).","created_at":"2026-08-25T11:00:09.717000Z"}]}
{"id":"bd-5f4","title":"netfetch end-to-end harness: still unverified on Linux/Termux","description":"Everything in the 2026-07-27 netfetch session was verified on Windows only. The harness and the tokenizer fix are platform-neutral BY INSPECTION (std::env::split_paths is platform-defined; ModelStore already resolves $HOME/.cache on unix), but that is an argument, not a test run — and that session is itself a lesson in the difference, since every synthetic fixture passed while the real model was broken.\n\nRUN ON TERMUX after pulling develop:\n\n  cargo test --release -p kopitiam-tokenizer\n  KOPITIAM_NETFETCH=1 cargo test --release -p kopitiam-runtime --test netfetch_end_to_end -- --nocapture\n\nThe default picks the small model (SmolLM2-360M) by itself, so no flags are needed. Use the 360M, NOT the 1.7B: the 1.7B forward pass took ~453s on a 14-core ThinkPad and wants ~1GB resident.\n\nIf Termux egress blocks huggingface.co the run SKIPs green and says so — that is the designed behaviour, not a pass. To prove the chain with no network at all, drop a .gguf somewhere and point KOPITIAM_NETFETCH_PATHS at it (separator ':' on unix, ';' on Windows).\n\nExpected on success:\n  PASS  smollm2-360m-instruct-q8_0   reached generate","status":"open","priority":2,"issue_type":"task","created_at":"2026-07-27T13:17:39.237000Z","updated_at":"2026-08-24T23:55:42.587000Z","external_ref":"gh-49","labels":["testing"]}
{"id":"bd-5js","title":"kvim publish tree still AGPL-3.0-only on crates.io — needs 0.1.8 relicense bump","description":"Repo relicensed AGPL-3.0-only -> GPL-3.0-only in 213e619 (2026-07-25 07:24 SGT), but all 7 crates in the kvim publish tree are live on crates.io at 0.1.7 with `license = \"AGPL-3.0-only\"`: kopitiam-ontology, kopitiam-config, kopitiam-syntax, kopitiam-snippet, kopitiam-lua, kopitiam-semantic, kopitiam-neovim (all published 2026-07-18).\n\ncrates.io versions are immutable — cannot patch metadata in place. Only fix is bump [workspace.package].version 0.1.7 -> 0.1.8 and re-run scripts/publish-kvim.sh.\n\nVerified by diffing the published .crate against the working tree: kopitiam-neovim 0.1.7 source is otherwise IDENTICAL to local — only src/icons.rs (one rustdoc line, AGPLv3->GPLv3) and README.md (two lines) differ. So this is a licence-metadata-only republish, no feature/bugfix content.\n\nBlast radius: workspace version is shared, so the bump also moves the CLI publish tree (scripts/publish.sh) — those crates are equally AGPL-stale on crates.io. kopitiam-core/kopitiam-tensor are on their own 0.0.x line and unaffected by the kvim tree.\n\nPUBLISH IS MAINTAINER-GATED — agent must not run publish-kvim.sh.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-24T23:40:18.031000Z","updated_at":"2026-08-24T23:55:42.489000Z","closed_at":"2026-07-25T03:04:19.768000Z","close_reason":"done","external_ref":"gh-38"}
{"id":"bd-6j8","title":"kopi-beans: --json mode emits no structured diagnostic for a failed/timed-out sync","description":"Noticed while fixing bd-95p. PRE-EXISTING CLI-wide shape, not made worse there.\n\n`bn sync` now prints a proper diagnostic on failure/timeout — remote, dirty,\nsync_in_progress, consecutive_failures, last error. Human-readable, exit 1.\n\nBut under `--json`, stdout stays clean (correct — verified) and only a one-line\nerror goes to stderr. So a scripting consumer gets the exit code and nothing\nmachine-readable about WHY. They would have to scrape the human text, which is\nexactly what --json exists to avoid.\n\nThe lane state is already assembled for the human renderer, so this is\nplumbing: emit the same fields as a JSON object on the error path.\n\nApplies beyond sync — worth checking whether other commands' error paths have\nthe same gap before picking a shape, so we do it once, consistently. Wider than\none command, hence P3 rather than folding it into bd-95p.\n","status":"open","priority":3,"issue_type":"task","created_at":"2026-08-13T09:25:43.363000Z","updated_at":"2026-08-24T23:55:42.729000Z","external_ref":"gh-65","labels":["cli","kopi-beans"]}
{"id":"bd-7ab","title":"kopitiam check/test: no --release / --profile flag (blocks release-only workspaces)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/1\n\n`kopitiam check` and `kopitiam test` always run the `dev` profile — got no way\nto ask for `--release` at all. For this workspace that's damn jialat lah: our own\nCLAUDE.md Build Rules say release-mode only, so kopitiam's own diagnostics cannot\nbe the authoritative check. You ask a release-profile question, you get a\ndev-profile answer.\n\nNot cosmetic hor — release changes real behaviour: overflow checks off,\ndifferent optimisation-dependent timing/precision paths. A suite green in dev can\nstill die in release, so a dev-only `kopitiam test` can report a false all-clear.\n\nWhat to add (apps/cli/src/diagnostics.rs, CheckArgs + TestArgs):\n  * `--release`  -> `cargo check --release` / `cargo test --release`\n  * `--profile <NAME>` -> `--profile <NAME>` for custom profiles\n  * both must flow through `base_command()` so passthrough AND\n    --compact/--json paths get the same profile.\n\nLower priority extras from the reporter:\n  * trailing cargo passthrough (`kopitiam check -- --all-targets`) — matters\n    because examples/tests must compile for an Android target and a --lib-only\n    check silently misses them.\n  * doc nit: `check`'s one-line summary says \"report one deduplicated line per\n    distinct diagnostic\", but the dedup is opt-in via `--compact`. Summary line\n    running ahead of itself.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:46:33.809000Z","updated_at":"2026-08-13T00:20:32.109000Z","closed_at":"2026-08-13T00:20:32.109000Z","close_reason":"done","external_ref":"gh-1","labels":["cli","dogfooding"]}
{"id":"bd-7mp","title":"kopi-beans: SyncWarning::ClockSkew has no source field — cannot say WHICH reference stamp it compared","description":"Follow-up from bd-pl2 / GH#21, point 1 of the reporter's ask, only PARTLY met.\n\nThe new warning names the comparands as \"the newest write stamp bn has seen for\nthis store\" — a label chosen because it is true of ALL FOUR call sites. But the\nfour are genuinely different references:\n\n  daemon/runtime/core/repo_load.rs:264  -> max stamp across every bead loaded\n                                           from the git repo\n  daemon/runtime/coord/sync.rs:165,:323 -> RUNNING max for the store\n                                           (max_write_stamp; never decreases)\n  daemon/runtime/core/repl_ingest.rs:261 -> newest stamp in the batch that just\n                                           arrived (plain assign — CAN decrease)\n  daemon/runtime/executor.rs:461        -> the stamp of the write this daemon\n                                           just made (local HLC post-tick)\n\nTelling the user which one produced the figure would let them distinguish \"my\nclock vs my own repo\" from \"my clock vs whatever a peer just sent me\" — quite\ndifferent diagnoses.\n\nBlocked on a change OUTSIDE the fixing agent's allowed paths, which is why it\nwas not done: add a `source: ClockSkewSource` field to `SyncWarning::ClockSkew`\nin crates/kopi-beans/src/api/issues.rs:32-35. Once that exists the daemon side is\na one-line wire-through each at query_executor.rs:803 and git_lane.rs:45, and the\nrenderer in cli_surface/commands/status.rs just prints it.\n\nNOTE while you are in there: `WriteStamp` carries NO actor attribution — only\n`Stamp` has `by` (core/time.rs:130-134), and `max_write_stamp()` throws it away\n(core/state/canonical.rs:621-630). So the reporter's suggested \"actor <id>'s last\nwrite\" phrasing CANNOT be honoured without either plumbing `by` through or\ninventing a fact. Don't invent lah.\n","status":"open","priority":3,"issue_type":"task","created_at":"2026-08-13T00:21:01.273000Z","updated_at":"2026-08-13T00:21:01.273000Z","external_ref":"gh-21","labels":["kopi-beans"]}
{"id":"bd-8f8","title":"runtime: tied output projection runs as dequantized f32 - 188MB and the fused kernel skipped","description":"Found while wiring the GPU output head (bd-jrq). This is a CPU-path win, and it matters most exactly where there is NO GPU - Termux, which typically has no usable Vulkan driver.\n\nTHE PROBLEM. Neither SmolLM2 release ships a separate output.weight; both tie the output projection to token_embd.weight. crates/kopitiam-runtime/src/weights.rs loads token_embd through load_tensor_f32 - correctly, because the embedding lookup (gather_rows) needs real floats - and then the tied fallback does:\n\n    let output_weight = match load_matmul_weight_opt(model, 'output.weight')? {\n        Some(w) => w,\n        None => token_embd.clone(),      // <- f32 clone\n    };\n\nSo for every tied model - which is both of ours, and most small models - the SINGLE BIGGEST MATMUL IN THE NETWORK runs against a fully dequantized f32 weight. It skips the fused Tensor::quantized_matmul path that every other projection gets, purely as a side effect of sharing a tensor with the embedding table.\n\nCOST, SmolLM2-360M (49152 x 960):\n  memory: 188MB of f32 held resident, where the Q8_0 source is 47MB. 4x.\n  speed : the output projection measures 25-26ms per token on CPU, more than every other decode matmul combined.\n\nWHAT TO DO. Load the output projection separately from the embedding table when they are tied: keep token_embd as f32 for gather_rows, and load a SECOND handle to the same tensor via load_matmul_weight (which preserves Q8_0 and takes the fused kernel). They are the same bytes on disk, so this costs one extra tensor handle, not a second copy of the data - and it should REDUCE peak memory, since the f32 expansion of a 49152-row table is the largest single allocation in the model.\n\nWorth measuring both effects: tokens/sec on the CPU-only path, and RSS at model load. Expect the memory win to be the more dramatic one on a phone.\n\nCAVEAT to check first: confirm gather_rows cannot read directly from a quantized tensor. If it can, the f32 expansion may be avoidable entirely rather than merely duplicated - which would be a bigger win again.","notes":"FIXED. Bigger than expected, and it partly invalidates the GPU headline from bd-jrq - saying so here rather than letting the nicer number stand.\n\nMEASURED, CPU-only path (--no-default-features, the Termux case):\n                                  before     after\n  'The capital of France is'       6.77s     1.39s    4.9x\n  '...over the lazy'               7.03s     1.84s    3.8x\n  token_embd resident            188.7MB    50.1MB    3.8x smaller\n  weights load (gpu path)          570ms     418ms\nOutput byte-identical across cpu-only, gpu, and llama.cpp.\n\nCORRECTION TO THIS BEAD'S OWN PLAN. It said to load a SECOND handle via load_matmul_weight while keeping the f32 one for gather_rows, and claimed that would reduce memory. That was WRONG - it would have held both the 188MB f32 AND the 50MB quantized, i.e. worse. The caveat at the bottom of the bead turned out to be the actual fix: make gather_rows read quantized tables directly.\n\nWHAT LANDED:\n1. Tensor::gather_rows now decodes ONLY the requested rows from a block-quantized table. A row is a whole number of blocks, so row r is exactly blocks_per_row consecutive blocks at r*blocks_per_row and decodes on its own. A forward pass reads one row per token - often one total during decode - so dequantizing all 49152 rows to get them was the whole waste. Refuses a straddling row or a strided/offset view rather than returning wrong rows.\n2. ModelWeights::load takes token_embd through load_matmul_weight, so it stays Q8_0 where a fused kernel exists and still falls back to f32 where none does (the 1.7B's Q6_K). Win or no-op, never a regression. The tied output_weight clone now shares quantized storage instead of duplicating an f32 expansion.\n\nHONEST CONSEQUENCE FOR bd-jrq: the GPU output head measured 4.9x against the OLD cpu path. Against the fixed one it is 1.39s vs 1.31s - about 6%. The GPU kernel is still correct, still tested, and still the right foundation for larger models and for prefill offload (3.9-5.9x, bd-2c2), but on THIS model fixing the CPU path mattered far more than offloading it. Anyone reading bd-jrq's numbers should read this too.\n\nGates: 156 tensor + 146 runtime + 70 ai green, clippy -D warnings clean in both feature states, aarch64-linux-android clean in both, netfetch canonical gate green on real weights.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-28T12:45:15.198000Z","updated_at":"2026-08-24T23:55:42.669000Z","closed_at":"2026-07-28T12:56:51.350000Z","close_reason":"done","external_ref":"gh-58","labels":["kopitiam-runtime","performance"],"comments":[{"id":1,"issue_id":"bd-8f8","author":"fifad@Teddy-Thinkpad","text":"FIXED. Bigger than expected, and it partly invalidates the GPU headline from bd-jrq - saying so here rather than letting the nicer number stand.\n\nMEASURED, CPU-only path (--no-default-features, the Termux case):\n                                  before     after\n  'The capital of France is'       6.77s     1.39s    4.9x\n  '...over the lazy'               7.03s     1.84s    3.8x\n  token_embd resident            188.7MB    50.1MB    3.8x smaller\n  weights load (gpu path)          570ms     418ms\nOutput byte-identical across cpu-only, gpu, and llama.cpp.\n\nCORRECTION TO THIS BEAD'S OWN PLAN. It said to load a SECOND handle via load_matmul_weight while keeping the f32 one for gather_rows, and claimed that would reduce memory. That was WRONG - it would have held both the 188MB f32 AND the 50MB quantized, i.e. worse. The caveat at the bottom of the bead turned out to be the actual fix: make gather_rows read quantized tables directly.\n\nWHAT LANDED:\n1. Tensor::gather_rows now decodes ONLY the requested rows from a block-quantized table. A row is a whole number of blocks, so row r is exactly blocks_per_row consecutive blocks at r*blocks_per_row and decodes on its own. A forward pass reads one row per token - often one total during decode - so dequantizing all 49152 rows to get them was the whole waste. Refuses a straddling row or a strided/offset view rather than returning wrong rows.\n2. ModelWeights::load takes token_embd through load_matmul_weight, so it stays Q8_0 where a fused kernel exists and still falls back to f32 where none does (the 1.7B's Q6_K). Win or no-op, never a regression. The tied output_weight clone now shares quantized storage instead of duplicating an f32 expansion.\n\nHONEST CONSEQUENCE FOR bd-jrq: the GPU output head measured 4.9x against the OLD cpu path. Against the fixed one it is 1.39s vs 1.31s - about 6%. The GPU kernel is still correct, still tested, and still the right foundation for larger models and for prefill offload (3.9-5.9x, bd-2c2), but on THIS model fixing the CPU path mattered far more than offloading it. Anyone reading bd-jrq's numbers should read this too.\n\nGates: 156 tensor + 146 runtime + 70 ai green, clippy -D warnings clean in both feature states, aarch64-linux-android clean in both, netfetch canonical gate green on real weights.","created_at":"2026-07-28T12:56:51.271000Z"}]}
{"id":"bd-8sh","title":"kvim: nucleo panic 'should have been caught by prefilter' when typing capitalised words (Markdown)","description":"Typing an uppercase-starting prefix in insert-mode completion crashed kvim:\n\n  nucleo-matcher-0.3.1/src/fuzzy_optimal.rs:37: should have been caught by prefilter\n\nRoot cause, ours not nucleo's: lsp/completion.rs merge_and_rank() passed the RAW typed prefix as the needle to Matcher::fuzzy_match while Config::DEFAULT has ignore_case = true. nucleo documents that the caller must hand over a needle already case-folded + unicode-normalised. Break that and prefilter_ascii (which folds one-way only: needle 't' matches 't'/'T', but needle 'T' matches 'T' alone) disagrees with the matrix setup (which lowercases the haystack and compares the needle verbatim) — nucleo then asserts its own broken invariant.\n\nNeeds a gap to fire, which is why it looked random: if the prefilter window comes back exactly needle.len() wide, fuzzy_match short-circuits to calculate_score and never touches the matrix. So 'Th' vs 'Theorem' is fine; 'Th' vs 'Through' (second 'h' in -gh) widens the window, matrix runs, panic. Markdown is where it bites because prose is full of Capitalised words and there is no LSP, so buffer-word (<C-n>) completion sees uppercase prefixes constantly.\n\nFix: score through nucleo::pattern::Atom (CaseMatching::Smart, Normalization::Smart, AtomKind::Fuzzy) instead of calling Matcher::fuzzy_match raw. Atom normalises the needle and Atom::score sets ignore_case/normalize on the matcher to match the needle it holds, so prefilter and matrix can never disagree. Leaves prefer_prefix alone. Completion is now smart-case, same as the picker.\n\nThe picker (plugins/picker.rs) was never affected — it already goes through Pattern.\n\nVerified: reproduced the exact upstream panic with a scratch test on the old call shape, then confirmed green. 3 regression tests added (uppercase prefix, smart-case both directions, non-ASCII prefix). cargo test --release -p kopitiam-neovim: 926 pass. clippy clean.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-25T03:01:30.994000Z","updated_at":"2026-08-24T23:55:42.542000Z","closed_at":"2026-07-25T03:03:09.964000Z","close_reason":"done","external_ref":"gh-44"}
{"id":"bd-95p","title":"kopi-beans: 'bn sync' hangs past 90s on a store with no pending work (Linux, 0.1.4)","description":"Hit while closing out the 2026-08-13 GH-issue session. Own dogfooding, on the\nfreshly-installed 0.1.4.\n\n    $ timeout 90 bn sync\n    Terminated\n\nNinety seconds, no output, killed by timeout. Every OTHER command on the same\nstore in the same minute was instant: `bn list` (35 rows), `bn count` (49),\n`bn close` x4, `bn create` x6 — all returned immediately. So the store is\nhealthy and the daemon is up; it is `sync` specifically that does not come back.\n\n`bn sync` is documented as \"Wait for debounced sync flush to complete\". Nothing\nshould have been pending — the writes had already landed and\n`.beads/issues.jsonl` showed no diff afterwards.\n\nHost: Arch, Linux 7.1.7, kopi-beans 0.1.4 from crates.io, remote is\nhttps://github.com/theodoreOnzGit/kopitiam (a real network remote).\n\nSuspects, in order:\n  1. Now that push actually goes over the network via the system git binary\n     (0.1.3, GH#19), a flush may be waiting on a real remote round-trip with no\n     timeout and no progress output. Would make this a REGRESSION introduced by\n     the push fix — before it, non-local push failed fast with an error.\n  2. Waiting on a debounce/flush condition that is never signalled when there is\n     nothing to flush, i.e. it hangs precisely in the no-op case.\n  3. Daemon lock contention, the bd-cni family.\n\nWhat to establish first: run it with `-v`/`-vv`, and check whether the hang\nsurvives `--no-push`-equivalent conditions or a `file://` remote. If it IS the\nnetwork round-trip, `sync` needs a timeout and a progress line — a command whose\nwhole job is \"wait\" must say what it is waiting for, same lesson as bd-pl2.\n\nWhatever the cause, `sync` must not hang unbounded and silent. An agent running\nit in a session-close script blocks forever.\n","notes":"ROOT CAUSE FOUND 2026-08-13. Two independent hang paths, both real, both need fixing.\n\nPATH 1 — the deadlock (primary suspect for what I hit):\n  daemon/git_lane.rs fail_sync():\n      sync_in_progress = false;\n      consecutive_failures += 1;\n      dirty = true;              // keep dirty so we retry after backoff\n  daemon/runtime/server/waiters.rs flush_sync_waiters() releases a waiter ONLY when\n      !dirty && !sync_in_progress\n  So a sync that FAILS sets dirty=true, the waiter's release condition can never be\n  true, and the retry fails again -> dirty=true again. Backoff caps at ~32s and\n  retries forever. The SyncWait waiter is parked FOREVER. bn sync blocks until you\n  kill it. There is no timeout anywhere: client send() has none, the daemon parks\n  the responder in a HashMap with no deadline, and nothing ever tells the waiter\n  'the sync keeps failing'.\n\nPATH 2 — no timeout on the push subprocess:\n  git/gix_compat.rs subprocess_push() does cmd.output() with NO deadline. If git\n  stalls (TCP connect hangs, TLS handshake stalls, ssh waits), sync_in_progress\n  stays TRUE forever and the same waiter never releases. GIT_TERMINAL_PROMPT=0\n  stops credential PROMPTS but does nothing about a stalled transfer.\n\nBoth end in the same place: bn sync never returns and never says why.\n\nWHAT THE FIX MUST DO (not just add a timeout — the deadlock is the real bug):\n  1. Wake waiters on FAILURE, not only on success, and tell them it failed:\n     consecutive_failures + the last error. A command that waits must be able to\n     report 'still failing' instead of hanging.\n  2. Deadline on the SyncWait waiter, with --timeout on bn sync.\n  3. Deadline on the git push subprocess. Consider git-native\n     http.lowSpeedLimit/lowSpeedTime for stalled transfers PLUS a hard process\n     watchdog (covers ssh and pre-transfer hangs).\n  4. On timeout, print the actual state: dirty, sync_in_progress,\n     consecutive_failures, last error, remote URL. Exit non-zero.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-08-13T00:25:39.398000Z","updated_at":"2026-08-13T09:31:36.665000Z","closed_at":"2026-08-13T09:31:36.665000Z","close_reason":"done","external_ref":"gh-25","labels":["dogfooding","kopi-beans"],"comments":[{"id":1,"issue_id":"bd-95p","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"ROOT CAUSE FOUND 2026-08-13. Two independent hang paths, both real, both need fixing.\n\nPATH 1 — the deadlock (primary suspect for what I hit):\n  daemon/git_lane.rs fail_sync():\n      sync_in_progress = false;\n      consecutive_failures += 1;\n      dirty = true;              // keep dirty so we retry after backoff\n  daemon/runtime/server/waiters.rs flush_sync_waiters() releases a waiter ONLY when\n      !dirty && !sync_in_progress\n  So a sync that FAILS sets dirty=true, the waiter's release condition can never be\n  true, and the retry fails again -> dirty=true again. Backoff caps at ~32s and\n  retries forever. The SyncWait waiter is parked FOREVER. bn sync blocks until you\n  kill it. There is no timeout anywhere: client send() has none, the daemon parks\n  the responder in a HashMap with no deadline, and nothing ever tells the waiter\n  'the sync keeps failing'.\n\nPATH 2 — no timeout on the push subprocess:\n  git/gix_compat.rs subprocess_push() does cmd.output() with NO deadline. If git\n  stalls (TCP connect hangs, TLS handshake stalls, ssh waits), sync_in_progress\n  stays TRUE forever and the same waiter never releases. GIT_TERMINAL_PROMPT=0\n  stops credential PROMPTS but does nothing about a stalled transfer.\n\nBoth end in the same place: bn sync never returns and never says why.\n\nWHAT THE FIX MUST DO (not just add a timeout — the deadlock is the real bug):\n  1. Wake waiters on FAILURE, not only on success, and tell them it failed:\n     consecutive_failures + the last error. A command that waits must be able to\n     report 'still failing' instead of hanging.\n  2. Deadline on the SyncWait waiter, with --timeout on bn sync.\n  3. Deadline on the git push subprocess. Consider git-native\n     http.lowSpeedLimit/lowSpeedTime for stalled transfers PLUS a hard process\n     watchdog (covers ssh and pre-transfer hangs).\n  4. On timeout, print the actual state: dirty, sync_in_progress,\n     consecutive_failures, last error, remote URL. Exit non-zero.","created_at":"2026-08-13T08:42:33.699000Z"}]}
{"id":"bd-9tu","title":"bn export: any bd/bn write command re-symlinks .beads/issues.jsonl outside the repo","description":"Second occurrence (first fixed at f7e89cc, this one at ac32a5d). Running any bn write command (e.g. `bn update <id> --notes ...`) that touches export state replaces the tracked .beads/issues.jsonl with a symlink pointing at /root/.local/share/beads-rs/exports/<store-id>/issues.jsonl, and drops a .beads/issues.jsonl.bak of the prior real-file content alongside it. Since CLAUDE.md's hard rule is that issues.jsonl is a passive export and must be a real committed file (not a symlink -- the target path is per-machine/per-daemon and would not resolve for another clone), this silently corrupts the repo state on every session that runs a bn write and then commits without checking `file .beads/issues.jsonl` first. Workaround used both times: cp the symlink target's content back over a fresh regular file, rm the .bak, then commit. Real fix belongs in kopi-beans's export path -- it should write the export in place (or write-then-atomic-rename within the same directory), never point .beads/issues.jsonl at a symlink.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-25T11:01:50.523000Z","updated_at":"2026-08-25T11:02:04.700000Z","external_ref":"gh-68"}
{"id":"bd-aif","title":"kopi-beans: verify GH#19 leftovers — migrate/push transactionality and stale-remote-ref version aggregation","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/19\n\nPoint 1 of that issue — \"bn cannot push to a non-local remote\" — IS fixed, in\n0.1.3 (commits f06e642, 653c22f: network push now delegates to the system `git`\nbinary; see crates/kopi-beans/src/git/gix_compat.rs:875-924). gix 0.86 still has\nno send-pack (upstream #306), we just stopped pretending it does.\n\nBut the issue raised FOUR points and only the first is confirmed done. This bead\nis to verify (or fix) the other three before closing GH#19:\n\n2. TRANSACTIONALITY of `bn store migrate` w.r.t. the push. Original failure left\n   the local ref migrated to v2 while `refs/remotes/origin/beads/store` stayed\n   v1 — a half-state `bn` itself could not make progress from. `--no-push` now\n   exists on the migrate args (cli_surface/commands/store.rs handle_migrate).\n   QUESTION: with push now working, is the half-state still reachable when the\n   push fails for some OTHER reason (no network, auth reject, non-fast-forward)?\n   If yes, either don't advance the local ref, or complete locally and exit 0\n   with the exact `git push origin refs/heads/beads/store` instruction.\n\n3. VERSION AGGREGATION. Reporter: \"bn aggregates the format version across\n   several ref probes and reports the oldest\", so a fully-migrated LOCAL ref got\n   reported as v1 because the stale remote-tracking ref was older — and `bn\n   status` then told the user to run the very migrate that had just half-\n   succeeded. Loop with no exit except plain `git`. There IS test-shaped code\n   around this at crates/kopi-beans/src/git/sync.rs:3282-3286, so it may already\n   be handled. CONFIRM with an actual repro; don't assume.\n\n4. DOCUMENT the gix push limitation prominently. It shaped the whole daemon\n   auto-sync story, not just migration. Belongs in the crate rustdoc and\n   ACKNOWLEDGEMENTS-adjacent notes, not buried at gix_compat.rs:875.\n\nDo NOT close GH#19 until 2, 3 and 4 each have an answer backed by a real run.\n","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-12T23:52:08.766000Z","updated_at":"2026-08-12T23:52:08.766000Z","external_ref":"gh-19","labels":["kopi-beans"]}
{"id":"bd-aru","title":"kopi-beans: the FETCH side has no deadline either (~130s TCP stall keeps sync_in_progress true)","description":"Third hang path, found by dogfooding while fixing bd-95p. NOT fixed there —\nbd-95p stopped `bn sync` from hanging, but the underlying stall is still real.\n\nMeasured against a black-hole IP (10.255.255.1): `sync_in_progress` stayed TRUE\nfor ~130 SECONDS — the kernel's TCP connect timeout — before finally erroring\nwith \"failed to fetch from remote\".\n\nLocation: crates/kopi-beans/src/git/gix_compat.rs:926,\n    prepare.receive(gix::progress::Discard, &interrupt)\n\nThe interesting part: there IS already an `AtomicBool` interrupt flag right\nthere at line 924. Nothing ever sets it from a timer. So the mechanism to abort\na stalled fetch is ALREADY WIRED UP and simply never fired. Should be a small\nfix — spawn a watchdog that flips the flag at a deadline.\n\nNote the asymmetry this leaves us with, which is why it deserves fixing rather\nthan shrugging at:\n  * PUSH now has a watchdog (bd-95p): process deadline + git-native\n    http.lowSpeedLimit/lowSpeedTime.\n  * FETCH has neither. A stalled fetch still pins the lane for ~130s.\n\nUser-visible consequence even after bd-95p: `bn sync` returns promptly and\nhonestly says `sync in progress: true`, so nobody is stuck at the CLI — but the\ndaemon lane really is blocked for those ~130s, so the sync genuinely does not\nprogress and any waiter behind it just gets told to try again.\n\nUse the same deadline reasoning as bd-95p's push watchdog and cross-reference\nthe constants so the two sides stay consistent.\n","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-13T09:25:43.346000Z","updated_at":"2026-08-24T23:55:42.721000Z","external_ref":"gh-64","labels":["daemon","kopi-beans"]}
{"id":"bd-aws","title":"gix_compat: migrate_store_ref_to_v2 dry-run test fails (OrSetV1 vs LegacyEdges)","description":"git::sync::tests::migrate_store_ref_to_v2_dry_run_peeks_remote_without_mutating_tracking_ref fails at crates/kopi-beans/src/git/sync.rs:3279.\n\n  assertion `left == right` failed\n    left: OrSetV1\n   right: LegacyEdges\n\nPRE-EXISTING, not caused by the network-push change in f06e642 -- verified by stashing that change and re-running on a clean tree at 77410f2, where it fails identically. Rest of the git suite is green (130 passed, 1 failed, 4 ignored).\n\nThe test builds its own temp local/remote repos and writes a legacy-edges store commit, then asserts the dry run reports deps_format_before == LegacyEdges. It observes OrSetV1 instead, i.e. the dry run is reading a deps format that has already been converted. Worth checking whether write_store_commit_with_custom_deps is actually producing legacy edges, or whether migrate_store_ref_to_v2's peek path normalises before reporting.\n\nFound while fixing the gix push gate.","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-10T09:08:08.025000Z","updated_at":"2026-08-24T23:55:42.687000Z","external_ref":"gh-60"}
{"id":"bd-b9x","title":"runtime: our forward pass diverges from llama.cpp, and the gap GROWS with prompt length","description":"CONFIRMED against a reference implementation, not by argument. I built llama.cpp from the vendored clone (crates/kopitiam-ai/vendor/llama.cpp, MSVC/cmake, targets llama-completion + llama-tokenize) and ran it on the SAME .gguf with the SAME prompts, greedy (--temp 0).\n\nTHE PROOF, identical 33-token ChatML prompt:\n  llama.cpp : 'hello, how can I assist you today?' [end of text]\n  kopitiam  : 'I''m a bot, I''m a bot, I''m a chatbot, ...'\n\nOur distribution at that position is FLAT and disagrees hard:\n  #0 'I'=12.238  #1 'The'=12.150  #2 'A'=12.007 ... #9 'Hello'=11.237\n  llama.cpp's greedy pick 'hello' is our RANK 69, logit 8.771, gap 3.47.\nTop-10 spans only 1.0 logit. Not a near-tie flip — a real disagreement.\n\nIT SCALES WITH PROMPT LENGTH (this is the key signature):\n  5 tok  'The capital of France is'      -> ours ' Paris'=20.337 rank 0, MATCHES llama.cpp exactly\n  8 tok  '...over the lazy'              -> ours ' dog'=22.597 rank 0, MATCHES exactly\n  3 tok  'The capital of'                -> llama.cpp ' France'; ours ranks it #2, gap 0.53 (near-tie)\n  18 tok 'Once upon a time...near the'   -> ours ' sea. She had'  vs ref ' sea. She loved'\n  33 tok plain text ending 'France is'   -> ours ' a.\\n\\nThe '     vs ref ' Paris. The capital of the'\nConfident short prompts agree EXACTLY. Divergence grows with length.\n\nELIMINATED, each with evidence (do not re-check these):\n  Tokenisation: our ids are byte-identical to llama-tokenize for the chat prompt, including the 'ass'+'istant' split. No BOS on either side (add_bos_token=false, honoured).\n  Config: every field matches the file for both models (360M theta 100000, 1.7B theta 130000, head_dim/heads/kv/ffn/layers/eps).\n  RoPE: split-half/NEOX; tables verified numerically vs hand computation at positions 0,1,5,12,19,33,100 with real params -> max abs diff 0e0.\n  Tensor ops vs hand loops at REAL shapes: q@K^T [15,19,19,64] and [15,1,33,64]; probs@V incl. [32,33,33,64]; softmax normalisation; masked softmax; scalar rank-0 div; rms_norm with a NON-UNIFORM weight; silu; gather_rows; causal-mask BROADCAST across all 15 heads. All exact.\n  KV cache: concat on dim 1 correct; batched prefill and one-token-at-a-time agree.\n  Quantisation vs ggml-quants.c: dequant_q4_k == dequantize_row_q4_K, get_scale_min_k4 matches incl. the q[j-0] case, dequant_q6_k matches incl. the SIGNED i8 scales.\n  NOT the quantized kernel: running the whole model with KOPITIAM_FORCE_F32_WEIGHTS=1 (new debug knob, see bridge.rs) gives byte-identical outputs to the quantized path. So the fused kernel is not the cause.\n  NOT GQA: the 1.7B is 32/32 (no GQA) and fails the same way.\n  MLP is silu(gate)*up -> down; block is pre-norm with correct residuals. Both match llama.cpp's build_llama.\n\nWHAT IT IS NOT: model weakness. The 1.7B fails identically, and llama.cpp gets the right answer from the same file.\n\nNEXT STEP (do this, do not guess): bisect the forward pass numerically against llama.cpp layer by layer. Build its eval-callback example (GGML_DEBUG / examples/eval-callback) which dumps every intermediate tensor, run the same 33-token prompt through both, and compare after each block — embeddings, then attn_norm out, attn out, residual, ffn_norm out, mlp out. The first layer whose output diverges localises it. Everything cheaper than that has been tried and is listed above.\n\nRepro:\n  printf 'hello\\n' | kopitiam ai chat\n  llama.cpp: build/bin/Release/llama-completion.exe -m <gguf> -e -p '<|im_start|>system\\n...<|im_start|>assistant\\n' -n 20 --temp 0 -no-cnv","notes":"FIXED in commit 406cd3ed44. Root cause: WRONG RoPE PAIRING CONVENTION for llama-architecture models.\n\nrope.rs implemented split-half (NEOX) exclusively and its docs asserted 'LLaMA and every model derived from it - including every Qwen release - uses split-half'. Only the Qwen half is true. Per llama.cpp's llama_model_rope_type (src/llama-model.cpp, switch commented 'use what we call a normal RoPE, operating on pairs of consecutive head values'), LLM_ARCH_LLAMA is NORM/interleaved and LLM_ARCH_QWEN2 is NEOX/split-half. SmolLM2 reports general.architecture=llama, so DEFAULT_MODEL_ID ran with mispositioned Q and K.\n\nThe trap underneath the trap: HuggingFace LlamaAttention really does use rotate_half (split-half) in Python, but convert_hf_to_gguf.py PERMUTES the Q/K rows so ggml's interleaved rotation reproduces it. Reading the HF modelling code and concluding split-half is right about the architecture and wrong about the file.\n\nWhy it hid: at position 0 the angle is 0 so both conventions are the identity; error grows with position. Short prompts stayed correct ('The capital of France is' -> ' Paris', '...over the lazy' -> ' dog') while long ones degraded, which reads as 'small weak model'. It is also invisible to shape/sanity checks since both conventions preserve each pair's norm. Both facts are now regression tests.\n\nFound by layer-by-layer bisection against llama.cpp's eval-callback, which is what finally worked after every component had been individually verified correct in isolation:\n                ours(before)   ours(after)   llama.cpp\n  inp_embd        -6.247214     -6.247214    -6.247214\n  attn_norm-0     -4.775917     -4.775917    -4.775916\n  Qcur-0.rope    -32.685047    -37.304260   -37.306450   <- divergence starts here\n  Kcur-0.rope    +59.619232    +59.988953   +59.997219\n  attn_out-0     +11.727785    +11.524756   +11.535707\n  blk.0.out         -236.6        -241.21      -242.45\n\nFix: RopeKind::{Interleaved,SplitHalf} selected per architecture by config::rope_kind_for_architecture, mirroring llama.cpp's table; element indexing transcribed from ggml's rotate_pairs. Unknown architectures keep the split-half default, documented as a guess.\n\nA TEST WAS ASSERTING THE BUG: llama_and_qwen2_arch_with_identical_weights_produce_bit_identical_logits demanded the two architectures agree bit-for-bit because 'nothing in the forward pass branches on the arch string'. That premise WAS the bug, and the test passed for exactly as long as the bug existed. Rewritten to assert the opposite, with the reasoning recorded.\n\nVerified: ai chat 'hello' -> 'i'm happy to assist you. what can i help you with today?'; 'What is the capital of France?' -> 'The capital of France is Paris.' 142 runtime tests green, netfetch canonical gate green, clippy -D warnings clean.\n\nKnock-on: bd-2c2 (wgpu offload) is unblocked - the CPU forward pass now agrees with llama.cpp, so a GPU/CPU/llama.cpp three-way comparison is finally meaningful.","status":"closed","priority":0,"issue_type":"bug","created_at":"2026-07-27T23:47:32.838000Z","updated_at":"2026-08-24T23:55:42.641000Z","closed_at":"2026-07-28T10:30:26.108000Z","close_reason":"done","external_ref":"gh-55","labels":["kopitiam-runtime","models"],"comments":[{"id":1,"issue_id":"bd-b9x","author":"fifad@Teddy-Thinkpad","text":"FIXED in commit 406cd3ed44. Root cause: WRONG RoPE PAIRING CONVENTION for llama-architecture models.\n\nrope.rs implemented split-half (NEOX) exclusively and its docs asserted 'LLaMA and every model derived from it - including every Qwen release - uses split-half'. Only the Qwen half is true. Per llama.cpp's llama_model_rope_type (src/llama-model.cpp, switch commented 'use what we call a normal RoPE, operating on pairs of consecutive head values'), LLM_ARCH_LLAMA is NORM/interleaved and LLM_ARCH_QWEN2 is NEOX/split-half. SmolLM2 reports general.architecture=llama, so DEFAULT_MODEL_ID ran with mispositioned Q and K.\n\nThe trap underneath the trap: HuggingFace LlamaAttention really does use rotate_half (split-half) in Python, but convert_hf_to_gguf.py PERMUTES the Q/K rows so ggml's interleaved rotation reproduces it. Reading the HF modelling code and concluding split-half is right about the architecture and wrong about the file.\n\nWhy it hid: at position 0 the angle is 0 so both conventions are the identity; error grows with position. Short prompts stayed correct ('The capital of France is' -> ' Paris', '...over the lazy' -> ' dog') while long ones degraded, which reads as 'small weak model'. It is also invisible to shape/sanity checks since both conventions preserve each pair's norm. Both facts are now regression tests.\n\nFound by layer-by-layer bisection against llama.cpp's eval-callback, which is what finally worked after every component had been individually verified correct in isolation:\n                ours(before)   ours(after)   llama.cpp\n  inp_embd        -6.247214     -6.247214    -6.247214\n  attn_norm-0     -4.775917     -4.775917    -4.775916\n  Qcur-0.rope    -32.685047    -37.304260   -37.306450   <- divergence starts here\n  Kcur-0.rope    +59.619232    +59.988953   +59.997219\n  attn_out-0     +11.727785    +11.524756   +11.535707\n  blk.0.out         -236.6        -241.21      -242.45\n\nFix: RopeKind::{Interleaved,SplitHalf} selected per architecture by config::rope_kind_for_architecture, mirroring llama.cpp's table; element indexing transcribed from ggml's rotate_pairs. Unknown architectures keep the split-half default, documented as a guess.\n\nA TEST WAS ASSERTING THE BUG: llama_and_qwen2_arch_with_identical_weights_produce_bit_identical_logits demanded the two architectures agree bit-for-bit because 'nothing in the forward pass branches on the arch string'. That premise WAS the bug, and the test passed for exactly as long as the bug existed. Rewritten to assert the opposite, with the reasoning recorded.\n\nVerified: ai chat 'hello' -> 'i'm happy to assist you. what can i help you with today?'; 'What is the capital of France?' -> 'The capital of France is Paris.' 142 runtime tests green, netfetch canonical gate green, clippy -D warnings clean.\n\nKnock-on: bd-2c2 (wgpu offload) is unblocked - the CPU forward pass now agrees with llama.cpp, so a GPU/CPU/llama.cpp three-way comparison is finally meaningful.","created_at":"2026-07-28T10:30:26.042000Z"}]}
{"id":"bd-baf","title":"kopi-beans: global '--json' before a subcommand swallows it ('bn --json daemon stop' fails)","description":"Found while building bd-4et (bn daemon stop/restart). PRE-EXISTING and affects\nEVERY subcommand, not just daemon.\n\n    $ bn --json daemon stop\n    error: unrecognized subcommand 'stop'\n\nCause: the global flag is declared `--json [<JSON>]` — an OPTIONAL value. So clap\nhappily eats the next token as that value: `--json` takes `daemon` as its\nargument, and then `stop` is left dangling as an unknown top-level subcommand.\n\nWorks fine:\n    bn daemon stop --json        (flag after the subcommand)\n    bn --json=true daemon stop   (value glued on with =)\n\nWhy it matters: `--json` reads as a global flag (it IS declared globally, and it\nis listed in every subcommand's help), so putting it first is the natural thing\nto type — and the error you get back names the wrong problem entirely. It says\n\"unrecognized subcommand 'stop'\", which sends you looking for a typo in the\nsubcommand instead of at the flag. Confusing sia.\n\nOptions:\n  1. Make it a plain `bool` flag (`--json` with no value, `SetTrue`). Cleanest,\n     matches how every other CLI does it, and `--json=true` can stay working via\n     `num_args(0..=1)` + `default_missing_value`. NOTE: check the compat surface\n     first — the `[possible values: true, false]` in the help suggests something\n     out there may pass an explicit value positionally.\n  2. Keep the optional value but set `require_equals(true)`, so a bare `--json`\n     cannot consume the next token.\n\n(2) is the smaller change and fixes the reported symptom exactly. (1) is what a\nuser expects. Either way, add a test that `bn --json <subcommand>` parses.\n","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-13T00:51:49.866000Z","updated_at":"2026-08-24T23:55:42.704000Z","external_ref":"gh-62","labels":["cli","kopi-beans"]}
{"id":"bd-cfu","title":"Workspace suite cannot go green on a stock Windows box: ~28 environment-dependent tests","description":"Inventory taken 2026-07-27 on the maintainer's Windows box. 120 test targets outside the two vendored forks: 3 red, ~2900 green. None caused by that session's work. Filed so this is not rediscovered every time.\n\nTWO MECHANICAL TRAPS FIRST, both cost real time:\n* 'cargo test --workspace' STOPS at the first failing crate, so everything after kmux alphabetically silently never runs. Use --no-fail-fast.\n* Do NOT pipe cargo through 'Select-Object -First N' in PowerShell: it closes the pipe and KILLS cargo mid-run, which looks exactly like a short passing run. Cost two bogus results in one session.\n\nTHE FAILURES, all environmental:\n* kvim termemu::* / ui::app::* / lsp::install::* / editor::shell / gf (~17) — PTY, real-terminal and installed-binary assumptions.\n* kmux (4 targets, ~6 tests) — 'ConPTY executable not found on PATH: pwsh.exe' (PowerShell 7 not installed), Windows daemon spawn, mouse-border resize. queued_background_if_shell_... is FLAKY: failed one run, passed the next. Same family as bd-2wo.\n* kopi-beans (~11) — tombstone should-panics, symlink export (Windows symlinks need privilege), WAL executor.\n* providers::csharp::compile_remove_globs_exclude_matching_sources — 'Excluded\\**\\*.cs' treats backslash as a glob separator on Windows.\n* providers::python::a_non_executable_file_does_not_satisfy_detection — Windows has no executable bit, so everything looks executable.\n* providers::rust_analyzer::collects_symbols_* — rust-analyzer times out; it stood down here ('project too big for full analysis on this device').\n\nThe kopitiam-semantic lsp_types failures are NOT in this bucket — that one is a real bug, bd-e12.\n\nWANTED: #[cfg]-gate or environment-guard these so a stock Windows box can be green, and a genuine regression is visible instead of buried in expected noise. Worked example landed this session: kvim's an_unreadable_directory_renders_an_honest_error_row used std::os::unix ungated, which did not skip on Windows — it failed to COMPILE, taking the whole --all-targets build down. Now #[cfg(unix)].","notes":"PARTLY FIXED, and the inventory in this bead was WRONG about two of them. kopitiam-semantic is now fully green on Windows (147 passed, 0 failed) - see commit 107e0cf320. Two of the three were not environment noise at all, they were real cross-platform production bugs: (1) glob_matches normalized the PATTERN's backslashes but not the PATH's, so MSBuild <Compile Remove> exclusions silently did nothing on Windows - the platform where .csproj files mostly live; (2) is_executable_file returned true for EVERY file off-Unix, so a text file named pylsp on PATH was detected as a language server and handed to Command::spawn. Only the third (rust-analyzer indexing a 40-crate workspace) was genuinely environmental, and it now skips on TIMEOUT specifically while every other error still fails, so a real provider break cannot hide behind the guard. bd-klf (gguf_tokenizer vendor absence) also fixed and closed. REMAINING under this bead: the 10 kvim failures, all pty/ConPTY, plus the kopi-beans and kmux ones in the vendored forks. The kvim ones are blocked on ConPTY not working on this box at all - see bd-hyk, which also carries a proposed (unapplied, unverifiable) fix for a real ':term' bug found while investigating. LESSON worth keeping: 'these tests fail on Windows' is a hypothesis, not a diagnosis. Two of three here were the code being wrong, not the environment.","status":"open","priority":3,"issue_type":"bug","created_at":"2026-07-27T13:18:04.235000Z","updated_at":"2026-08-24T23:55:42.596000Z","external_ref":"gh-50","labels":["testing","windows"],"comments":[{"id":1,"issue_id":"bd-cfu","author":"fifad@Teddy-Thinkpad","text":"PARTLY FIXED, and the inventory in this bead was WRONG about two of them. kopitiam-semantic is now fully green on Windows (147 passed, 0 failed) - see commit 107e0cf320. Two of the three were not environment noise at all, they were real cross-platform production bugs: (1) glob_matches normalized the PATTERN's backslashes but not the PATH's, so MSBuild <Compile Remove> exclusions silently did nothing on Windows - the platform where .csproj files mostly live; (2) is_executable_file returned true for EVERY file off-Unix, so a text file named pylsp on PATH was detected as a language server and handed to Command::spawn. Only the third (rust-analyzer indexing a 40-crate workspace) was genuinely environmental, and it now skips on TIMEOUT specifically while every other error still fails, so a real provider break cannot hide behind the guard. bd-klf (gguf_tokenizer vendor absence) also fixed and closed. REMAINING under this bead: the 10 kvim failures, all pty/ConPTY, plus the kopi-beans and kmux ones in the vendored forks. The kvim ones are blocked on ConPTY not working on this box at all - see bd-hyk, which also carries a proposed (unapplied, unverifiable) fix for a real ':term' bug found while investigating. LESSON worth keeping: 'these tests fail on Windows' is a hypothesis, not a diagnosis. Two of three here were the code being wrong, not the environment.","created_at":"2026-07-28T14:08:01.078000Z"}]}
{"id":"bd-cjr","title":"Shipped: kopitiam TUI + GPL relicense + K-quant/dequant + release prep + kmux rename","description":"Delivered and pushed to main: (1) kopitiam tui — themed ratatui chat over LocalAdapter, runs the Qwen-0.5B model locally; (2) workspace relicense AGPL-3.0-only -> GPL-3.0-only; (3) GGUF K-quant dequant (Q2_K..Q8_K, bit-exact) + dequantize-to-f32 on load so K-quant models run; (4) full version-bump release prep (workspace 0.1.8, first-time CLI crates 0.1.0, seeds graduated) + rewritten publish.sh with vendor/ exclusion; (5) kopitiam-mux -> kmux rename. Commits: 32712bc, 9a39ab7, 213619(relicense), a7dbbd9, 67b2924, a5da42e, 213eef3, 2cae0cc. Remaining: maintainer runs cargo login + ./scripts/publish.sh (agents must not). Future work: bd-250 model ladder, bd-v3x Gemma runtime, bd-uij tui auto-download, bd-h58 OpenFOAM roadmap.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-25T00:59:37.868000Z","updated_at":"2026-08-24T23:55:42.533000Z","closed_at":"2026-07-25T00:59:38.064000Z","close_reason":"done","external_ref":"gh-43"}
{"id":"bd-cni","title":"bn on Windows: a read autostarts a daemon that then blocks every write","description":"Hit repeatedly while filing this session's beads, 2026-07-27. Makes bn writes effectively unusable on Windows without manual intervention.\n\nSYMPTOM: any read (bn status / list / ready) autostarts a daemon which takes the store lock. The next MUTATING command (create / comment / close / sync) then dies with:\n\n  error: store lock already held for <store-id> at ...\\store.lock\n\nWORKAROUND, needed before nearly every write: kill every bn process, run 'bn store unlock --store-id <id> --force', then run the write IMMEDIATELY before another read starts a daemon again.\n\nLIKELY CAUSE: the CLI cannot reach the running daemon over IPC and falls back to taking the store lock directly, colliding with the daemon that legitimately holds it. Two tests on this box fail in exactly that area and are probably the same root cause:\n  surface::ipc::client::tests::connect_with_autostart_override_waits_for_socket_instead_of_child_exit\n  surface::ipc::client::tests::connect_with_autostart_reports_spawned_child_exit_immediately\n\nDANGER WORTH FIXING PROPERLY, not just documenting: 'store unlock --force' does not check whether the lock holder is alive. I force-removed a lock held by a LIVE daemon (pid 6124) and a SECOND daemon (pid 10140) promptly started — two daemons on one store, which is exactly the split-brain the lock exists to prevent. No corruption resulted (store verified: 20 records intact, reads consistent), but that was luck, not design. --force should at minimum refuse, or loudly warn, when the recorded pid is still running.\n\nRelated: daemon::runtime::store::lock::tests::acquire_reclaim_rechecks_path_before_remove and the two stale_* lock tests also fail on this box — the lock reclaim path is undertested here in general.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-07-27T13:57:43.632000Z","updated_at":"2026-08-24T23:55:42.615000Z","external_ref":"gh-52","labels":["beads","windows"]}
{"id":"bd-cw3","title":"kopi-beans: add an interactive TUI (bn tui) for browsing epics, children and dependencies","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/22\n\nNot a bug — nothing broken, no workaround in use. `bn` is one-shot CLI only,\nwhich is perfect for agents but jialat for a human browsing a big store.\nReporter's store: 909 issues (`bn count`), and `bn epic status` prints 124 lines\nflat with no fold, no filter, no drill-in.\n\nToday epic support is only:\n    bn epic status          # flat stream, every epic\n    bn epic close-eligible\nThere is no `bn epic list`. To go epic -> children you must leave that output,\nrun `bn show <id>` / `bn list` per epic, and rebuild the tree in your head.\n`bn dep` has the edges but no view that walks them.\n\nWanted (`bn tui` or `bn browse`):\n  * epics as a TREE, expand to child issues inline with status/priority\n  * epic progress (n/m children closed) in context, no separate command\n  * live filter/search (status, priority, label, assignee, text), tree updates\n  * dependency navigation — jump to what blocks this / what this blocks\n  * detail pane (description, notes, deps), `bn ready` set highlighted\n  * ideally in-place mutations: claim, close, bump priority\n\nConstraints:\n  * Termux/Android capability is a stated goal of this fork — ratatui is\n    terminal-native so no conflict, but keep the dep set pure-Rust.\n  * PER CLAUDE.md: a TUI CANNOT be verified headless. This needs HUMAN\n    dogfooding. Build it, but never auto-\"fix\" its interactive behaviour on the\n    strength of a passing test.\n\nData model already supports all of it — the gap is purely presentational.\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-08-12T23:47:30.446000Z","updated_at":"2026-08-12T23:47:30.446000Z","external_ref":"gh-22","labels":["kopi-beans","tui"]}
{"id":"bd-d46","title":"kopi-beans: 'bn init' against an unreachable remote prints ERROR but still initialises and looks successful","description":"Spotted while building the bd-95p test fixtures.\n\n`bn init` in a repo whose `origin` is unreachable prints:\n\n    ERROR error: failed to fetch from remote\n\n...and then goes ahead and initialises the store anyway, exiting in a way that\nreads as success. So the user sees a red ERROR line and a working store, and\ncannot tell which of these happened:\n\n  a) init failed and they must retry, or\n  b) init succeeded locally and only the initial remote fetch failed (which is\n     fine and expected offline).\n\nIt is (b). But nothing says so.\n\nOffline / air-gapped init is a legitimate and supported thing — CLAUDE.md's\nOffline First principle says so explicitly. So the fix is NOT to make this fail.\nIt is to stop calling a survivable condition an ERROR:\n\n  * demote to a WARNING, and\n  * say what it means and what to do: \"store initialised locally; could not\n    reach <remote> to fetch existing state. If the remote has an existing beads\n    store, run `bn sync` once you are online before creating issues.\"\n\nThat last clause matters — initialising locally when the remote ALREADY has a\nstore is the one genuinely risky case (two divergent roots), and the current\nmessage gives no hint of it.\n","status":"open","priority":3,"issue_type":"bug","created_at":"2026-08-13T09:25:58.683000Z","updated_at":"2026-08-24T23:55:42.738000Z","external_ref":"gh-66","labels":["cli","kopi-beans"]}
{"id":"bd-dlo","title":"kopitiam_skill.md generator: teach it the new check/test --release, --profile and -- passthrough flags","description":"Falls out of bd-7ab / GH#1: `kopitiam check` and `kopitiam test` grew `--release`,\n`--profile <NAME>` and `-- <CARGO_ARGS>` trailing passthrough, but the agent skill\ndoes not mention any of them.\n\nMatters more here than in a normal repo: CLAUDE.md's Build Rules mandate release\nmode for this workspace, and the STANDARD RULE tells agents to reach for\n`kopitiam check --compact` / `test --compact` instead of raw cargo. So an agent\nfollowing the skill as written will run DEV-profile checks on a release-only\nworkspace and report a dev-profile answer — the exact bug GH#1 was filed about,\nreintroduced through the documentation.\n\nHARD CONSTRAINT, per CLAUDE.md: `kopitiam_skill.md` is DETERMINISTICALLY generated\nby `scripts/gen-kopitiam-skill.sh`. Fix the GENERATOR, never hand-edit the skill.\n\nWhile in there, the same section still owes the code-navigation recipe\n(tokens -> outline -> refs -> read) that CLAUDE.md says the skill currently lacks.\nWorth doing in the same pass.\n","status":"open","priority":3,"issue_type":"task","created_at":"2026-08-13T00:21:23.025000Z","updated_at":"2026-08-24T23:55:42.695000Z","external_ref":"gh-61","labels":["cli","docs"]}
{"id":"bd-e12","title":"kopitiam-semantic: lsp_types definition parsing returns ZERO results on Windows (7 tests red)","description":"Found while running the full workspace suite on the maintainer's Windows box, 2026-07-27. NOT introduced by that session's work — kopitiam-semantic was not touched.\n\nAll 7 lsp_types::tests::definition_* tests fail, and they fail by producing NOTHING rather than something wrong:\n\n  definition_parses_a_single_location_object            left: 0  right: 1\n  definition_parses_a_location_array                    left: 0  right: 2\n  definition_parses_a_location_link_array_...           left: 0  right: 1\n  definition_link_falls_back_to_target_range_...        left: 0  right: 1\n  definition_drops_a_non_file_uri_but_keeps_its_siblings left: 0  right: 1\n  definition_converts_the_target_offset_from_each_wire_encoding  index out of bounds: len is 0 but index is 0\n  definition_converts_an_offset_after_an_astral_emoji            index out of bounds: len is 0 but index is 0\n\nEvery parse yields an empty vec, so the two index-out-of-bounds panics are the same root cause showing a different face.\n\nHYPOTHESIS (not yet confirmed): file:// URI -> path conversion on Windows. A 'file:///C:/foo/bar.rs' URI has a drive letter and a leading slash that unix-shaped parsing drops or rejects, so every location is discarded as non-file. The one test that ASSERTS a non-file URI gets dropped (definition_drops_a_non_file_uri_but_keeps_its_siblings) failing with left:0 right:1 fits exactly: the real one is being dropped too.\n\nWHY IT MATTERS BEYOND THE TESTS: this is the code path behind kvim's <leader>gd / gr / hover. If it really is URI handling, go-to-definition is broken for every Windows user of kvim, not merely red in CI. Termux/Linux is unaffected, which is why this survived.\n\nNEXT STEP: read crates/kopitiam-semantic/src/lsp_types.rs around lines 485-600 and check the uri->PathBuf conversion against 'file:///C:/...'.","notes":"CORRECTION to this bead's own diagnosis (fix landed in c587071). I claimed kvim go-to-definition was broken for every Windows user. WRONG — probed instead of trusting it: on Windows file:///tmp/lib.rs -> None (what the tests hit) but file:///C:/tmp/lib.rs -> C:\\tmp\\lib.rs and file:///c%3A/tmp/lib.rs -> c:\\tmp\\lib.rs both convert fine, and jdt:// correctly drops. Real rust-analyzer on Windows sends drive-letter URIs, so gd/gr/hover was never broken in production. The tests were unix-only by accident, hardcoding file:///tmp/... and asserting /tmp/..., which quietly made the definition suite Linux-only. THE REAL DEFECT, now fixed: to_file_path() needs a drive letter on Windows, so a genuine file: URI from a Linux/Termux/WSL/container server was dropped SILENTLY and indistinguishably from the jdt: case that is meant to be dropped. Reachable because KOPITIAM targets Windows and Termux together and the server need not run on the client's platform. uri_to_path now drops on SCHEME never on platform, percent-decodes the URI path when the native converter refuses, and refuses a non-empty host (UNC) rather than stripping it into a confidently wrong local path. 6 new tests pin both platforms, cfg-splitting expectations where platforms genuinely differ instead of weakening them. kopitiam-semantic: 10 failures -> 3, the 3 left are bd-cfu's. clippy -D warnings clean.","status":"closed","priority":1,"issue_type":"bug","assignee":"fifad@Teddy-Thinkpad","created_at":"2026-07-27T13:17:39.149000Z","updated_at":"2026-08-24T23:55:42.578000Z","closed_at":"2026-07-27T13:56:38.289000Z","close_reason":"done","external_ref":"gh-48","labels":["lsp","windows"],"comments":[{"id":1,"issue_id":"bd-e12","author":"fifad@Teddy-Thinkpad","text":"CORRECTION to this bead's own diagnosis (fix landed in c587071). I claimed kvim go-to-definition was broken for every Windows user. WRONG — probed instead of trusting it: on Windows file:///tmp/lib.rs -> None (what the tests hit) but file:///C:/tmp/lib.rs -> C:\\tmp\\lib.rs and file:///c%3A/tmp/lib.rs -> c:\\tmp\\lib.rs both convert fine, and jdt:// correctly drops. Real rust-analyzer on Windows sends drive-letter URIs, so gd/gr/hover was never broken in production. The tests were unix-only by accident, hardcoding file:///tmp/... and asserting /tmp/..., which quietly made the definition suite Linux-only. THE REAL DEFECT, now fixed: to_file_path() needs a drive letter on Windows, so a genuine file: URI from a Linux/Termux/WSL/container server was dropped SILENTLY and indistinguishably from the jdt: case that is meant to be dropped. Reachable because KOPITIAM targets Windows and Termux together and the server need not run on the client's platform. uri_to_path now drops on SCHEME never on platform, percent-decodes the URI path when the native converter refuses, and refuses a non-empty host (UNC) rather than stripping it into a confidently wrong local path. 6 new tests pin both platforms, cfg-splitting expectations where platforms genuinely differ instead of weakening them. kopitiam-semantic: 10 failures -> 3, the 3 left are bd-cfu's. clippy -D warnings clean.","created_at":"2026-07-27T13:57:13.969000Z"}]}
{"id":"bd-e39","title":"kopi-beans: an orphaned daemon ignores SIGTERM indefinitely (socket deleted underneath it)","description":"Found while building bd-4et. Observed for real, not theorised.\n\nA daemon whose `daemon.sock` had been removed out from under it SURVIVED 10+\nseconds of SIGTERM and only died to SIGKILL. A healthy daemon on the same box\nexits 60/54/54 ms after SIGTERM (measured on a 55-bead store), so this is not\nslow shutdown — it is not handling the signal at all.\n\nMost likely wedged in its accept loop: blocked accepting on a socket that no\nlonger exists, so it never reaches the point where it would notice the shutdown\nflag.\n\nMitigated but NOT fixed by bd-4et: `bn daemon stop` now polls actual process\nliveness and escalates SIGTERM -> SIGKILL, so a user is no longer stuck. But\nSIGKILL means no clean shutdown — no WAL checkpoint, no orderly socket/meta\ncleanup — so we are relying on crash recovery every time this path fires. Not\nwhere you want to be for a tracker.\n\nReal fix is in the daemon: make the accept loop interruptible and the signal\nhandler effective even when the socket has gone. Worth checking whether it\nnotices socket deletion at all, since that is also a \"should I exit?\" signal.\n\nHow to reproduce: start a daemon, `rm` its `daemon.sock`, then SIGTERM it and\nwatch it sit there.\n","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-13T00:51:49.889000Z","updated_at":"2026-08-24T23:55:42.712000Z","external_ref":"gh-63","labels":["daemon","kopi-beans"]}
{"id":"bd-ef2","title":"kopi-beans: two #[should_panic] tests can never pass under --release (rely on debug_assert!)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/18\n\n`crates/kopi-beans/src/core/tombstone.rs` — `Tombstone::join` guards its two\nCRDT preconditions with `debug_assert_eq!` (lines 60-61), and two tests\n(`test_join_different_id_panics` @162, `test_join_different_lineage_panics` @170)\nare `#[should_panic]` on exactly those messages.\n\nUnder `--release` `debug_assert!` compiles out, `join` returns normally, and both\ntests fail with \"test did not panic as expected\". So they are STRUCTURALLY\nincapable of passing in release mode — fail every time, clean tree, no defect.\nThis workspace is release-only (CLAUDE.md Build Rules), so\n`cargo test --release -p kopi-beans` can never be green, and a real regression\nhas to be hand-separated from these two permanent reds. Cannot trust like that.\n\nOptions the reporter listed, in rough order:\n  1. `#[cfg(debug_assertions)]` on both tests — cheapest, but leaves the\n     invariant unchecked in release.\n  2. Promote both to real `assert_eq!`. `join` is a CRDT merge — joining two\n     tombstones with different ids/lineages is a corruption bug, not a hot path.\n     Cost is two comparisons per join.\n  3. Make `join` return `Result` and test the error.\n\nRecommendation: (2). A CRDT merge that silently accepts mismatched lineage\nproduces wrong data quietly, which is worse than a panic. Two `assert_eq!` on a\nmerge is not a perf problem.\n\nRelated (NOT this bead, needs its own): reporter saw `daemon::runtime::*`\n(WAL/failpoint/HLC) tests flaky in release — failure count varied 10/11/12 across\nrepeated runs on an unmodified tree.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:46:57.175000Z","updated_at":"2026-08-13T00:20:32.090000Z","closed_at":"2026-08-13T00:20:32.090000Z","close_reason":"done","external_ref":"gh-18","labels":["kopi-beans"]}
{"id":"bd-fc1","title":"gpu: validate wgpu against mesa lavapipe — NVIDIA's driver is permissive and hides AMD-portability bugs","description":"Context: crates/kopitiam-gpu and apps using wgpu 30 (plus outram-park-backend, which\nexplicitly selects eframe's `wgpu` feature over `glow`) are only ever exercised against\nNVIDIA's proprietary Vulkan driver on the maintainer's box (RTX 3050, GA107, nvidia-open\nkernel module + proprietary Vulkan/GL userspace).\n\nTHE PROBLEM\nNVIDIA's Vulkan driver is permissive. It accepts shader and API usage that the mesa\ndrivers reject outright. RADV (AMD), NVK and lavapipe all share the mesa validation\nbehaviour, so code that is clean on NVIDIA can still fail hard on any AMD card. This is\nthe classic \"works on my machine, breaks on AMD\" class of bug, and right now nothing in\nthe workspace would catch it before a user does.\n\nThis matters more than usual here because AMD compatibility is a stated project\nconstraint, not a nice-to-have.\n\nPROPOSED FIX — test against lavapipe, do not switch drivers\nvulkan-swrast (lavapipe) is mesa's CPU Vulkan driver. It is built from the same mesa\ncodebase as RADV, so it enforces the same strictness, and it:\n  * installs alongside the NVIDIA driver, no driver switch, no reboot\n  * runs headless, so it works in CI\n  * is slow, which is irrelevant because this tests correctness, not framerate\n\n    sudo pacman -S vulkan-swrast vulkan-validation-layers\n    VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json \\\n      VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation \\\n      WGPU_BACKEND=vulkan cargo test -p kopitiam-gpu\n\n(confirm the lavapipe ICD filename after install — recent mesa dropped the .x86_64\nsuffix on some manifests, e.g. this box has radeon_icd.json, not radeon_icd.x86_64.json)\n\nWHY NOT JUST USE NVK\nNVK (vulkan-nouveau) would give the same mesa strictness on real hardware, but it only\nenumerates devices when the `nouveau` kernel driver is bound. With nvidia-open loaded it\nfails outright — verified on this box:\n\n    VK_DRIVER_FILES=/usr/share/vulkan/icd.d/nouveau_icd.json vulkaninfo --summary\n    -> vkEnumeratePhysicalDevices failed with ERROR_INITIALIZATION_FAILED\n\nSwitching to nouveau is viable on Ampere now (GSP reclocking, default since Linux 6.18)\nbut costs roughly half the proprietary driver's performance and still has incomplete ray\ntracing. Not worth it just to get validation that lavapipe provides for free.\n\nRELATED\nbd-2c2 (gpu: offload the matmuls to wgpu) — that work should land with this validation\npath already in place, otherwise the compute shaders are the most likely place for an\nNVIDIA-only assumption to hide.\n","acceptance_criteria":"A documented, repeatable way to run the wgpu paths against mesa lavapipe on the maintainer's NVIDIA box, with validation layers on, that does not require unloading the nvidia driver. Any spec violations it surfaces are either fixed or filed as their own beads.","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-11T15:59:40.819000Z","updated_at":"2026-08-24T23:48:27.329000Z","external_ref":"gh-20","labels":["gpu","portability","testing","wgpu"]}
{"id":"bd-g7q","title":"kopitiam check/test: --compact/--json still exit 0 when there ARE real diagnostics or test failures","description":"Found while fixing bd-7ab / GH#1. Left out of that fix on purpose: changing it\nalters behaviour for EVERY existing caller, so it wants its own decision.\n\nWhat got fixed in bd-7ab: cargo dying while producing nothing now errors instead\nof printing a cheerful \"no test failures\" with exit 0. That was the severe case —\na build failure reporting a clean suite.\n\nWhat is STILL true: when cargo exits non-zero because there are genuine\ndiagnostics or genuine test failures, the `--compact` / `--json` paths report\nthem correctly on stdout but still exit **0**. So this silently passes:\n\n    kopitiam test --compact && echo \"safe to deploy\"\n\nAny `&&` chain, CI step, or pre-commit hook that trusts the exit code gets a\nfalse all-clear — the same class of bug GH#1 was about, just the last corner of\nit. An agent reading stdout is fine; a shell reading `$?` is not.\n\nThe fix is easy (propagate cargo's status, or exit non-zero when the reduced\nreport is non-empty). The DECISION is not, because anything currently relying on\n`kopitiam check --compact` exiting 0 will start failing. Needs the maintainer's\ncall on whether that is a breaking change worth making, and if so whether it\nlands behind a flag first (`--exit-code`, like `git diff`) or straight up in the\nnext minor.\n\nRecommendation: do it, and do it unflagged. An exit code that cannot express\nfailure is not worth preserving, and this tool's whole selling point is being\nthe authoritative check.\n","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-13T00:21:22.992000Z","updated_at":"2026-08-13T00:21:22.992000Z","external_ref":"gh-1","labels":["cli"]}
{"id":"bd-h58","title":"OpenFOAM → KOPITIAM scientific crates: FVM/solver translation roadmap","description":"3-part OpenFOAM-dev survey (GPL-3.0 vendored, future work). Translate FV math+data-layout, not C++ machinery. Crates to build: kopitiam-units (Dimension[i8;7]+Dimensioned<T>), NEW kopitiam-mesh (polyMesh: points+CSR faces+owner/neighbour, geometry algos, ASCII reader), kopitiam-discretization (Field<T,Loc>, fvc:: Gauss ops, enum schemes), kopitiam-equation (FvMatrix LDU + fvm::), kopitiam-solver (LduMatrix+PCG/PBiCGStab+DIC/DILU, defer GAMG), kopitiam-scientific (ODE: ButcherTableau RK). First targets: Dimension → mesh geom+owner/neighbour → Field → fvc grad/laplacian → FvMatrix LDU + PCG; milestone = steady diffusion solve with analytic test. Decisions: add mesh/solver/scientific crates; runtime Dimension vs uom; fields no-mesh-ref; enum-dispatch schemes; serial-first keep MPI out of v1; translate-vs-reimplement-from-literature per item. Full detail in session notes; recommend ADR + engineering-journal before coding.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-07-24T23:29:25.826000Z","updated_at":"2026-08-24T23:55:42.472000Z","external_ref":"gh-36","labels":["openfoam","scientific","translation"]}
{"id":"bd-hfd","title":"kopi-beans: clock_skew's POSITIVE branch is a staleness detector, not a skew detector (false positive on any idle store)","description":"Found while fixing bd-pl2 / GH#21. Deliberately left alone there because it is a\nBEHAVIOUR change, not a message change, and would invert an existing test.\n\nThe finding: `detect_clock_skew(now_ms, reference_ms)` compares the local wall\nclock against `WriteStamp::wall_ms` — which is wall-clock-AT-WRITE-TIME. So a\npositive delta is just THE AGE OF THE NEWEST WRITE. `CLOCK_SKEW_WARN_MS` is 5\nminutes (crates/kopi-beans/src/daemon/runtime/core/helpers.rs). Meaning: any\nstore nobody wrote to for 5 minutes warns about clock skew ON A PERFECTLY\nCORRECT CLOCK.\n\nThat is exactly what GH#21's reporter hit. Their +61 min was consistent with\n\"nobody wrote for 61 minutes\", NOT with their (genuinely) unsynced NTP. We sent\nthem chasing a clock problem that was not there. Aiyo.\n\nThe NEGATIVE branch is the real signal — a future-dated stamp cannot be\nexplained by an idle store.\n\nOptions:\n  1. Warn only on negative delta. Cheapest, kills the false positive dead, but\n     gives up on detecting a locally-fast clock (which DOES matter — see below).\n  2. Compare against a second clock source (remote HEAD commit time, peer stamp\n     during replication) so there is a real second opinion.\n  3. Keep positive warnings but require corroboration — e.g. only warn positive\n     when a write has landed recently, so \"ahead\" cannot be explained by idleness.\n\nWhy it is not merely cosmetic: `Lww::join` (core/crdt.rs:38-49) orders on\n`WriteStamp::wall_ms` FIRST (core/time.rs:44-49,148-151), so a fast-clocked host\nwins merges it should lose. Same comparison drives note ordering\n(core/state/notes.rs:97, core/bead.rs:764) and the delete-vs-recreate\nresurrection rule. Partially absorbed by `Clock::tick` clamping forward jumps\npast `hlc_max_forward_drift_ms` (default 600_000, core/limits.rs:131). So we DO\nwant real skew detection — we just don't currently have it in the positive\ndirection.\n\nGUARD ALREADY IN PLACE: test `detect_clock_skew_positive_side_also_fires_on_a_\nmerely_idle_store` documents the current behaviour, so whoever changes it does\nso deliberately and not by accident.\n","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-13T00:21:01.262000Z","updated_at":"2026-08-13T00:21:01.262000Z","external_ref":"gh-21","labels":["kopi-beans"]}
{"id":"bd-hyk","title":"kvim: ':term <cmd>' cannot work on Windows - hardcoded /bin/sh -c (patch proposed, needs dogfooding)","description":"PROPOSED, NOT APPLIED. kvim's :term is an interactive surface and CLAUDE.md says to flag rather than auto-fix what cannot be verified without a real terminal. I wrote this patch, could not verify it, and reverted it. Needs the maintainer's dogfooding.\n\nTHE BUG, which is unambiguous: crates/kopitiam-neovim/src/termemu.rs build_command() does\n\n    let shell = std::env::var(SHELL).unwrap_or_else(|_| /bin/sh.to_string());\n    cmd.arg(-c);\n\nThere is no /bin/sh on Windows, and '-c' is not cmd's flag. So ':term ls' on Windows spawns nothing and paints an empty pane with no error. On Termux this is fine (SHELL is set, /bin/sh exists), so it only bites the desktop.\n\nPROPOSED PATCH (cfg-split inside build_command):\n  windows  -> %COMSPEC%, else cmd.exe, with /C\n  not(windows) -> \\, else /bin/sh, with -c\nDo NOT consult \\ on Windows: Git Bash exports it, and honouring it would hand a cmd-flavoured command line to sh.\n\nWHY I COULD NOT VERIFY IT: the four termemu pty tests still fail on this box after the patch, with an EMPTY screen rather than a spawn error - so TermSession::spawn succeeds and the child produces nothing. That points below the shell choice, at ConPTY itself. Corroborating: kmux's own windows tests fail here with 'ConPTY executable not found on PATH: pwsh.exe' (crates/kmux/src/pty/backend/windows/application.rs:33). Something about ConPTY on this machine is not working, and until that is sorted the pty tests cannot confirm or deny the shell fix.\n\nSECOND, SEPARATE ISSUE FOUND: the pty tests hardcode UNIX COMMANDS - 'printf hello', 'cat', 'exit 3'. Even with a working ConPTY those cannot pass on Windows, because cmd.exe has none of them. If the shell fix lands, they need platform-appropriate commands too: 'echo X' works in both shells, and 'findstr \"^\"' is the cmd equivalent of 'cat' (matches every line, reprints it). That part is test-only and safe to apply without dogfooding.\n\nORDER TO DO THIS IN: (1) find out why ConPTY does not work on this box - it blocks both kvim and kmux, and without it nothing here is verifiable; (2) apply the shell fix and confirm ':term ls' actually paints output in a real terminal; (3) make the test commands portable so the suite can hold the line afterwards.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-07-28T14:07:40.748000Z","updated_at":"2026-08-24T23:55:42.678000Z","external_ref":"gh-59","labels":["kvim","windows"]}
{"id":"bd-i11","title":"Unblock the crates.io publish chain: tensor/models 0.0.2 bump, kopitiam-ai vendor exclude, apps/cli path dep","description":"scripts/publish.sh now covers all 17 crates `kopitiam` (apps/cli) depends on, in topological order, with per-crate versions (the workspace is no longer single-version). Its preflight blocks the run on four things. One is done, three are NOT — they sit in files owned by other agents this session, so this session never touched them.\n\nDONE (this session):\n- kopitiam-core 0.0.1 -> 0.0.2 (crates/kopitiam-core/Cargo.toml) + matching req in root Cargo.toml [workspace.dependencies]. Workspace resolves; Cargo.lock updated.\n- scripts/publish.sh: 9 -> 17 crates, per-crate crate_version(), preflight, quiet already_published().\n\nSTILL BLOCKING (preflight FATALs, in order):\n1. kopitiam-tensor is live at 0.0.1 but its local source has moved on — published dtype.rs has no Q2_K..Q8_K, local quant.rs/storage.rs/tensor/{concat,matmul}.rs all use them. Bump to 0.0.2 in crates/kopitiam-tensor/Cargo.toml AND the req in root Cargo.toml, same commit. ^0.0.1 matches ONLY 0.0.1 (every 0.0.x is its own major), so bumping one side alone breaks the local build.\n2. kopitiam-models same story: live at 0.0.1, local has a new src/hf.rs plus changed catalog.rs/lib.rs. Needs 0.0.2. It is pinned inline in its own [package], no workspace req to chase.\n3. crates/kopitiam-ai/Cargo.toml needs `exclude = [\"vendor/\"]`. .gitignore does not save you — vendor/llama.cpp is a nested git repo and cargo's packager walks in. Measured: cargo package --list -p kopitiam-ai = 677 files, 660 under vendor/, ~198 MB vs crates.io's 10 MiB cap. Also a GPLv3 hygiene problem: do not ship vendored llama.cpp/ggml/candle/tensorflow inside our tarball.\n4. apps/cli/Cargo.toml line 24: kopitiam-models = { path = \"../../crates/kopitiam-models\" } has no version, so `kopitiam` itself cannot publish. Move it to root [workspace.dependencies] with path+version and use { workspace = true } here, like every other internal dep.\n\nALSO WORTH DOING BEFORE ANY PUBLISH (not preflight-enforced):\n- No crate dir carries a LICENSE file. cargo only packages files under the crate dir, so the root LICENSE never travels — every published tarball so far ships GPL-3.0-only with no licence text in it. kopitiam-core 0.0.1 already went up that way.\n- kopitiam-ai (and most of the runtime chain) has no README, so the crates.io page renders blank. Ties into the CLAUDE.md open question about Singlish on docs.rs / the public crate face.\n\nRATE LIMIT: six brand-new names in the list (loader, tokenizer, runtime, ai, workflow, kopitiam). crates.io burst is 5, refill 1 per 10 min, so expect one ~10 min pause near the end. publish_with_retry() parses the Retry-After and sleeps it out. Bumping an existing name is a looser limit.\n\nPublishing stays maintainer-only: run ./scripts/publish.sh --dry-run first, and the real run only on an explicit prompt.","notes":"PROGRESS 2026-07-25 (SGT, Saturday — outside NUS hours):\n\nkopitiam-ai's whole dependency closure is now publish-clean. Verified with\n  cargo publish --dry-run --allow-dirty -p kopitiam-core -p kopitiam-tensor -p kopitiam-loader -p kopitiam-tokenizer -p kopitiam-runtime -p kopitiam-ai\nall six package + verify, nothing uploaded. Release tests green across the six (28/15/18/12/122/123/43/9 passed, 0 failed).\n\nDONE:\n- kopitiam-core 0.0.1 -> 0.0.2, kopitiam-tensor 0.0.1 -> 0.0.2, both with matching reqs in root Cargo.toml [workspace.dependencies] (same commit — ^0.0.x is exact).\n- crates/kopitiam-ai/Cargo.toml: exclude = [\"vendor/\"]. cargo package --list went 677 files / ~198 MB -> 16 files / 94.6 KiB (29.7 KiB compressed). Was a hard crates.io 10 MiB failure AND a GPLv3 hygiene problem.\n- .gitignore: crates/kopitiam-ai/vendor/ -> crates/*/vendor/, with a note that gitignore alone does NOT keep a nested-repo vendor tree out of cargo package.\n- scripts/publish.sh: 17-crate topological list, per-crate crate_version(), 4-check preflight, quiet already_published() (no more curl 404 spam).\n\nSTILL BLOCKING the FULL chain (both about kopitiam-models, which is a dep of apps/cli, NOT of kopitiam-ai — out of scope of the session's instruction, files left untouched):\n1. kopitiam-models live at 0.0.1 but local source has a new src/hf.rs + changed catalog.rs/lib.rs. Needs 0.0.2 in crates/kopitiam-models/Cargo.toml (pinned inline, no workspace req to chase).\n2. apps/cli/Cargo.toml:24 — kopitiam-models = { path = \"../../crates/kopitiam-models\" } has no version, so `kopitiam` itself cannot publish. Move to root [workspace.dependencies] with path+version, use { workspace = true } here.\n\nNEW FINDING — publish.sh may be largely obsolete. Cargo 1.97 supports multi-package publish (cargo publish -p a -p b ...): it computes the topological order ITSELF and verifies each package against a temp registry built from its packaged siblings, so a whole chain of never-published interdependent crates can be dry-run in one shot (proved above). That removes the need for the script's hand-rolled CRATES ordering AND wait_for_index(). What stays worth keeping: the preflight checks (stale-seed, vendor/, path-dep-without-version) and publish_with_retry()'s 429 Retry-After handling, none of which cargo does. Worth an AID + a rewrite pass.\n\nNOT DONE, still worth doing before any real publish: no crate dir carries a LICENSE file (cargo only packages files under the crate dir, so root LICENSE never travels — every tarball so far ships GPL-3.0-only with no licence text); no README on the runtime chain, so crates.io pages render blank.\n\nNothing committed, nothing published.","status":"open","priority":1,"issue_type":"task","created_at":"2026-07-24T23:34:48.635000Z","updated_at":"2026-08-24T23:55:42.481000Z","external_ref":"gh-37","comments":[{"id":1,"issue_id":"bd-i11","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"PROGRESS 2026-07-25 (SGT, Saturday — outside NUS hours):\n\nkopitiam-ai's whole dependency closure is now publish-clean. Verified with\n  cargo publish --dry-run --allow-dirty -p kopitiam-core -p kopitiam-tensor -p kopitiam-loader -p kopitiam-tokenizer -p kopitiam-runtime -p kopitiam-ai\nall six package + verify, nothing uploaded. Release tests green across the six (28/15/18/12/122/123/43/9 passed, 0 failed).\n\nDONE:\n- kopitiam-core 0.0.1 -> 0.0.2, kopitiam-tensor 0.0.1 -> 0.0.2, both with matching reqs in root Cargo.toml [workspace.dependencies] (same commit — ^0.0.x is exact).\n- crates/kopitiam-ai/Cargo.toml: exclude = [\"vendor/\"]. cargo package --list went 677 files / ~198 MB -> 16 files / 94.6 KiB (29.7 KiB compressed). Was a hard crates.io 10 MiB failure AND a GPLv3 hygiene problem.\n- .gitignore: crates/kopitiam-ai/vendor/ -> crates/*/vendor/, with a note that gitignore alone does NOT keep a nested-repo vendor tree out of cargo package.\n- scripts/publish.sh: 17-crate topological list, per-crate crate_version(), 4-check preflight, quiet already_published() (no more curl 404 spam).\n\nSTILL BLOCKING the FULL chain (both about kopitiam-models, which is a dep of apps/cli, NOT of kopitiam-ai — out of scope of the session's instruction, files left untouched):\n1. kopitiam-models live at 0.0.1 but local source has a new src/hf.rs + changed catalog.rs/lib.rs. Needs 0.0.2 in crates/kopitiam-models/Cargo.toml (pinned inline, no workspace req to chase).\n2. apps/cli/Cargo.toml:24 — kopitiam-models = { path = \"../../crates/kopitiam-models\" } has no version, so `kopitiam` itself cannot publish. Move to root [workspace.dependencies] with path+version, use { workspace = true } here.\n\nNEW FINDING — publish.sh may be largely obsolete. Cargo 1.97 supports multi-package publish (cargo publish -p a -p b ...): it computes the topological order ITSELF and verifies each package against a temp registry built from its packaged siblings, so a whole chain of never-published interdependent crates can be dry-run in one shot (proved above). That removes the need for the script's hand-rolled CRATES ordering AND wait_for_index(). What stays worth keeping: the preflight checks (stale-seed, vendor/, path-dep-without-version) and publish_with_retry()'s 429 Retry-After handling, none of which cargo does. Worth an AID + a rewrite pass.\n\nNOT DONE, still worth doing before any real publish: no crate dir carries a LICENSE file (cargo only packages files under the crate dir, so root LICENSE never travels — every tarball so far ships GPL-3.0-only with no licence text); no README on the runtime chain, so crates.io pages render blank.\n\nNothing committed, nothing published.","created_at":"2026-07-24T23:39:12.291000Z"}]}
{"id":"bd-jrq","title":"gpu: quantized resident output-head matmul - the 25.67ms/token that IS the chat lag","description":"Split out of bd-2c2 after measurement identified it as the single highest-value GPU target.\n\nWHY THIS ONE FIRST. The output projection (49152 x 960) costs 25.67ms per token on CPU - MORE THAN EVERY OTHER DECODE MATMUL COMBINED (attn q/o 443us, mlp gate/up 1.28ms, mlp down 1.29ms). It is the chat lag. Everything else is noise next to it.\n\nWHY IT CANNOT USE THE EXISTING KERNEL. As f32 the weight is 188MB, against a 128MB max_storage_buffer_binding_size on the maintainer's Intel adapter - it does not fit a single storage binding, and matmul_nt_gpu correctly refuses it (see the guard added in ce74f2b1be). Kept Q8_0 it is ~47MB, which fits comfortably.\n\nWHAT IT NEEDS:\n1. A q8 matmul kernel: dequantise in-shader (block scale per 32 elements, ggml Q8_0 layout - crates/kopitiam-tensor/src/quant.rs::dequant_q8_0 is the reference, and it matches ggml byte for byte as verified during bd-b9x).\n2. Weight residency: uploaded once at model load, not per call. Per-call upload is what makes decode a 0.24-0.45x LOSS today.\n3. The usual comparison discipline: element-wise against the CPU twin at the real shape, then end-to-end against llama.cpp per docs/REFERENCE-ORACLE.md.\n\nWATCH OUT: the vocab dimension is 49152, so the output is a 49152-wide row per token. Do not read it back and then argmax on the CPU if the sampler can run on-device later - but do NOT fuse sampling into this change; get the matmul right first, measure, then decide.\n\nDepends on the resident-weight design from bd-2c2 item 3, or can carry its own if that lands later.","notes":"KERNEL DONE and measured, commit 685cb2943f. The GPU half of this bead is complete; what remains is wiring it into kopitiam-runtime.\n\nMEASURED, SmolLM2-360M output head (49152 x 960), Intel integrated GPU vs 14 CPU cores:\n  one-time upload : 97.12ms\n  cpu  per token  : 26.12ms\n  gpu  per token  :  8.55ms   (3.06x)\n  upload amortises after ~6 tokens\nAbout 17.6ms saved per token on the dominant decode cost, paid back inside a six-token reply. For contrast the naive per-call f32 path in the same bench is a 0.24-0.45x LOSS - the entire difference is residency, not the kernel.\n\nWHAT LANDED: ResidentBlockQ8Weight in kopitiam-gpu. Upload once at model load, dispatch per token. Deliberately expressed as GENERIC block-scaled int8 (32 quants per f32 scale) rather than named Q8_0, because kopitiam-gpu describes itself as domain-agnostic parallel-compute infrastructure and must not learn GGUF; the caller separates scales from quants. Format knowledge stays in kopitiam-runtime where the layout is already verified byte-for-byte against ggml-quants.c.\n\nTHE RISK THE TESTS ARE BUILT AROUND: WGSL has no i8, so quants travel packed four-per-u32 and the shader sign-extends by hand. Get it wrong and 0xFF reads as +255 instead of -1, every negative weight flips sign, and the model emits fluent nonsense rather than failing. Fixtures span the full signed range including -128 and -1; a fixture of small positives would pass either way.\n\nREMAINING, to actually reach the chat:\n1. Repack the GGUF Q8_0 output.weight into (scales, quants) at model load. kopitiam-tensor's dequant_q8_0 already documents the block layout; this needs the inverse split, not a dequantise.\n2. Hold the ResidentBlockQ8Weight next to the model and route linear() to it for output.weight only, guarded on Executor::has_gpu(), CPU twin otherwise.\n3. COMPARE AGAINST LLAMA.CPP END TO END, per the maintainer's standing instruction - not merely against our own CPU twin. docs/REFERENCE-ORACLE.md has the procedure. bd-b9x is fixed so the CPU path is a trustworthy baseline now.\n4. Only then consider prefill offload (matmul_nt, worth 3.9-5.9x on prompt ingestion) and a tiled kernel.\n\nCAVEAT worth stating plainly: 97ms of upload at model load is fine for a chat session but is pure cost for a one-shot CLI invocation that emits a couple of tokens. Whatever wires this in should decide residency lazily - on first use, or when the reply is expected to exceed ~6 tokens - rather than unconditionally at load.\n\nDONE and wired into the chat, commits 70e172cf9d + 00536c9ec5.\n\nMEASURED A/B on the same binary, same prompts, greedy (gpu feature on vs --no-default-features):\n                              gpu on    gpu off   speedup\n  model load                   570ms      295ms   +275ms once\n  'The capital of France is'   1.37s      6.77s     4.9x\n  '...over the lazy'           1.68s      7.03s     4.2x\n  chat, 24 tokens              5.93s     14.40s     2.4x\n\nCOMPARED TO LLAMA.CPP as instructed - not merely against our own CPU twin. Byte-identical:\n  ours (hybrid) -> 'hello, how can I assist you today?'\n  llama.cpp     -> 'hello, how can I assist you today?'\nAnd every greedy completion matches between gpu-on, gpu-off and llama.cpp.\n\nSCOPE: only the output projection is offloaded. The small decode matmuls measure 0.24-0.45x on the GPU, so offloading uniformly would have made chat 2-4x SLOWER. That is why this bead was worth splitting out of bd-2c2.\n\nA CPU-SIDE FINDING WORTH ITS OWN BEAD fell out of this. Neither SmolLM2 ships a separate output.weight; both tie it to token_embd.weight. ModelWeights loads token_embd via load_tensor_f32 (the embedding lookup needs real floats) and the tied fallback CLONES it - so the single biggest matmul in the model has been running against a fully dequantized f32 weight, skipping the fused quantized kernel every other projection gets, and holding 188MB of f32 where 47MB of Q8_0 would do. Fixing the CPU path to keep the quantized tensor for the output projection would speed up and shrink the no-GPU case too, which matters for Termux where there is no usable Vulkan driver.\n\nFEATURE GATE: 'gpu', default ON. Not for Termux's sake - cargo check --target aarch64-linux-android passes both with and without it, and the cascade handles a missing GPU at runtime. It exists so a lean build can say --no-default-features and skip the wgpu tree entirely.\n\nHONESTY FIX: the CLI notice said 'Using local model on CPU' unconditionally, which stopped being true. Now reports 'hybrid CPU + GPU (output projection on GPU)' or 'CPU', from QwenModel::gpu_output_head_active().\n\nFalls through to CPU on any GPU failure mid-session - a slower answer beats a wrong one.\n\nGates: 146 runtime + 11 gpu + 310 cli + 70 ai green, clippy -D warnings clean in BOTH feature states, aarch64-linux-android clean in both.\n\nCROSS-REFERENCE from bd-8f8: the 4.9x figure in this bead was measured against a CPU path that was itself defective - it dequantized the tied embedding/output weight to f32 and skipped the fused quantized kernel. With that fixed (bd-8f8), the GPU output head is worth about 6 percent on SmolLM2-360M, not 4.9x. The kernel remains correct and is still the right foundation for larger models and prefill offload, but the honest current value on this model is small. Do not quote the 4.9x without this correction.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-28T11:52:50.079000Z","updated_at":"2026-08-24T23:55:42.659000Z","closed_at":"2026-07-28T12:44:45.932000Z","close_reason":"done","external_ref":"gh-57","labels":["kopitiam-gpu","performance"],"comments":[{"id":1,"issue_id":"bd-jrq","author":"fifad@Teddy-Thinkpad","text":"KERNEL DONE and measured, commit 685cb2943f. The GPU half of this bead is complete; what remains is wiring it into kopitiam-runtime.\n\nMEASURED, SmolLM2-360M output head (49152 x 960), Intel integrated GPU vs 14 CPU cores:\n  one-time upload : 97.12ms\n  cpu  per token  : 26.12ms\n  gpu  per token  :  8.55ms   (3.06x)\n  upload amortises after ~6 tokens\nAbout 17.6ms saved per token on the dominant decode cost, paid back inside a six-token reply. For contrast the naive per-call f32 path in the same bench is a 0.24-0.45x LOSS - the entire difference is residency, not the kernel.\n\nWHAT LANDED: ResidentBlockQ8Weight in kopitiam-gpu. Upload once at model load, dispatch per token. Deliberately expressed as GENERIC block-scaled int8 (32 quants per f32 scale) rather than named Q8_0, because kopitiam-gpu describes itself as domain-agnostic parallel-compute infrastructure and must not learn GGUF; the caller separates scales from quants. Format knowledge stays in kopitiam-runtime where the layout is already verified byte-for-byte against ggml-quants.c.\n\nTHE RISK THE TESTS ARE BUILT AROUND: WGSL has no i8, so quants travel packed four-per-u32 and the shader sign-extends by hand. Get it wrong and 0xFF reads as +255 instead of -1, every negative weight flips sign, and the model emits fluent nonsense rather than failing. Fixtures span the full signed range including -128 and -1; a fixture of small positives would pass either way.\n\nREMAINING, to actually reach the chat:\n1. Repack the GGUF Q8_0 output.weight into (scales, quants) at model load. kopitiam-tensor's dequant_q8_0 already documents the block layout; this needs the inverse split, not a dequantise.\n2. Hold the ResidentBlockQ8Weight next to the model and route linear() to it for output.weight only, guarded on Executor::has_gpu(), CPU twin otherwise.\n3. COMPARE AGAINST LLAMA.CPP END TO END, per the maintainer's standing instruction - not merely against our own CPU twin. docs/REFERENCE-ORACLE.md has the procedure. bd-b9x is fixed so the CPU path is a trustworthy baseline now.\n4. Only then consider prefill offload (matmul_nt, worth 3.9-5.9x on prompt ingestion) and a tiled kernel.\n\nCAVEAT worth stating plainly: 97ms of upload at model load is fine for a chat session but is pure cost for a one-shot CLI invocation that emits a couple of tokens. Whatever wires this in should decide residency lazily - on first use, or when the reply is expected to exceed ~6 tokens - rather than unconditionally at load.","created_at":"2026-07-28T11:59:42.727000Z"},{"id":2,"issue_id":"bd-jrq","author":"fifad@Teddy-Thinkpad","text":"DONE and wired into the chat, commits 70e172cf9d + 00536c9ec5.\n\nMEASURED A/B on the same binary, same prompts, greedy (gpu feature on vs --no-default-features):\n                              gpu on    gpu off   speedup\n  model load                   570ms      295ms   +275ms once\n  'The capital of France is'   1.37s      6.77s     4.9x\n  '...over the lazy'           1.68s      7.03s     4.2x\n  chat, 24 tokens              5.93s     14.40s     2.4x\n\nCOMPARED TO LLAMA.CPP as instructed - not merely against our own CPU twin. Byte-identical:\n  ours (hybrid) -> 'hello, how can I assist you today?'\n  llama.cpp     -> 'hello, how can I assist you today?'\nAnd every greedy completion matches between gpu-on, gpu-off and llama.cpp.\n\nSCOPE: only the output projection is offloaded. The small decode matmuls measure 0.24-0.45x on the GPU, so offloading uniformly would have made chat 2-4x SLOWER. That is why this bead was worth splitting out of bd-2c2.\n\nA CPU-SIDE FINDING WORTH ITS OWN BEAD fell out of this. Neither SmolLM2 ships a separate output.weight; both tie it to token_embd.weight. ModelWeights loads token_embd via load_tensor_f32 (the embedding lookup needs real floats) and the tied fallback CLONES it - so the single biggest matmul in the model has been running against a fully dequantized f32 weight, skipping the fused quantized kernel every other projection gets, and holding 188MB of f32 where 47MB of Q8_0 would do. Fixing the CPU path to keep the quantized tensor for the output projection would speed up and shrink the no-GPU case too, which matters for Termux where there is no usable Vulkan driver.\n\nFEATURE GATE: 'gpu', default ON. Not for Termux's sake - cargo check --target aarch64-linux-android passes both with and without it, and the cascade handles a missing GPU at runtime. It exists so a lean build can say --no-default-features and skip the wgpu tree entirely.\n\nHONESTY FIX: the CLI notice said 'Using local model on CPU' unconditionally, which stopped being true. Now reports 'hybrid CPU + GPU (output projection on GPU)' or 'CPU', from QwenModel::gpu_output_head_active().\n\nFalls through to CPU on any GPU failure mid-session - a slower answer beats a wrong one.\n\nGates: 146 runtime + 11 gpu + 310 cli + 70 ai green, clippy -D warnings clean in BOTH feature states, aarch64-linux-android clean in both.","created_at":"2026-07-28T12:44:45.862000Z"},{"id":3,"issue_id":"bd-jrq","author":"fifad@Teddy-Thinkpad","text":"CROSS-REFERENCE from bd-8f8: the 4.9x figure in this bead was measured against a CPU path that was itself defective - it dequantized the tied embedding/output weight to f32 and skipped the fused quantized kernel. With that fixed (bd-8f8), the GPU output head is worth about 6 percent on SmolLM2-360M, not 4.9x. The kernel remains correct and is still the right foundation for larger models and prefill offload, but the honest current value on this model is small. Do not quote the 4.9x without this correction.","created_at":"2026-07-28T12:56:51.411000Z"}]}
{"id":"bd-klf","title":"gguf_tokenizer tests fail wherever vendor/ is absent — should skip, not fail","description":"Three kopitiam-runtime tests hard-fail with Io NotFound whenever crates/kopitiam-ai/vendor/ is not present:\n\n  gguf_tokenizer::tests::builds_a_tokenizer_from_the_real_vendored_qwen2_vocab_file\n  gguf_tokenizer::tests::encode_decode_round_trips_ordinary_text\n  gguf_tokenizer::tests::a_control_token_is_matched_atomically_not_split_by_bpe\n\nThey read crates/kopitiam-ai/vendor/llama.cpp/models/ggml-vocab-qwen2.gguf via unwrap/expect at gguf_tokenizer.rs:155/169/180.\n\nCORRECTION TO AN EARLIER NOTE, worth banking because it sent someone the wrong way: docs/SESSION-STATE.md claimed this was container-only and 'present on the maintainer's box'. It is NOT. There is no vendor/ directory on the maintainer's Windows box at all. vendor/ is gitignored shallow clones, so this fails on any fresh checkout anywhere — nobody should go hunting for a tokenizer bug here.\n\nWANTED: detect the missing file and skip with a printed reason, rather than unwrap. A test that cannot run is not a test that failed, and right now three permanent reds train people to ignore the runtime suite. If the vocab is genuinely required, vendor it properly (it is a small vocab-only GGUF, no weight tensors) instead of depending on an ignored directory.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-07-27T13:18:04.318000Z","updated_at":"2026-08-24T23:55:42.605000Z","closed_at":"2026-07-28T14:08:01.015000Z","close_reason":"done","external_ref":"gh-51","labels":["testing"]}
{"id":"bd-nnk","title":"kopitiam-pdf: load_obj_stm poisons the cache with superseded objects from an old object stream","description":"gh-70. On an incrementally-updated PDF (/Prev chain, e.g. Okular annotation save), rasterizing a page before resolving objects makes later resolve() return the OLD pre-update object. Page /Annots resolves 3 entries instead of 8.\n\nRoot cause: mupdf/xref.rs load_obj_stm() caches EVERY object it parses out of an object stream (self.cache.entry(onum).or_insert(cached)) without checking the xref actually says that object number lives in THIS object stream. On an incremental update the superseded object still physically sits in the old objstm; rasterize touches some other object in that same objstm, the whole stream gets decompressed, and the stale copy lands in the cache first-write-wins. Every later resolve() of that num is served the stale copy.\n\nUpstream MuPDF guards exactly this in pdf_load_obj_stm (source/pdf/pdf-xref.c:2213): it only stores the parsed object when (entry->type == 'o' || entry->type == 'O') && entry->ofs == num -- i.e. the xref must claim that object for this very stream -- and pdf_drop_obj()s it otherwise. Our port dropped that guard.\n\nFix: port the guard, plus a regression test with an incremental update superseding an objstm object.","notes":"FIXED. Ported MuPDF's guard from pdf_load_obj_stm (pdf-xref.c:2213) as PdfDocument::claimed_by_objstm(): an object parsed out of object stream S is only cached when the xref still says XrefEntry::Compressed{stm_num: S}. Objects the newest xref section has superseded (now 'n' at a fresh offset) are skipped before parsing, so they can never reach the first-write-wins cache.\n\nRegression test mupdf::xref::tests::incremental_update_supersedes_object_stream_copy + fixture incremental_update_pdf(): original body packs objs 1/2/3 into objstm 4 (xref stream 5); appended update supersedes obj 3 uncompressed with MediaBox 400 + /Annots of 5, indexed by xref stream 6 via /Prev + /Index [3 1 6 1]. Verified the test FAILS without the guard (MediaBox 200 not 400 -- same shape as the reported 3-vs-8 /Annots) and passes with it.\n\nGates: cargo test --release -p kopitiam-pdf = 329+1+2 pass; all 8 reverse-dep crates pass; cargo clippy --release --all-targets clean for xref.rs (4 warnings all pre-existing in doc_stream/font/page_image/stream.rs). Journal: docs/engineering-journal/2026-08-25-dropped-guards-in-a-port.md","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-08-25T15:07:28.371000Z","updated_at":"2026-08-25T15:16:01.392000Z","closed_at":"2026-08-25T15:16:01.392000Z","close_reason":"done","external_ref":"gh-70","comments":[{"id":1,"issue_id":"bd-nnk","author":"root@vm","text":"FIXED. Ported MuPDF's guard from pdf_load_obj_stm (pdf-xref.c:2213) as PdfDocument::claimed_by_objstm(): an object parsed out of object stream S is only cached when the xref still says XrefEntry::Compressed{stm_num: S}. Objects the newest xref section has superseded (now 'n' at a fresh offset) are skipped before parsing, so they can never reach the first-write-wins cache.\n\nRegression test mupdf::xref::tests::incremental_update_supersedes_object_stream_copy + fixture incremental_update_pdf(): original body packs objs 1/2/3 into objstm 4 (xref stream 5); appended update supersedes obj 3 uncompressed with MediaBox 400 + /Annots of 5, indexed by xref stream 6 via /Prev + /Index [3 1 6 1]. Verified the test FAILS without the guard (MediaBox 200 not 400 -- same shape as the reported 3-vs-8 /Annots) and passes with it.\n\nGates: cargo test --release -p kopitiam-pdf = 329+1+2 pass; all 8 reverse-dep crates pass; cargo clippy --release --all-targets clean for xref.rs (4 warnings all pre-existing in doc_stream/font/page_image/stream.rs). Journal: docs/engineering-journal/2026-08-25-dropped-guards-in-a-port.md","created_at":"2026-08-25T15:15:54.921000Z"}]}
{"id":"bd-p1d","title":"kopi-beans: daemon::runtime WAL/failpoint/HLC tests are flaky in release (10-12 failures, varies per run)","description":"Split out of GH#18 (https://github.com/theodoreOnzGit/kopitiam/issues/18), where\nthe reporter noted it but deliberately did not file it separately.\n\nOn an UNMODIFIED tree, `cargo test --release -p kopi-beans` gave a release-mode\nfailure count that varied between 10, 11 and 12 across repeated runs. The\n`daemon::runtime::*` tests (WAL, failpoint, HLC) are the suspects.\n\nWhy it matters: non-deterministic failures mean \"did my change break anything?\"\ncannot be answered without running a baseline on a clean worktree first — every\nsingle time. That is the exact cost this tracker exists to remove.\n\nTwo permanent (deterministic) failures in that count are bd-ef2's tombstone\ntests; this bead owns the REST — the ones that move.\n\nFirst step is measurement, not a fix: run the suite N times on a clean tree,\nrecord which test names appear in the failing set and how often, then decide\nper-test whether it is a real race (HLC ordering, WAL fsync timing) or a test\nharness timing assumption that got tighter under release optimisation.\n","notes":"2026-08-13, from the bd-ef2 agent's baseline runs — SHARPENS this bead considerably:\n\n`daemon::runtime::core::tests::restores_hlc_state_for_non_daemon_actor` is NOT flaky.\nIt failed 3/3 on re-run in release. Deterministically red:\n\n    thread panicked at crates/kopi-beans/src/daemon/runtime/core/mod.rs:3527:9:\n    assertion failed: restored > persisted\n\nPure HLC/WAL path (`wal_index.reader().load_hlc()`, `WriteStamp` comparison), zero\ntombstone involvement, so definitely not collateral from the bd-ef2 assert_eq change.\n\nSo the ~10-12 figure from GH#18 is really TWO populations, and they need splitting:\n  (a) deterministic reds like this one — a real bug, reproducible, fix it properly;\n  (b) genuinely varying ones — the actual flakes.\nDo the N-run tally first and label each failing test (a) or (b) before touching code.\nChasing a deterministic bug as if it were a race wastes the whole session.\n\nAlso useful: `cargo test --release -p kopi-beans --lib core:: -- --skip daemon::` is\nclean at 364 passed / 0 failed, so `core::` is a trustworthy signal today. The rot is\nconfined to `daemon::runtime`.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-12T23:47:16.691000Z","updated_at":"2026-08-13T00:00:42.473000Z","external_ref":"gh-18","labels":["flaky","kopi-beans"],"comments":[{"id":1,"issue_id":"bd-p1d","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"2026-08-13, from the bd-ef2 agent's baseline runs — SHARPENS this bead considerably:\n\n`daemon::runtime::core::tests::restores_hlc_state_for_non_daemon_actor` is NOT flaky.\nIt failed 3/3 on re-run in release. Deterministically red:\n\n    thread panicked at crates/kopi-beans/src/daemon/runtime/core/mod.rs:3527:9:\n    assertion failed: restored > persisted\n\nPure HLC/WAL path (`wal_index.reader().load_hlc()`, `WriteStamp` comparison), zero\ntombstone involvement, so definitely not collateral from the bd-ef2 assert_eq change.\n\nSo the ~10-12 figure from GH#18 is really TWO populations, and they need splitting:\n  (a) deterministic reds like this one — a real bug, reproducible, fix it properly;\n  (b) genuinely varying ones — the actual flakes.\nDo the N-run tally first and label each failing test (a) or (b) before touching code.\nChasing a deterministic bug as if it were a race wastes the whole session.\n\nAlso useful: `cargo test --release -p kopi-beans --lib core:: -- --skip daemon::` is\nclean at 364 passed / 0 failed, so `core::` is a trustworthy signal today. The rot is\nconfined to `daemon::runtime`.","created_at":"2026-08-13T00:00:42.473000Z"}]}
{"id":"bd-pl2","title":"kopi-beans: clock_skew warning doesn't name which two clocks it compared, and self-clears","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/21\n\n`bn status` printed:\n\n    warnings:\n      clock_skew: 3657601 ms ahead (at 2026-08-12 04:07)\n\n~61 minutes. Then ~20 min later, with NO corrective action — no clock change, no\ndaemon restart, nothing — the warning just disappeared.\n\nReporter is NOT claiming the detection is wrong (their host had\n`System clock synchronized: no`, NTP inactive). The complaint is the message is\nnot actionable: cannot tell a `bn` bug from a host clock problem without guessing.\n\nWhat the message must say:\n  1. WHICH TWO CLOCKS. \"ahead\" of what? Local system clock vs a store timestamp?\n     vs another actor's clock in the store? vs the git remote? Right now names\n     neither side.\n  2. DIRECTION, unambiguously. \"3657601 ms ahead\" doesn't say whether local is\n     ahead of the reference or the reference is ahead of local. Opposite fixes.\n  3. WHAT IT AFFECTS. Does skew this size threaten issue ordering, dedup, or\n     last-write-wins? Or purely informational?\n  4. WHY IT CAN CLEAR ITSELF. Appears and vanishes with no user action = reads\n     like a measurement artefact. If it IS transient (stale timestamp aging out\n     of the comparison window), say so, so nobody chases a clock problem that\n     isn't there.\n\nReporter's suggested shape:\n    clock_skew: local system clock is 61m 0s behind actor <id>'s last write\n    (2026-08-12 04:07); may affect ordering\n\nAlso worth a doc line on the self-clearing window, wherever the warning is\ncomputed.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-08-12T23:47:16.720000Z","updated_at":"2026-08-13T00:20:32.097000Z","closed_at":"2026-08-13T00:20:32.097000Z","close_reason":"done","external_ref":"gh-21","labels":["kopi-beans","ux"]}
{"id":"bd-ski","title":"pdf2md: CJK / annotated-PDF field report — the validation gate cannot fail on this input class","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/2\n\nEPIC. Tracking bead for the CJK / annotated-PDF field report (2026-08-01). The\nheadline: our pdf2md validation gate CANNOT fail on this input class — it passes\ngarbage. Children F-0..F-10 are the individual findings. Source docs live in\ndocs/issues/field-report-2026-08-01/ and docs/agent-field-report-2026-08-01-cjk-pdf.md.\n\n--- original GH body, verbatim ---\n\n\nField report from using `kopitiam pdf2md` on five Cambridge IGCSE Japanese (0716) specimen papers — born-digital, furigana (ruby) annotated, some subset-embedded fonts.\n\n**All five reported `recovery_ratio: 1.0`, `passes: true`, `Status: PASS` while the Japanese body text was materially corrupted.**\n\nFull write-up: `docs/agent-field-report-2026-08-01-cjk-pdf.md`.\n\n## §0 — Corrections to the first draft\n\nThe first draft of this report was written from output alone, before reading source. Three claims were **wrong**. Recorded because §2.1 of the work order warns about exactly this failure mode, and because it is why every card carries a confidence level.\n\n| First-draft claim | Reality | Evidence |\n|---|---|---|\n| \"Ratio is word-count based; CJK has no whitespace so it degenerates\" | **Already fixed.** Non-whitespace *character* counts, with a doc comment explaining tokenization is a PDF artifact | `crates/kopitiam-document/src/validation/mod.rs:189`, doc comment `:193-205`, refs `kopitiam-wwr` |\n| \"Per-page ratios not computable\" | **Already shipped** | `validation/mod.rs:113` `per_page_recovery`, `:177` `parse_page_anchor` |\n| \"The `���` runs are undecodable *image* bytes\" | **Font CID→Unicode mapping failure.** U+FFFD is the deliberate graceful fallback | `crates/kopitiam-pdf/src/mupdf/font.rs:554`, `agl.rs:40` |\n\n## §1 — The finding\n\nThis is **not** the §2.1 recovery-ratio trap, and **not** the word-count problem solved by `kopitiam-wwr`. Recovery is a **conservation** check: did content survive extraction → reconstruction → render. Every defect found is **additive or substitutive**:\n\n- **Duplication** (#F-3) inflates extracted *and* rendered sides equally — structurally invisible to a conservation check.\n- **U+FFFD substitution** (#F-1) replaces one character with one character. Count preserved on both sides.\n- **Ruby displacement** (#F-4) moves text within the document. Nothing lost, nothing measured.\n\nThe ratio behaves exactly as designed. The gap is that **nothing else is checked**, so `PASS` reads as \"output is good\" when it only means \"nothing vanished.\"\n\n**A gate that cannot fail on an input class is worse than no gate, because it is trusted.** I took it at face value and consumed all five documents; the corruption was found later by reading the Japanese.\n\n## §2.4 — Shared principle for validation cards\n\n**Conservation checks cannot detect additive corruption.** Any new validation signal must be an **absolute** property of the output — a count, a density, a rate — not a comparison between two sides of the pipeline.\n\n## §2.1 — No copyrighted fixtures\n\nThe Cambridge PDFs are free to download but not ours to commit. Every card specifies a **synthetic** fixture built from `TextSpan` primitives. Reproduce locally against the real corpus; commit only synthetic.\n\n## §2.3 — Re-verify line numbers\n\nAll coordinates read 2026-08-01 via `kopitiam outline` / `slice` / `grep`. Orientation, not gospel.\n\n## Dispatch\n\n| Wave | Cards | Parallel? |\n|---|---|---|\n| 0 | F-0 survey | single (blocking) |\n| 1 | F-6 skill doc | single — docs only, land immediately |\n| 2 | F-1, F-3, F-5 | parallel (disjoint files) |\n| 3 | F-2, F-4 | after wave 2 (shared files) |\n| 4 | F-7, F-8, F-9 | one agent (all touch `main.rs`) |\n| 5 | F-10 | after F-7 |\n\n**Contended files:** `mupdf_extract.rs` (F-3, F-4) · `validation/mod.rs` (F-1, F-2) · `main.rs` (F-7, F-8, F-9)\n\n## Reproduction\n\n```bash\nfor f in *.pdf; do kopitiam pdf2md \"$f\" -o \"markdown/${f%.pdf}.md\" --index; done\ngrep -c \"会 会話 話\" markdown/*.md                    # F-3\ngrep -nE \"^#{1,6} [^[:alnum:]]*$\" markdown/*.md       # F-5\nkopitiam pdf2md x.pdf -o /tmp/x.md --report-json | jq '.recovery_ratio, .passes'  # F-1\n```\n","status":"open","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:47:43.091000Z","updated_at":"2026-08-12T23:47:43.091000Z","external_ref":"gh-2","labels":["epic","pdf2md"]}
{"id":"bd-ski.1","title":"[pdf2md] F-0 — Survey before wave 2 (blocking)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/3\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** n/a. **Owns:** nothing; read-only.\n\nSix questions blocking F-3, F-4 and F-5. Written by an agent that has read ~200 lines of this codebase — you can likely answer most from memory, in which case answer them inline on those cards and close this. Answering them moves F-3/F-4/F-5 from Medium to High confidence and saves an agent rediscovering it mid-task.\n\n- [ ] **1.** Where exactly are `TextSpan`s constructed on the mupdf path? (`crates/kopitiam-pdf/src/mupdf_extract.rs:154` is the candidate.) Does `extractor.rs:315` on the legacy path need the same fix?\n- [ ] **2.** Is glyph **origin** (x, y) available at that point, or only a bounding box? F-3 needs per-glyph origin to dedup positionally.\n- [ ] **3.** Is `font_size` available per span, and is a page-level median/modal body size computed anywhere? F-4 needs a body-size baseline to identify ruby.\n- [ ] **4.** At `reconstruction/mod.rs:289` (`blocks.push(Block::Heading(...))`), what decides heading-ness and what sets `level`? F-5 needs the predicate.\n- [ ] **5.** Does `ConversionReport` have anywhere to carry non-fatal warnings, or does F-1 need to add the field?\n- [ ] **6.** Does anything already collapse runs of identical adjacent glyphs? (grep for `dedup` found nothing in `kopitiam-pdf` / `kopitiam-document`.)\n\n---\n\nTracking: #2\n\n","status":"open","priority":1,"issue_type":"task","created_at":"2026-08-12T23:51:00.531000Z","updated_at":"2026-08-12T23:51:00.531000Z","external_ref":"gh-3","labels":["field-report-2026-08-01","question"],"dependencies":[{"issue_id":"bd-ski.1","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.10","title":"[cli] F-9 — tokens --tree is dominated by vendored trees (90% of output was vendor/)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/12\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** High (reproduced 2026-08-01). **Owns:** `apps/cli/src/main.rs`\n\n## Problem\n\nOrienting in this repo, `kopitiam tokens crates/ --tree` reported:\n\n```\ncrates/  74135961 tokens (22673 files)\n  kopitiam-ai/  66959903 tokens (20830 files)\n    vendor/  66903180 tokens (20813 files)\n      OpenFOAM-dev/  40633875 tokens (17566 files)\n        tutorials/ ...\n```\n\n**90% of the reported total is `kopitiam-ai/vendor/OpenFOAM-dev`**, mostly CFD mesh geometry. The first 25 lines of output were entirely vendored tutorials.\n\nThe tool built to make budgeting cheap gave an answer dominated by files no agent will ever read, and I had to re-run with explicit crate paths.\n\n## Change\n\nDefault-exclude `vendor/`, `target/`, `node_modules/`, `.git/`; add `--include-vendor` to restore.\n\n**Preferred:** report them as a single collapsed line rather than omitting silently —\n\n```\nvendor/  66959903 tokens (20813 files) [excluded]\n```\n\n— which is honest and equally cheap.\n\n## Acceptance\n\n- [ ] `tokens crates/ --tree` surfaces the real source tree within the first screen.\n- [ ] Vendored totals shown as one collapsed line, not expanded.\n- [ ] `--include-vendor` restores current behaviour.\n\n---\n\nTracking: #2\n\n","status":"open","priority":2,"issue_type":"feature","created_at":"2026-08-12T23:51:10.867000Z","updated_at":"2026-08-12T23:51:10.867000Z","external_ref":"gh-12","labels":["enhancement","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.10","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.11","title":"[cli] F-10 — Extend outline/slice to prose documents (the token-max loop has no document half)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/13\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** Low (design), but **highest leverage on the stated goal of spreading deterministic tools**. **Owns:** `apps/cli/src/main.rs`, shared with the `--index` writer. **After F-7.**\n\n## Problem\n\nThe token-max loop (`tokens → outline → refs → slice`) is Rust/rust-analyzer-only. Follow-up P6 plans Python — more **languages**, still the **code** half.\n\nThe gap hit this session was different: **the same loop applied to converted Markdown.** After `pdf2md` there is no `outline` for the result. I fell back to reading whole documents — the exact behaviour the loop exists to prevent.\n\n## Change\n\nMake `kopitiam outline <file.md>` return the heading skeleton with line ranges. **The `--index` sidecar already computes exactly this** — it is simply not exposed through the same verb. Confirm `slice` works on non-Rust files (it appeared to this session) and document it.\n\n## Why this over more languages\n\nThe product has two halves — documents and code. The deterministic navigation loop exists only for code. Unifying `outline` across both makes the document half navigable **with no new analysis machinery**, because the data structure is already being written to disk.\n\nAdding Python extends the half that already works. This fixes the half that has nothing.\n\n## Acceptance\n\n- [ ] `kopitiam outline paper.md` returns headings + line ranges matching `paper.md.index.json`.\n- [ ] `kopitiam tokens paper.md && kopitiam outline paper.md && kopitiam slice paper.md 40-80` works end to end on a converted PDF.\n\n---\n\nTracking: #2\n\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-08-12T23:51:12.594000Z","updated_at":"2026-08-12T23:51:12.594000Z","external_ref":"gh-13","labels":["enhancement","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.11","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.2","title":"[pdf2md] F-1 — Validation: detect substitutive corruption (U+FFFD density)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/4\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** High. **Owns:** `crates/kopitiam-document/src/validation/{mod.rs,report.rs}`\n\n## Problem\n\n`729152` emits runs of `���` and scores `recovery_ratio: 1.0`, `passes: true`.\n\nU+FFFD is a legitimate graceful fallback at the font layer (`crates/kopitiam-pdf/src/mupdf/font.rs:554`) — a CID→Unicode lookup miss, not a crash. But by the time a document is **rendered**, a page dense in replacement characters is a failed page, and nothing says so.\n\n## Change\n\nCompute replacement-character density per page (count of U+FFFD ÷ content chars). Surface in `ConversionReport` per page and document-wide.\n\nGate: any nonzero count emits a warning; above a threshold (suggest 1% of a page's content chars, tunable) the page fails and the document does not `PASS`.\n\nPer §2.4 this must be an **absolute** property of the rendered side — do **not** implement as an extracted-vs-rendered comparison, which cancels out.\n\n## Acceptance\n\n- [ ] Synthetic fixture: a `Page` of spans where 5% of characters are `\\u{FFFD}` → document does not pass; report names the page.\n- [ ] Negative fixture: clean page with a single legitimate `\\u{FFFD}` in body text → warns, does not fail.\n- [ ] Existing tests in `validation/mod.rs` (`dropped_content_fails`, `repaired_hyphenation_still_passes`, …) pass unchanged.\n- [ ] Real corpus: `729152` no longer reports `PASS`.\n\n---\n\nTracking: #2\n\n","status":"open","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:51:01.739000Z","updated_at":"2026-08-12T23:51:01.739000Z","external_ref":"gh-4","labels":["bug","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.2","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.3","title":"[pdf2md] F-2 — Validation: detect additive corruption (duplication rate)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/5\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** High (defect), Medium (statistic choice). **Owns:** `crates/kopitiam-document/src/validation/{mod.rs,report.rs}`\n\n**Depends on F-1** (shared files).\n\n## Problem\n\nThe glyph doubling in F-3 is invisible to recovery — both sides inflate equally. Per §2.4, an absolute statistic is needed.\n\n## Change\n\nAdd a cheap, language-agnostic statistic over the **rendered** side: rate of adjacent repeated tokens. Two candidates — pick one and justify:\n\n- **Repeated-bigram rate** — fraction of positions where `text[i..i+2] == text[i+2..i+4]`. Catches `会話会話` and, space-tolerant, `会 会話 話`.\n- **Repeated-word rate** — fraction of whitespace-delimited tokens equal to their predecessor. Catches `スーパーで スーパーで`, **misses** the interleaved CJK form.\n\nThe bigram form is more general; the observed interleaving (`会 会話 話`, not `会話 会話`) is exactly what makes the word-level check insufficient.\n\n**Warn, do not fail.** Legitimate repetition exists (`々`, `いろいろ`, tables of identical cells). This is a signal to look, not a gate.\n\n## Acceptance\n\n- [ ] Synthetic fixture reproducing `会 会話 話 1: 先生が 会話を はじめます。` → warning raised, naming the page.\n- [ ] Negative fixture with legitimate repetition (`いろいろな人が来ました。`, a 3-row table with a repeated cell value) → no warning.\n- [ ] Real corpus: `730209` (18 occurrences) warns; a clean Latin PDF does not.\n\n---\n\nTracking: #2\n\n","status":"open","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:51:02.788000Z","updated_at":"2026-08-12T23:51:02.788000Z","external_ref":"gh-5","labels":["bug","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.3","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.4","title":"[pdf2md] F-3 — Extraction: positionally dedup double-struck glyphs (affects ALL bold text)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/6\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** High (defect), Medium (fix site — see F-0 Q1/Q2). **Owns:** `crates/kopitiam-pdf/src/mupdf_extract.rs` (+ possibly `extractor.rs`)\n\n## Problem\n\nPDFs render bold by stroking the same glyph twice at a small offset. Both copies are emitted. Measured across the corpus:\n\n| Output string | Count | File |\n|---|---|---|\n| `会 会話 話` | 18 | `730209` |\n| `会 会話 話` | 2 | `729150` |\n| `日本 本語 語` | 3 | `729154` |\n| `前 半 半` | 1 | `729150` |\n| `スーパーで スーパーで` | 1 | `729152` |\n| `二つ 二つ` | 1 | `729152` |\n\n**This is not CJK-specific.** It affects every bold run in every PDF — Latin merely degrades more visibly (`bboolldd`), so it was likely noticed and dismissed as cosmetic. In CJK it produces plausible-but-wrong strings and **defeats grep on any phrase longer than the doubled unit**.\n\n## Change\n\nAt span construction, drop a glyph whose codepoint equals the previous glyph's **and** whose origin is within ε of it, where ε is a small fraction of font size (start at `0.15 * font_size`; a named constant with a comment, not a magic number). Purely positional — no language knowledge required.\n\n**Precision over recall.** Two genuinely adjacent identical characters (`ここ`, `々`, `1000`) sit a full advance width apart, far outside ε. If per-glyph origin is unavailable (F-0 Q2), say so and stop rather than approximating with bounding boxes.\n\n## Acceptance\n\n- [ ] Synthetic fixture: spans for `会話` where each glyph is duplicated at `+0.05 * font_size` → extraction yields `会話`, not `会 会話 話`.\n- [ ] Negative fixture: `ここ` (two identical glyphs a full advance apart) → both retained.\n- [ ] Negative fixture: Latin `bold` double-struck → `bold`.\n- [ ] Real corpus: all six strings above disappear.\n- [ ] **Ratio honesty (§2.1):** removing duplicates legitimately reduces the extracted side, so the ratio *will* move. State the new value and why it is correct.\n\n---\n\nTracking: #2\n\n","status":"open","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:51:03.885000Z","updated_at":"2026-08-12T23:51:03.885000Z","external_ref":"gh-6","labels":["bug","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.4","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.5","title":"[pdf2md] F-4 — Extraction: identify ruby / annotation runs (furigana is hoisted out of position)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/7\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** High (defect), Low (fix design — needs F-0 Q3). **Owns:** `crates/kopitiam-pdf/src/mupdf_extract.rs` + a new reconstruction module. **After F-3.**\n\n## Problem\n\nFurigana — small kana above kanji giving the reading — are real text objects at reduced size on a raised baseline. They are treated as body text and emitted **before** the run they annotate:\n\n```\nぶんしょう つぎの 文  章 を 読みなさい。\n```\n\n`ぶんしょう` is the reading *of* `文章`; it now sits at the head of the sentence as a stray word, and `文章` has acquired internal spaces. Further examples from the corpus:\n\n```\nはくぶつかん 町には、にんじゃの 博 物 館もあります。\nい が 伊 伊賀 賀市へ ようこそ 市へ ようこそ        ← ruby + F-3 doubling together\nねんれい せいと                                      ← a line that is ENTIRELY ruby\n```\n\n**Impact is worst precisely where this tool is most useful.** For a language-learning corpus the ruby is the highest-value content, and it has been detached from what it annotates.\n\n## Change\n\nDetect an annotation run: font size below ~60% of the page's body size (F-0 Q3), baseline raised relative to a larger run, horizontal overlap with that run.\n\nExpose `--ruby=drop|inline|html`:\n\n- `drop` (**default**) — omit. Already a large improvement over emitting inline.\n- `inline` — `文章（ぶんしょう）`\n- `html` — `<ruby>文章<rt>ぶんしょう</rt></ruby>`\n\n**Worth generalising in the work order:** *small text on a raised baseline that horizontally overlaps a larger run is an annotation, not a paragraph.* The same predicate covers superscript footnote markers, which currently become stray digits in body text.\n\n## Acceptance\n\n- [ ] Synthetic fixture: body spans `文章` at 12pt plus spans `ぶんしょう` at 6pt, raised, horizontally overlapping → `drop` yields `文章`; `inline` yields `文章（ぶんしょう）`.\n- [ ] Negative fixture: a genuine small-print footnote **below** body text with no horizontal overlap → retained as a paragraph.\n- [ ] **Ratio honesty (§2.1):** `drop` removes real extracted content. Either exclude classified ruby from the extracted side, or document why the new number is correct.\n- [ ] Real corpus: the standalone `ねんれい せいと` line disappears; `文章` is contiguous.\n\n---\n\nTracking: #2\n\n","status":"open","priority":2,"issue_type":"bug","created_at":"2026-08-12T23:51:04.959000Z","updated_at":"2026-08-12T23:51:04.959000Z","external_ref":"gh-7","labels":["bug","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.5","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.6","title":"[pdf2md] F-5 — Undecodable text is promoted to Markdown headings, poisoning --index","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/8\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** High. **Owns:** `crates/kopitiam-document/src/reconstruction/mod.rs` (heading push at `:289`)\n\n## Problem\n\n`729152` page 3 produced **seven headings at six different levels**:\n\n```markdown\n### ���\n#### ����\n## ��\n# ��\n#### ���\n###### ���\n```\n\n**This poisons `--index`,** which keys on headings. The sidecar contains:\n\n```json\n{\"text\": \"��\", \"level\": 1, \"line_start\": 19, \"line_end\": 59}\n```\n\nA level-1 heading with no retrievable name claiming lines 19–59 — **two thirds of a 59-line document**. The one deterministic navigation aid is unusable on this file, so I read whole documents instead. Directly counter to the token-max goal.\n\nIt fails **silently**: `--index` still writes a well-formed sidecar.\n\nDistinct from Task I-D (collapse figure regions to captions) — the problem here is that undecodable content was classified as a heading at all.\n\n## Change\n\nInvariant at the heading-construction site: **a candidate heading whose text is empty after stripping U+FFFD, control characters, whitespace and punctuation is not a heading.** Demote to paragraph, or drop if also empty as a paragraph.\n\nCheap, independent of every other card, worth asserting regardless of what upstream produces. Consider the same invariant on the `--index` writer as defence in depth.\n\n## Acceptance\n\n- [ ] Synthetic fixture: a large-font span of `\\u{FFFD}\\u{FFFD}\\u{FFFD}` → no `Block::Heading` emitted.\n- [ ] Negative fixture: a legitimate heading containing one `\\u{FFFD}` among real text → still a heading, text preserved.\n- [ ] Real corpus: `729152.md.index.json` contains no heading whose text is empty after stripping; no heading spans two thirds of the document.\n\n---\n\nTracking: #2\n\n","status":"open","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:51:06.106000Z","updated_at":"2026-08-12T23:51:06.106000Z","external_ref":"gh-8","labels":["bug","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.6","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.7","title":"[docs] F-6 — kopitiam_skill.md overstates what a PASS report means","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/9\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** High. **Owns:** `kopitiam_skill.md`. **Land first — docs only, no code.**\n\n## Problem\n\nThe skill doc instructs agents:\n\n> Read the validation / recovery report: `pdf2md` prints a report comparing the extracted word count against the rendered Markdown word count, ending in a PASS/FAIL line.\n\nTwo problems:\n\n1. It says **word** count — stale since `kopitiam-wwr`; it is non-whitespace character count now (`validation/mod.rs:189`).\n2. It implies PASS means the output is good.\n\n**Agents follow this literally.** I did: I read `Status: PASS` on five documents, trusted it, and used corrupted output downstream.\n\n## Change\n\nState what PASS does and does not mean:\n\n> **PASS means no content was lost. It does not mean the output is correct.**\n\nName the classes it cannot see — duplication, character substitution, displacement (see tracking issue §1) — and advise a visual check for CJK or annotation-heavy documents. Fix \"word count\" → \"non-whitespace character count\". Once F-1/F-2 land, add the new warnings to the same paragraph.\n\n## Acceptance\n\n- [ ] An agent reading only the skill doc would not conclude PASS means the output is trustworthy.\n\nCheapest fix on the list. Do it first — it stops the next agent repeating my mistake while the code cards are still in survey.\n\n---\n\nTracking: #2\n\n","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-12T23:51:07.230000Z","updated_at":"2026-08-12T23:51:07.230000Z","external_ref":"gh-9","labels":["documentation","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.7","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.8","title":"[pdf2md] F-7 — Add --headings-only structure preview","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/10\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** Low (design). **Owns:** `apps/cli/src/main.rs`\n\n## Problem\n\nFacing five unfamiliar PDFs, there was no way to learn their shape without converting all five in full and reading them. `--pages` (shipped) helps once you know where to look; nothing helps you **find out where to look**.\n\n## Change\n\nEmit only the heading tree with page and line ranges — the `--index` content, without the body. Should cost a few dozen tokens against a document whose full conversion runs to thousands.\n\n## Acceptance\n\n- [ ] On a multi-heading fixture: output contains every heading, no body paragraph, page numbers matching `--index`.\n\n**Depends on F-5**, or it faithfully reproduces garbage headings.\n\n---\n\nTracking: #2\n\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-08-12T23:51:08.154000Z","updated_at":"2026-08-12T23:51:08.154000Z","external_ref":"gh-10","labels":["enhancement","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.8","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-ski.9","title":"[pdf2md] F-8 — Cross-document boilerplate stripping for batch conversion","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/11\nPart of the CJK / annotated-PDF field report (2026-08-01), epic bd-ski.\nOriginal write-up: docs/issues/field-report-2026-08-01/\n\n--- original GH body, verbatim ---\n\n\n> Part of the **pdf2md CJK/annotated-PDF** work order — see the tracking issue for shared context, the corrections in §0, and the additive-corruption principle these cards rest on.\n\n**Confidence:** Low (design). **Owns:** `apps/cli/src/main.rs` + new module\n\n## Problem\n\nThe five papers share ~200 words of identical front matter (instructions, \"Dictionaries are not allowed\", the copyright block) plus an identical per-page footer — roughly **1,000 duplicated words across the batch, paid for five times**.\n\nTask I-C strips *intra*-document running heads by signature recurrence across pages. The natural extension is *inter*-document.\n\n## Change\n\nAccept multiple inputs in one invocation. Shingle-hash paragraphs, drop those appearing verbatim in a majority of documents, write them once to a shared `boilerplate.md` alongside the outputs.\n\n```bash\nkopitiam pdf2md *.pdf --out-dir markdown/ --strip-shared-boilerplate\n```\n\nSecondary benefit: makes **batch conversion first-class**. Today the skill doc's recipe is a shell `for` loop, so nothing can reason across the set.\n\n## Acceptance\n\n- [ ] Three synthetic PDFs sharing a header paragraph → it appears in `boilerplate.md` and in none of the three outputs.\n- [ ] A paragraph present in only one document is never stripped.\n\n---\n\nTracking: #2\n\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-08-12T23:51:09.206000Z","updated_at":"2026-08-12T23:51:09.206000Z","external_ref":"gh-11","labels":["enhancement","field-report-2026-08-01"],"dependencies":[{"issue_id":"bd-ski.9","depends_on_id":"bd-ski","type":"parent-child","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}]}
{"id":"bd-u7j","title":"kopi-beans: every daemon git push pops a console window on Windows (regression from 0.1.3)","description":"Maintainer, 2026-08-13: \"the popup windows on windows is super annoying.\"\n\nREGRESSION, introduced by 0.1.3 (GH#19, commits f06e642 / 653c22f). Chain is:\n\n1. The daemon is spawned with `DETACHED_PROCESS | CREATE_NO_WINDOW`\n   (surface/ipc/spawn_sanitizer.rs:163). Correct — but it means the daemon\n   process HAS NO CONSOLE.\n2. 0.1.3 made network push shell out to the system git binary:\n   `subprocess_push` at git/gix_compat.rs:910 does a plain\n   `std::process::Command::new(\"git\")` with NO creation flags.\n3. On Windows, a console-subsystem child spawned from a parent that has NO\n   console gets a BRAND NEW CONSOLE WINDOW allocated for it.\n4. The daemon debounces and AUTO-SYNCS. So: one popup window per sync, forever.\n\nBefore 0.1.3 push failed fast in-process and never shelled out, so no popups.\nWe fixed the push and gave the user a strobe light. Paiseh.\n\nEvery `std::process::Command` in the crate needs `CREATE_NO_WINDOW` on Windows,\nbecause ANY of them can run under the daemon:\n  * git/gix_compat.rs:910          — `git push`  (THE hot one, fires every sync)\n  * bootstrap/repo.rs:91           — `jj` detection\n  * cli_surface/commands/show.rs:732 — `jj`\n\n`CREATE_NO_WINDOW` (0x08000000) is exactly right here: all three capture\nstdout/stderr via `.output()`, so no console is wanted at all. Note this is\nDIFFERENT from `DETACHED_PROCESS` — detached is for the long-lived daemon; for a\nshort-lived child whose output we capture, plain `CREATE_NO_WINDOW` is correct.\nDo NOT combine them (`DETACHED_PROCESS | CREATE_NO_WINDOW` on a child we then\n`.output()` is not what we want).\n\nImplementation: one shared `pub(crate)` helper that takes `&mut Command` and, on\nWindows only, sets `.creation_flags(CREATE_NO_WINDOW)` via\n`std::os::windows::process::CommandExt`; a no-op on unix. Apply at all three\nsites. Do not hand-roll `CreateProcessW` — `.creation_flags` is enough for\n`.output()` children, unlike the daemon spawn which needs the full call.\n\nVERIFICATION PROBLEM, be honest about it: the fix lives entirely in\n`#[cfg(windows)]` code, which NEVER compiles on Linux, so a typo ships broken.\n`rustup target add x86_64-pc-windows-gnu` + `cargo check --target\nx86_64-pc-windows-gnu -p kopi-beans` at minimum proves it compiles. Whether the\npopups are actually GONE can only be confirmed on the maintainer's Windows box —\nthat part is human dogfooding, do not claim it verified.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-08-13T08:23:01.868000Z","updated_at":"2026-08-13T08:38:20.455000Z","closed_at":"2026-08-13T08:38:20.455000Z","close_reason":"done","external_ref":"gh-29","labels":["kopi-beans","windows"]}
{"id":"bd-uij","title":"kopitiam tui: auto-download the local model on startup","description":"Maintainer wants the TUI to auto-download the Gemma model when it starts (instead of falling back to Echo when no .gguf is present).\n\nBLOCKERS / prerequisites (why this can't just be switched on):\n1. No concrete Gemma catalog entry. kopitiam-models has Family::Gemma enum only; no ModelSpec with repo/commit/filename. Every catalog sha256 is the placeholder 0x64, so ensure_available's checksum gate REJECTS any download until a real sha256 (+ pinned HF commit) is recorded. Need real gemma-4-E2B GGUF coordinates (HF repo, commit SHA, filename, sha256, size).\n2. Loader can't run gemma-4 arch yet (see bd-250): hybrid local/global attention + Per-Layer Embeddings not in the dense Qwen2/Llama loader. Downloaded gemma-4 would fail LocalAdapter::load -> Echo. So auto-download of gemma-4 is pointless until loader support lands.\n3. Design principle: apps/cli/src/adapter.rs deliberately does NOT autofetch ('rude surprise sia'). Auto-download-on-start must be consented (first-run prompt) + show progress in the TUI, not silent. kopitiam-models::ensure_available + hf.rs give the fetch+verify+progress plumbing to build on.\n\nRECOMMENDED SEQUENCING:\n- Build the TUI auto-download mechanism model-agnostically (consent screen + progress bar over ensure_available), pointed at a runnable Apache-2.0 model that WORKS on today's loader as the shipping default (MiniCPM5-1B, Llama arch, GGUF available) — needs its real catalog entry too.\n- Land gemma-4 loader support (bd-250), add the real gemma-4-E2B catalog entry, then flip the TUI default to gemma-4.\nDepends on bd-250.","notes":"LOADER REALITY (from research + code read, applies to any download target):\n1. QUANT GATE: kopitiam-loader/src/gguf.rs:241-252 dtype_from_ggml_type only maps F32,F16,Q4_0,Q4_1,Q5_0,Q5_1,Q8_0,BF16. K-quants (Q4_K_M/Q6_K) HARD-FAIL at parse. Most modern small GGUFs are K-quants. Bridge confirms: kopitiam-runtime/src/bridge.rs:43 handles only Q4_0|Q4_1|Q5_0|Q5_1|Q8_0.\n2. TOKENIZER GATE: kopitiam-runtime/src/gguf_tokenizer.rs expects tokenizer.ggml.model=='gpt2' (byte-level BPE + merges). SentencePiece/llama tokenizers error.\n=> Effectively only Qwen-style classic-quant GGUFs actually load+chat today.\n\ngemma-4-E2B-it (https://huggingface.co/google/gemma-4-E2B-it): Apache-2.0 CONFIRMED, but ships model.safetensors ONLY (NO GGUF), ~5B params (multi-GB). Not llama-shaped, SentencePiece tokenizer => cannot load on current runtime. Downloading it = large pull -> Echo. Do NOT wire the downloader to it yet.\n\nRunnable-today stand-in that stays non-Qwen: MiniCPM5-1B-Q8_0.gguf (openbmb/MiniCPM5-1B-GGUF, Apache-2.0) is a supported quant (Q8_0) — BUT verify its tokenizer.ggml.model=='gpt2' first (MiniCPM base is SPM; if so it fails the tokenizer gate too). Q4_K_M variant is unsupported (K-quant).\n\nONE ADDITION from a 2026-07-25 code read (bd-250.4 was filed covering this ground and is closed as a duplicate of this bead; everything else in it — placeholder sha256 gate, need for real HF coordinates, the Gemma licence split — is already captured here or in bd-250's comments):\n\n`KOPITIAM_MODEL=gemma-...` DOES NOT ERROR — IT SILENTLY GIVES YOU QWEN. apps/cli/src/adapter.rs's select_adapter does:\n\n    let spec = Catalog::find(&model_id).unwrap_or_else(|| Catalog::find(DEFAULT_MODEL_ID).expect(...));\n\nSo an unknown/unimplemented catalog id falls back to the default spec with NO message. `Architecture::Gemma` exists as a bare enum variant (catalog.rs:34-35) with no ModelSpec behind it, so a user who sets KOPITIAM_MODEL to any Gemma id today gets Qwen2.5-0.5B and is told nothing — SelectedAdapter::notice() reports 'Using local model on CPU: <qwen path>', which is truthful but does not mention that the model they ASKED for was silently discarded.\n\nSmall standalone fix, independent of everything else in this bead: an unknown model id should be a clear error (or at minimum a loud note naming both the requested id and the substituted one). The fallback itself is defensible — it keeps the offline path working — but it must be visible. Worth doing NOW, before any auto-download work, because it is the difference between 'Gemma isn't supported yet' and 'Gemma appeared to work'.","status":"open","priority":1,"issue_type":"feature","created_at":"2026-07-24T22:48:47.679000Z","updated_at":"2026-08-24T23:55:42.448000Z","external_ref":"gh-33","labels":["kopitiam-ai","models","tui"],"comments":[{"id":1,"issue_id":"bd-uij","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"LOADER REALITY (from research + code read, applies to any download target):\n1. QUANT GATE: kopitiam-loader/src/gguf.rs:241-252 dtype_from_ggml_type only maps F32,F16,Q4_0,Q4_1,Q5_0,Q5_1,Q8_0,BF16. K-quants (Q4_K_M/Q6_K) HARD-FAIL at parse. Most modern small GGUFs are K-quants. Bridge confirms: kopitiam-runtime/src/bridge.rs:43 handles only Q4_0|Q4_1|Q5_0|Q5_1|Q8_0.\n2. TOKENIZER GATE: kopitiam-runtime/src/gguf_tokenizer.rs expects tokenizer.ggml.model=='gpt2' (byte-level BPE + merges). SentencePiece/llama tokenizers error.\n=> Effectively only Qwen-style classic-quant GGUFs actually load+chat today.\n\ngemma-4-E2B-it (https://huggingface.co/google/gemma-4-E2B-it): Apache-2.0 CONFIRMED, but ships model.safetensors ONLY (NO GGUF), ~5B params (multi-GB). Not llama-shaped, SentencePiece tokenizer => cannot load on current runtime. Downloading it = large pull -> Echo. Do NOT wire the downloader to it yet.\n\nRunnable-today stand-in that stays non-Qwen: MiniCPM5-1B-Q8_0.gguf (openbmb/MiniCPM5-1B-GGUF, Apache-2.0) is a supported quant (Q8_0) — BUT verify its tokenizer.ggml.model=='gpt2' first (MiniCPM base is SPM; if so it fails the tokenizer gate too). Q4_K_M variant is unsupported (K-quant).","created_at":"2026-07-24T22:56:47.199000Z"},{"id":2,"issue_id":"bd-uij","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"ONE ADDITION from a 2026-07-25 code read (bd-250.4 was filed covering this ground and is closed as a duplicate of this bead; everything else in it — placeholder sha256 gate, need for real HF coordinates, the Gemma licence split — is already captured here or in bd-250's comments):\n\n`KOPITIAM_MODEL=gemma-...` DOES NOT ERROR — IT SILENTLY GIVES YOU QWEN. apps/cli/src/adapter.rs's select_adapter does:\n\n    let spec = Catalog::find(&model_id).unwrap_or_else(|| Catalog::find(DEFAULT_MODEL_ID).expect(...));\n\nSo an unknown/unimplemented catalog id falls back to the default spec with NO message. `Architecture::Gemma` exists as a bare enum variant (catalog.rs:34-35) with no ModelSpec behind it, so a user who sets KOPITIAM_MODEL to any Gemma id today gets Qwen2.5-0.5B and is told nothing — SelectedAdapter::notice() reports 'Using local model on CPU: <qwen path>', which is truthful but does not mention that the model they ASKED for was silently discarded.\n\nSmall standalone fix, independent of everything else in this bead: an unknown model id should be a clear error (or at minimum a loud note naming both the requested id and the substituted one). The fallback itself is defensible — it keeps the offline path working — but it must be visible. Worth doing NOW, before any auto-download work, because it is the difference between 'Gemma isn't supported yet' and 'Gemma appeared to work'.","created_at":"2026-07-25T00:02:01.677000Z"}]}
{"id":"bd-v3x","title":"kopitiam-runtime: Gemma architecture + SentencePiece tokenizer (make Gemma chat)","description":"Goal: a working Gemma demo in the kopitiam TUI. Requires REAL new runtime work (not a config branch). Sequenced AFTER the K-quant agent releases kopitiam-runtime (one-directory-one-owner).\n\nTHREE pieces:\n1. Gemma architecture in kopitiam-runtime forward pass. Differs from the current llama/qwen2 dense path: input embedding scaled by sqrt(hidden_size); GeGLU (gelu-gated) MLP not SwiGLU; RMSNorm uses (1 + weight); tied input/output embeddings; per-layer alternating local(sliding-window)/global attention (gemma-2/3/4); attention logit soft-capping (gemma-2). Confirm gemma-4-E2B specifics from config.json (https://huggingface.co/google/gemma-4-E2B-it). Reference: vendored llama.cpp ggml + llm_build_gemma (crates/kopitiam-ai/vendor/llama.cpp), MIT, attribute at point of use.\n2. SentencePiece tokenizer support. Runtime today only builds a gpt2 byte-BPE tokenizer (kopitiam-runtime/src/gguf_tokenizer.rs expects tokenizer.ggml.model=='gpt2'). Gemma uses SPM (tokenizer.model / tokenizer.json). Need an SPM path (pure-Rust; e.g. parse tokenizer.json unigram/spm, or the GGUF llama/spm vocab).\n3. Weights source. gemma-4-E2B-it ships model.safetensors only (no GGUF), ~5B params, Apache-2.0. Loader claims SafeTensors parsing — verify it can feed the Gemma forward pass. OR use a Gemma GGUF if one exists.\n\nDECISION PENDING (gates feasibility/time): demo on gemma-4-E2B-it (safetensors, newest, hardest, likely no GGUF) vs a mature Gemma with existing community GGUFs (gemma-2-2b-it / gemma-3) as the demo vehicle — same arch+tokenizer work, but a real GGUF to load today.\n\nBlocks on: bd-250 (loader/arch constraints). Working Qwen-0.5B demo already proves the TUI end-to-end (interim runnable model).","notes":"MODEL-SELECTION POLICY (maintainer, 2026-07-25): once Gemma runs, DEFAULT = Gemma (gemma-4-E2B-it), FALLBACK = Qwen-0.5B on devices with < 10 GB total RAM. Implement a RAM-aware tier in select_adapter: read total system RAM; >=10GB -> Gemma, <10GB -> Qwen. 10GB threshold is right because dequant-to-f32 expands gemma-4-E2B (~5B params) to ~f32 in memory (~10GB). SEQUENCING / do-not-regress: keep Qwen the working default NOW; only flip to Gemma-default when this bead's arch+SPM-tokenizer+weights land (else >=10GB machines fall back to Echo and lose today's working Qwen chat). Ship the RAM selector together with Gemma support, not before. Qwen stays as the explicit low-RAM fallback (this supersedes the earlier 'Qwen only if user supplies GGUF' — Qwen is now the bundled low-RAM fallback tier).\n\nPOLICY REFINED (maintainer): Qwen and other Chinese-origin models (MiniCPM/openbmb, etc.) are LAST RESORT, not the fallback tier. Corrected ladder:\n- Default (>=10GB RAM): Gemma (gemma-4-E2B-it) — Google, Apache-2.0.\n- Low-RAM (<10GB): a LOWER-PARAMETER, license-clean (GPL-3.0-compatible: Apache-2.0/MIT), NON-Chinese model. Candidates to evaluate: smaller Gemma variants, SmolLM (HF, Apache-2.0), Phi (Microsoft, MIT) — final pick TBD, must also be loader-compatible.\n- LAST RESORT only: Qwen (Alibaba) + other Chinese models.\nCAVEAT: today the ONLY runnable model is Qwen (gpt2-BPE tokenizer + qwen2 arch + classic quant). The preferred non-Chinese small models mostly use SentencePiece tokenizers, so they depend on the SAME SPM-tokenizer + arch work as Gemma (this bead). So Qwen remains the INTERIM runnable stopgap until SPM+arch lands, then it demotes to last-resort. Selection stays RAM-gated (>=10GB Gemma / <10GB small non-Chinese).\n\nADDITIONS from a 2026-07-25 code read (bd-250.2 was filed covering this same ground and has been closed as a duplicate of this bead — these are the only parts not already here):\n\n1. DESIGN QUESTION TO SETTLE BEFORE ANY KERNELS: does kopitiam-runtime grow a SECOND concrete model type, or a trait both Qwen and Gemma implement? Today QwenConfig/QwenModel are named concretely throughout (config.rs literally sets FORMAT = \"qwen-config\" and its docs say it 'encodes one specific architecture family'), and kopitiam-ai's LocalAdapter is written against them. A copy-pasted second transformer is the tempting and wrong answer. Per CLAUDE.md, propose the architecture and record an AID BEFORE writing the forward pass — this is the real design work, the kernels are the easy half.\n\n2. VALIDATION METHOD. 'It emits plausible English' proves nothing — a subtly wrong forward pass does exactly that (wrong RMSNorm convention, missing sqrt(hidden_size) embedding scale, SwiGLU where GeGLU belongs). The honest check is greedy decoding with a fixed prompt and fixed seed compared against llama.cpp's output for the SAME GGUF file, token for token. Worth building that comparison harness as part of this bead, not after.\n\n3. SCOPE FENCE: gemma-4-E2B is multimodal (text+image+audio). Text-only path first, explicitly out of scope here — worth stating so it does not creep.","status":"open","priority":1,"issue_type":"feature","created_at":"2026-07-24T23:04:47.932000Z","updated_at":"2026-08-24T23:55:42.456000Z","external_ref":"gh-34","labels":["gemma","kopitiam-ai","runtime","tokenizer"],"dependencies":[{"issue_id":"bd-v3x","depends_on_id":"bd-3t0","type":"blocks","created_at":"2026-08-12T23:51:12.594000Z","created_by":"teddy0@arch-aftershock-rapid-glass-S267454"}],"comments":[{"id":1,"issue_id":"bd-v3x","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"MODEL-SELECTION POLICY (maintainer, 2026-07-25): once Gemma runs, DEFAULT = Gemma (gemma-4-E2B-it), FALLBACK = Qwen-0.5B on devices with < 10 GB total RAM. Implement a RAM-aware tier in select_adapter: read total system RAM; >=10GB -> Gemma, <10GB -> Qwen. 10GB threshold is right because dequant-to-f32 expands gemma-4-E2B (~5B params) to ~f32 in memory (~10GB). SEQUENCING / do-not-regress: keep Qwen the working default NOW; only flip to Gemma-default when this bead's arch+SPM-tokenizer+weights land (else >=10GB machines fall back to Echo and lose today's working Qwen chat). Ship the RAM selector together with Gemma support, not before. Qwen stays as the explicit low-RAM fallback (this supersedes the earlier 'Qwen only if user supplies GGUF' — Qwen is now the bundled low-RAM fallback tier).","created_at":"2026-07-24T23:34:31.512000Z"},{"id":2,"issue_id":"bd-v3x","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"POLICY REFINED (maintainer): Qwen and other Chinese-origin models (MiniCPM/openbmb, etc.) are LAST RESORT, not the fallback tier. Corrected ladder:\n- Default (>=10GB RAM): Gemma (gemma-4-E2B-it) — Google, Apache-2.0.\n- Low-RAM (<10GB): a LOWER-PARAMETER, license-clean (GPL-3.0-compatible: Apache-2.0/MIT), NON-Chinese model. Candidates to evaluate: smaller Gemma variants, SmolLM (HF, Apache-2.0), Phi (Microsoft, MIT) — final pick TBD, must also be loader-compatible.\n- LAST RESORT only: Qwen (Alibaba) + other Chinese models.\nCAVEAT: today the ONLY runnable model is Qwen (gpt2-BPE tokenizer + qwen2 arch + classic quant). The preferred non-Chinese small models mostly use SentencePiece tokenizers, so they depend on the SAME SPM-tokenizer + arch work as Gemma (this bead). So Qwen remains the INTERIM runnable stopgap until SPM+arch lands, then it demotes to last-resort. Selection stays RAM-gated (>=10GB Gemma / <10GB small non-Chinese).","created_at":"2026-07-24T23:35:24.058000Z"},{"id":3,"issue_id":"bd-v3x","author":"teddy0@arch-aftershock-rapid-glass-S267454","text":"ADDITIONS from a 2026-07-25 code read (bd-250.2 was filed covering this same ground and has been closed as a duplicate of this bead — these are the only parts not already here):\n\n1. DESIGN QUESTION TO SETTLE BEFORE ANY KERNELS: does kopitiam-runtime grow a SECOND concrete model type, or a trait both Qwen and Gemma implement? Today QwenConfig/QwenModel are named concretely throughout (config.rs literally sets FORMAT = \"qwen-config\" and its docs say it 'encodes one specific architecture family'), and kopitiam-ai's LocalAdapter is written against them. A copy-pasted second transformer is the tempting and wrong answer. Per CLAUDE.md, propose the architecture and record an AID BEFORE writing the forward pass — this is the real design work, the kernels are the easy half.\n\n2. VALIDATION METHOD. 'It emits plausible English' proves nothing — a subtly wrong forward pass does exactly that (wrong RMSNorm convention, missing sqrt(hidden_size) embedding scale, SwiGLU where GeGLU belongs). The honest check is greedy decoding with a fixed prompt and fixed seed compared against llama.cpp's output for the SAME GGUF file, token for token. Worth building that comparison harness as part of this bead, not after.\n\n3. SCOPE FENCE: gemma-4-E2B is multimodal (text+image+audio). Text-only path first, explicitly out of scope here — worth stating so it does not creep.","created_at":"2026-07-25T00:01:45.664000Z"}]}
{"id":"bd-wd0","title":"kopitiam-pdf: Type1 (/FontFile) glyph outlines + predefined-Standard CFF encoding","description":"Fixes GitHub issue theodoreOnzGit/kopitiam#31 (Type1/predefined-encoding-CFF text rendered as solid colour boxes).\n\nImplemented:\n- crates/kopitiam-pdf/src/mupdf/glyph_type1.rs: a from-spec Type1 charstring interpreter (Adobe Type 1 Font Format spec) -- PFA/PFB unwrapping, eexec + charstring decryption, hsbw/sbw/seac/flex (OtherSubrs 0-3)/div, built-in /Encoding parsing.\n- crates/kopitiam-pdf/src/mupdf/font.rs: wires Type1Program in as a name-selected sibling to the GID-selected FontProgram (new LoadedFontProgram enum), /Length1//Length2 boundary hints, per-code glyph_names retained for name-based selection.\n- crates/kopitiam-pdf/src/mupdf/glyph_cff.rs: predefined-Standard-encoding resolution for simple CFF fonts (code -> name via encodings::STANDARD -> SID via the 391-entry CFF_STANDARD_STRINGS table -> gid via the charset). Predefined Expert encoding remains a documented ceiling (falls back to advance box).\n- End-to-end draw_device.rs test (ring-glyph discriminator) proves a Type1 glyph now renders a real letterform, not a filled box.\n\nSee AID-0055 for the 'hayro-font as a dependency' decision (declined; from-spec implementation kept) and docs/ai-decisions/README.md index.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-08-24T22:56:40.089000Z","updated_at":"2026-08-24T23:48:27.343000Z","closed_at":"2026-08-24T23:02:23.982000Z","close_reason":"done","external_ref":"gh-31"}
{"id":"bd-yy1","title":"kopi-beans: --features test-harness cannot compile (use crate as crate::daemon; in testkit/e2e.rs)","description":"GH: https://github.com/theodoreOnzGit/kopitiam/issues/17\n\n`crates/kopi-beans/src/daemon/testkit/e2e.rs:3` got a line that is simply not\nvalid Rust:\n\n    use crate as crate::daemon;\n\nBad artifact from the multi-crate -> single-crate collapse (old\n`beads-daemon`/`beads-core` sub-crates became intra-crate modules). So\n`--features test-harness` cannot parse at all, and `e2e-tests` (which enables it)\nis dead too. Default build never notices because the module is gated at\n`src/daemon/testkit/mod.rs:55` with `#[cfg(feature = \"test-harness\")]`.\n\nRepro:\n    cargo check --release -p kopi-beans --features test-harness\n\nKnock-on already recorded in Cargo.toml: the `fault-proxy` feature exists as a\nseparate gate ONLY because `test-harness` is broken (see the comment at\ncrates/kopi-beans/Cargo.toml:141). Once this is fixed, revisit whether\n`fault-proxy` should fold back into `test-harness`.\n\nFix = work out what the import was meant to say. Reporter deliberately did not\nguess; check what `e2e.rs` actually references from the daemon module and\nre-point it (likely `use crate::daemon;` plus whatever items it pulls).\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-08-12T23:46:44.662000Z","updated_at":"2026-08-13T00:20:32.072000Z","closed_at":"2026-08-13T00:20:32.072000Z","close_reason":"done","external_ref":"gh-17","labels":["kopi-beans"]}
{"id":"bd-ztz","title":"runtime: tokenizer.ggml.pre is ignored — SmolLM2 gets the wrong pre-tokenizer","description":"Split out of bd-11b after the greedy-sampling half was fixed (f07327f). This is the part that is still broken, and the evidence says it is NOT just a small weak model.\n\nTHE SMOKING GUN. On SmolLM2-1.7B-Instruct, with a correctly rendered ChatML prompt ending '<|im_start|>assistant\\\\n', the first GREEDY token is <|im_end|> — the model emits an EMPTY assistant turn:\n\n    GREEDY 1.7B     => ''                      (first argmax token IS eos)\n    STOCHASTIC 1.7B => '<|im_start|>\\\\n'        (opens a NEW turn instead of answering)\n\nA model that believes the turn is already over is being fed something out of distribution. Weak weights make BAD answers, not empty ones — and raw completion on the same file answers correctly ('The capital of France is' -> ' Paris.'), so weights, dequant and forward pass are all fine.\n\nEVERYTHING ELSE IS ALREADY RULED OUT, checked against the file rather than assumed:\n  <|im_start|> -> 1, <|im_end|> -> 2, <|endoftext|> -> 0   (registered, atomic)\n  ChatML prompt round-trips through encode/decode exactly\n  tokenizer.ggml.eos_token_id = 2, which is what we resolve\n  tokenizer.chat_template in the GGUF IS ChatML — exactly what render_chatml emits\n  tokenizer.ggml.add_bos_token = false — so omitting BOS is correct here\n  sampling is now stochastic with ollama's defaults (was greedy; that was the loop bug)\n\nPRIME SUSPECT: the file declares\n\n    tokenizer.ggml.model = 'gpt2'\n    tokenizer.ggml.pre   = 'smollm'\n\nand we IGNORE tokenizer.ggml.pre completely. crates/kopitiam-tokenizer/src/pretokenize.rs always applies the one GPT-2 regex:\n\n    's|'t|'re|'ve|'m|'ll|'d| ?\\\\p{L}+| ?\\\\p{N}+| ?[^\\\\s\\\\p{L}\\\\p{N}]+|\\\\s+\n\nllama.cpp selects a DIFFERENT pre-tokenizer regex per tokenizer.ggml.pre value (LLAMA_VOCAB_PRE_TYPE_*). If smollm's differs from gpt2's, our token boundaries differ from the ones the model was trained on — which would produce exactly this: plain prose still tokenizes the same and completes fine, while a structured prompt full of newlines and control tokens lands off-distribution and the model closes the turn.\n\nNOT YET CONFIRMED — this is a strong lead, not a diagnosis. Per the new CLAUDE.md hard rule, confirm from the reference before writing anything: find the smollm branch of llama.cpp's pre-tokenizer selection (vendored under crates/kopitiam-runtime/vendor/ollama, or vendor llama.cpp directly), compare its regex to ours, and only then decide whether to add per-pre dispatch. Two bugs in this area today each had an equally plausible opposite cause and the file settled both.\n\nREPRODUCE:\n  printf 'What is the capital of France?\\\\n' | kopitiam ai chat\n  (bad reply; and with the 1.7B via KOPITIAM_MODEL_GGUF, an empty or turn-opening reply)","notes":"REFUTED, and the bead's own hypothesis was wrong. Not a pre-tokenizer bug. Not a runtime bug at all.\n\nWHAT I CHECKED, all against the vendored reference rather than by reasoning (llama.cpp now vendored at crates/kopitiam-ai/vendor/llama.cpp):\n\n  tokenizer.ggml.pre='smollm' -> llama.cpp LLAMA_VOCAB_PRE_TYPE_SMOLLM adds a leading '\\p{N}' pass and ends '\\s+(?!\\S)' where ours ends '\\s+'. REAL difference, but harmless here: our encoder ALREADY splits digits individually ('12345' -> 1,2,3,4,5), and the ChatML pieces tokenize correctly ('system\\n' -> ['system','\\n'], 'assistant\\n' -> ['ass','istant','\\n'] — 'assistant' has no single-token id in this vocab, so that split is right).\n  RoPE: split-half/NEOX convention correct; tables verified numerically against hand computation at positions 0,1,5,12,19,33,100 with the real params (rope_dim 64, theta 100000) — max abs diff 0e0.\n  Config: every value matches the file. 360M theta 100000, 1.7B theta 130000, both read correctly. head_dim/heads/kv_heads/ffn/layers/eps all correct.\n  Tensor primitives: transpose+matmul vs hand loops at real shapes (15x19x19x64), softmax normalisation, scalar-rank-0 division, rms_norm, and the causal mask BROADCASTING over all 15 heads (the one my first probe could not have caught, since the mask is all-zeros for seq=1 and for the final row).\n  KV cache: concat on dim 1, verified. Prefill vs one-token-at-a-time agree exactly.\n  Quantization vs ggml-quants.c: dequant_q4_k matches dequantize_row_q4_K byte for byte, get_scale_min_k4 matches including the q[j-0] subtlety, dequant_q6_k matches including the SIGNED 'as i8' scales trap.\n\nWHERE I WENT WRONG. I built a length ladder with 'The weather is mild.' repeated five times and read the degradation as a runtime bug. That prompt is precisely the pattern that makes induction heads copy, so it measured repetition, not length. I rebuilt it with distinct sentences and still called it conclusive — also wrong, because unrelated prefix legitimately shifts a 360M's distribution ('Paris' at rank 63 of 49152 is still top-0.1 percent). Canonical completions settle it: 'The quick brown fox jumps over the lazy' -> ' dog.' and an 18-token 'Once upon a time...near the' -> ' sea. She had a'. The runtime is fine at these lengths.\n\nWHAT REMAINS UNEXPLAINED, and is NOT a runtime defect: SmolLM2-1.7B-Q4_K_M, given a correct ChatML prompt, puts <|im_end|>=18.50 and <|im_start|>=17.75 as its top two logits right after '<|im_start|>assistant\\n' — it wants to end the turn rather than answer. The SAME file does raw completion correctly (' Paris, the city of light, the'; ' dog') so its weights are being applied correctly. The 360M in the same code path is sane there ('The'=13.70, 'Yes', 'A'). So this is specific to that model/quantisation's chat behaviour, not to our stack. Filed as a separate observation, not a runtime bug.\n\nLANDED FROM THIS: the netfetch harness now asserts a canonical completion, not merely 'the chain ran'. That hole is what let a plausible-looking PASS coexist with gibberish and sent this whole hunt off. Also, vendoring llama.cpp restored ggml-vocab-qwen2.gguf, so bd-klf's three tests pass locally now (they will still fail on a fresh clone, since vendor/ is gitignored — that bead stands).","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-27T14:52:43.554000Z","updated_at":"2026-08-24T23:55:42.632000Z","closed_at":"2026-07-27T15:47:36.977000Z","close_reason":"done","external_ref":"gh-54","labels":["kopitiam-runtime","models"],"comments":[{"id":1,"issue_id":"bd-ztz","author":"fifad@Teddy-Thinkpad","text":"REFUTED, and the bead's own hypothesis was wrong. Not a pre-tokenizer bug. Not a runtime bug at all.\n\nWHAT I CHECKED, all against the vendored reference rather than by reasoning (llama.cpp now vendored at crates/kopitiam-ai/vendor/llama.cpp):\n\n  tokenizer.ggml.pre='smollm' -> llama.cpp LLAMA_VOCAB_PRE_TYPE_SMOLLM adds a leading '\\p{N}' pass and ends '\\s+(?!\\S)' where ours ends '\\s+'. REAL difference, but harmless here: our encoder ALREADY splits digits individually ('12345' -> 1,2,3,4,5), and the ChatML pieces tokenize correctly ('system\\n' -> ['system','\\n'], 'assistant\\n' -> ['ass','istant','\\n'] — 'assistant' has no single-token id in this vocab, so that split is right).\n  RoPE: split-half/NEOX convention correct; tables verified numerically against hand computation at positions 0,1,5,12,19,33,100 with the real params (rope_dim 64, theta 100000) — max abs diff 0e0.\n  Config: every value matches the file. 360M theta 100000, 1.7B theta 130000, both read correctly. head_dim/heads/kv_heads/ffn/layers/eps all correct.\n  Tensor primitives: transpose+matmul vs hand loops at real shapes (15x19x19x64), softmax normalisation, scalar-rank-0 division, rms_norm, and the causal mask BROADCASTING over all 15 heads (the one my first probe could not have caught, since the mask is all-zeros for seq=1 and for the final row).\n  KV cache: concat on dim 1, verified. Prefill vs one-token-at-a-time agree exactly.\n  Quantization vs ggml-quants.c: dequant_q4_k matches dequantize_row_q4_K byte for byte, get_scale_min_k4 matches including the q[j-0] subtlety, dequant_q6_k matches including the SIGNED 'as i8' scales trap.\n\nWHERE I WENT WRONG. I built a length ladder with 'The weather is mild.' repeated five times and read the degradation as a runtime bug. That prompt is precisely the pattern that makes induction heads copy, so it measured repetition, not length. I rebuilt it with distinct sentences and still called it conclusive — also wrong, because unrelated prefix legitimately shifts a 360M's distribution ('Paris' at rank 63 of 49152 is still top-0.1 percent). Canonical completions settle it: 'The quick brown fox jumps over the lazy' -> ' dog.' and an 18-token 'Once upon a time...near the' -> ' sea. She had a'. The runtime is fine at these lengths.\n\nWHAT REMAINS UNEXPLAINED, and is NOT a runtime defect: SmolLM2-1.7B-Q4_K_M, given a correct ChatML prompt, puts <|im_end|>=18.50 and <|im_start|>=17.75 as its top two logits right after '<|im_start|>assistant\\n' — it wants to end the turn rather than answer. The SAME file does raw completion correctly (' Paris, the city of light, the'; ' dog') so its weights are being applied correctly. The 360M in the same code path is sane there ('The'=13.70, 'Yes', 'A'). So this is specific to that model/quantisation's chat behaviour, not to our stack. Filed as a separate observation, not a runtime bug.\n\nLANDED FROM THIS: the netfetch harness now asserts a canonical completion, not merely 'the chain ran'. That hole is what let a plausible-looking PASS coexist with gibberish and sent this whole hunt off. Also, vendoring llama.cpp restored ggml-vocab-qwen2.gguf, so bd-klf's three tests pass locally now (they will still fail on a fresh clone, since vendor/ is gitignored — that bead stands).","created_at":"2026-07-27T15:47:18.238000Z"}]}
