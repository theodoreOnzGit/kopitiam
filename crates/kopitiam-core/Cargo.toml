[package]
name = "kopitiam-core"
# Graduated from its 0.0.1 name-reservation seed: now releases in lockstep with
# the rest of the tree at the workspace version.
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Core primitives (dtypes, shapes, devices, errors) for the Kopitiam Runtime, KOPITIAM's CPU-first local inference engine."

[dependencies]
thiserror.workspace = true
