[package]
name = "kopitiam-tensor"
# Graduated from its 0.0.x name-reservation seed: now releases in lockstep with
# the rest of the tree at the workspace version (kopitiam-core, which it depends
# on, graduates the same way).
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Kopitiam Runtime: tensor (work in progress)."

[dependencies]
kopitiam-core.workspace = true
